# IMPLEMENTATION_PROGRESS_LEDGER

Her batch bir satır. Statüler: DONE / IN_PROGRESS / BLOCKED / ROLLED_BACK.

| Batch | Phase | Model | Tarih | Scope | Testler | Rollback | Statü |
|---|---|---|---|---|---|---|---|
| PHASE0-AUDIT | 0 | Fable 5 | 2026-07-03 | 5-belge reconciliation + repo/runtime audit + 20 canonical artifact + Q-matrix CSV + protokol v1.1 | test koşulmadı (read-only faz; taban 119/119 §39'dan) | artifact dosyalarını sil (kod/runtime etkisi sıfır) | DONE |
| BATCH-P1-001 | 1 | Sonnet 5 | 2026-07-03 | warehouse iskeleti: `ami/warehouse/` (schema.py/init_db.py) + `data/ami/canonical.sqlite` (8 tablo, M-0001) | 4/4 ✓ (test_ami_warehouse_schema.py) | new-files-only (ami/warehouse/, data/ami/canonical.sqlite, test dosyası) | DONE |
| BATCH-P1-002 | 1 | Sonnet 5 | 2026-07-03 | artifact_ingest.py: root *.md + docs/ami/** + docs/protocols/** → artifact_registry (64 kayıt: 62 CANONICAL + 2 UNDER_RECONCILIATION); AMI-collision gate (0 quarantine; heuristik düzeltildi — self-aware AMI belgesi yanlış-pozitif vermiyor) | 6/6 ✓ (test_ami_warehouse_ingest.py) | new-file-only (artifact_ingest.py + test); ingest idempotent (2× çalıştırıldı, 64/64) | DONE |
| BATCH-P1-003 | 1 | Sonnet 5 | 2026-07-03 | question_seed.py: CSV(1058) + research.sqlite slug(14) → question_registry (1072) + question_families (29) | 5/5 ✓ (test_ami_warehouse_question_seed.py) | new-file-only; seed idempotent (2× çalıştırıldı, 1072/1072) | DONE |
| BATCH-P1-004 | 1 | Sonnet 5 | 2026-07-03 | registry_seed.py: CONFLICT-001..010 + CT-001..004 → contradiction_registry(14); OD-001..010 → operator_decision_queue(10); whitepaper v0.2↔v0.3 UNDER_RECONCILIATION → artifact_lineage(1). **Phase 1 TAMAMLANDI.** | 7/7 ✓ (test_ami_warehouse_registry_seed.py) + 1 yeni dump-roundtrip testi (test_ami_warehouse_schema.py, DoD §23.1) | new-file-only; seed idempotent (2× çalıştırıldı, 14/10/1) | DONE |
| BATCH-P2-001 | 2 | Sonnet 5 | 2026-07-03 | schema v1→v2: evidence_contamination, researcher_exposure_ledger, mt_family_registry, causal_assumption_registry, data_quality_events, market_structure_versions (M-0002) | 6/6 ✓ (test_ami_warehouse_schema.py, genişletildi) | new-schema-only (CREATE TABLE IF NOT EXISTS, v1 tabloları etkilenmedi) | DONE |
| BATCH-P2-002 | 2 | Sonnet 5 | 2026-07-03 | `ami/timing/contract.py` — known-at/available-at kontratı (Obs §6): enforce_known_at, reject_if_partial_candle, TimestampedValue | 10/10 ✓ (test_ami_timing_contract.py) | new-file-only (ami/timing/) | DONE |
| BATCH-P2-003 | 2 | Sonnet 5 | 2026-07-03 | `ami/warehouse/funding_oi_audit.py` — OD-006 read-only funding/OI denetimi → data_quality_events(7). **Bulgu:** OI canlı (oi_spot_poller); funding_rates'in hiçbir canlı üreticisi yok. Yeni collector başlatılmadı; OD-006 bulgularla güncellendi, OPEN kaldı. | 4/4 ✓ (test_ami_warehouse_funding_oi_audit.py) | new-file-only; audit read-only (mode=ro), idempotent (2× çalıştırıldı, 7/7) | DONE |
| BATCH-P3-001 | 3 | Sonnet 5 | 2026-07-03 | schema v2→v3: ami_events/ami_cycles/event_cycle_membership (M-0003); `ami/identity/event_identity.py` (immutable event_id + real-vs-proxy pooling guard R-09) + `ami/identity/shadow_ledger_ingest.py` (gerçek shadow ledger → 251 event, anchor-seviyesinde dedup, hepsi REAL_LIQUIDATION) | 7/7 (schema, genişletildi) + 7/7 (event_identity) + 7/7 (shadow_ledger_ingest) | new-file/schema-only; ingest idempotent (2× çalıştırıldı, 251/251) | DONE |
| BATCH-P3-002 | 3 | Sonnet 5 | 2026-07-03 | `ami/identity/cooldown_sensitivity.py` — Protocol §8.4 non-canonical cooldown view'ları (1h/2h/4h/6h/12h/24h) → event_cycle_membership(1506, is_canonical=0) | 5/5 ✓ (test_ami_identity_cooldown_sensitivity.py) | new-file-only; idempotent (2× çalıştırıldı, 1506/1506) | DONE |
| BATCH-P3-003 | 3 | Sonnet 5 | 2026-07-03 | `ami/identity/split_utils.py` — cycle-grouped chronological split + purge/embargo (OD-003'ten bağımsız, jenerik). **Phase 3 TAMAMLANDI** (ami_cycles hariç — OD-003 bekliyor, bkz. OPERATOR_DECISION_QUEUE.md). | 8/8 ✓ (test_ami_identity_split_utils.py) | new-file-only | DONE |
| FABLE-SUBSTITUTE-REVIEW-A | 1-3 gate | **Fable 5 (Opus 4.8 mevcut değil — substitute)** | 2026-07-03 | Phase 1-3 read-only mimari/bilimsel/güvenlik incelemesi. Bağımsız ledger→warehouse çapraz doğrulama (300 OPEN→269 trade→251 anchor birebir eşleşti). **Verdict: PASS_WITH_REMEDIATION** — 0 blocker, 6 remediation (F1-F6, SYSTEM_STATE §46). OD-003 önerisi: A2+B2+C2 (karar operatörde). | test koşulmadı (read-only review; taban 187/187) | yok (hiçbir dosya/schema değişmedi; yalnız SYSTEM_STATE+LEDGER+OD-kuyruk güncellemesi) | DONE |
| BATCH-P3-004 | 3 (remediation) | Sonnet 5 | 2026-07-03 | F1 (event_family route-scoped, belgelendi + immutability testi), F2 (duplicate-OPEN kimlik-alanı kontrolü, `DuplicateOpenConflict`), F4 (`PRAGMA foreign_keys=ON`), F5 (schema_versions.note dinamik), F6 (notional=max() belgesi). **Yan bulgu:** FK enforcement, `question_seed.py`'de gerçek bir bug ortaya çıkardı — `question_registry.family_id`, `question_families.family_id` ile eşleşmiyordu (1058/1058 satır); kod + UPSERT + gerçek veri onarıldı (M-0004) | 9/9 (schema) + 10/10 (shadow_ledger_ingest, +3 yeni) + 5/5 (question_seed) + 7/7 (registry_seed, +stub fixture) | new-code + veri onarımı; idempotent | DONE |
| BATCH-P3-005 | 3 | Sonnet 5 | 2026-07-03 | **OD-003 operatör onayı: A2+B2+C2.** `ami/identity/cycle_resolver.py` — canonical cycle_definition_version="canonical-v1" (immutable): symbol+family+4h continuity gate+`ami/states/engine.py` point-in-time dominant-structural-state (1h TF); cascade-continuity/shared-parent NOT_IMPLEMENTED (belgeli). 2-durum censoring; direction-conflict flag-only (WAIT). ami_cycles: **167 cycle** (252 event'ten, 15 direction_conflict). Sensitivity view'lar DOKUNULMADI (append-only). **Phase 3 TAMAMEN KAPANDI (OD-003 dahil).** | 11/11 ✓ (test_ami_identity_cycle_resolver.py) | new-file-only; idempotent (2× çalıştırıldı, 167/167) | DONE |
| BATCH-P4-001 | 4 | Sonnet 5 | 2026-07-03 | Schema v3→v4 (M-0006): `ami_candles`+`ami_candle_morphology`. `ami/chart/candle_builder.py` — gerçek `agg_trades`'ten closed-candle-only OHLCV inşası (known_at_ts=close_ts_ms, still-forming bucket asla saklanmaz, boş bucket fabrikasyon yerine atlanır); ETHUSDT 1m+5m, 48h lookback → **3456 candle** (2880×1m+576×5m), 0 OHLC ihlali. `ami/chart/candle_morphology.py` — §6.1 atomik özellikler + 3-durum close-quality (NEAR_HIGH/NEAR_LOW/MID_RANGE; provisional eşik, trained değil — belgeli); REJECTION/ACCEPTANCE/FOLLOW_THROUGH NOT_IMPLEMENTED (Level nesnesi gerektirir, P4-003'e ertelendi). | 9/9 (schema, güncellendi) + 7/7 (builder) + 6/6 (morphology) | new-file/schema-only; idempotent (2× çalıştırıldı, 3456/3456) | DONE |
| BATCH-P4-002 | 4 | Sonnet 5 | 2026-07-03 | `ami_swings` (schema v4 devamı). `ami/chart/swing_extractor.py` — §4.2 confirmed Swing: N=3 simetrik fraktal, known_at_ts=confirming-candle.close_ts_ms (pivot_ts'ten her zaman sonra, testli). Gerçek ETHUSDT 1m'den **556 swing** (294 HIGH/262 LOW), 0 known_at≤pivot ihlali. Flat-top tie'lar fabrikasyon yerine atlanır. | 6/6 ✓ (test_ami_chart_swing_extractor.py) | new-file/schema-only; idempotent (2× çalıştırıldı, 556/556) | DONE |
| BATCH-P4-003 | 4 | Sonnet 5 | 2026-07-03 | `ami_levels` (schema v4 devamı). `ami/chart/level_registry.py` — §4.3 Level: SESSION_HIGH/LOW + PREVIOUS_DAY_HIGH/LOW (session sınırları `s34_state_machine_live_executor.py`'den read-only referans alındı, import edilmedi) + SWING_HIGH/LOW (P4-002'den). Touch/rejection/acceptance istatistikleri hesaplandı. Diğer 12 level_type NOT_IMPLEMENTED (volume-profile/breakout altyapısı yok, belgeli). Gerçek veriden **576 level**. | 6/6 ✓ (test_ami_chart_level_registry.py) | new-file/schema-only; idempotent (2× çalıştırıldı, 576/576) | DONE |
| BATCH-P4-004 | 4 | Sonnet 5 | 2026-07-03 | `ami_pushes` (schema v4 devamı). `ami/chart/push_geometry.py` — §7.1 Push: ardışık alternating swing çiftleri; pullback_after_bps sonraki push'tan; known_at_ts ileri taşınır. **2 gerçek bug bulunup düzeltildi** (path_length hareketli-taban + swing-pivot uç segmenti eksikliği — efficiency_ratio>1 ile yakalandı, üçgen eşitsizliği sınır-testiyle kilitlendi). Gerçek veriden **454 push** (228 UP/226 DOWN), 0 ihlal, ort. efficiency=0.86. **Phase 4 TAMAMLANDI.** | 7/7 ✓ (test_ami_chart_push_geometry.py, +sınır testi) | new-file/schema-only; idempotent (2× çalıştırıldı, 454/454) | DONE |
| BATCH-P5-001 | 5 | Sonnet 5 | 2026-07-03 | Feature dictionary (SCHEMA_DICTIONARY.md'de konsolide edildi) + duplicate-engine denetimi: `tools/s34_v_engine_failure_anatomy.py:candle_features()` farklı-normalizasyonlu tarihsel bir mum-morfolojisi hesaplayıcısı bulundu — DOKUNULMADI (geçmiş araştırma, reproducibility). Kural: Phase 6+ yeni chart-feature ihtiyacı `ami/chart/*` kullanır. known-at testleri 4 nesnede de doğrulandı (candle/swing/level/push). **Phase 5 TAMAMLANDI.** | test koşulmadı (audit-only, mevcut 235/235 taban) | yok (yalnız dokümantasyon) | DONE |
| FABLE-SUBSTITUTE-REVIEW-B | 4-5 gate | **Fable 5 (Opus 4.8 mevcut değil — substitute)** | 2026-07-03 | Phase 4-5 read-only mimari/matematiksel/temporal/araştırma-güvenliği incelemesi. Bağımsız SQL çapraz-doğrulama: tüm candle/swing/push invariant'ları geçti; push onarımı teyit; duplicate engine korunmuş+coupling'siz. **Verdict: PASS_WITH_REMEDIATION** — 0 blocker, 5 bulgu (F-B1..B5, SYSTEM_STATE §49): **F-B4 MANIFEST** (pencere-kenarı truncation → depoda 4 hatalı session/day level satırı), F-B1 latent lookahead (level known_at sınıra değil son muma bağlı), F-B2 touch-stats point-in-time değil, F-B3 gaps-tablosu (812 kayıt) ile DQ çapraz-işaretleme yok, F-B5 gateway enforcement eksik (doc-only). | test koşulmadı (read-only review; taban 235/235) | yok (hiçbir dosya/kod/veri değişmedi) | DONE |
| BATCH-P6-000 | 6-ön-koşul | Sonnet 5 | 2026-07-03 | Schema v4→v5 (M-0007): `ami_levels.touch_stats_point_in_time` (idempotent ALTER). **F-B4 fix:** level_registry pencere-kenarı truncation kontrolü — gerçek veride 1 hatalı truncated SESSION_HIGH/LOW çifti tespit edilip kaldırıldı (`level-v1`→`level-v2-boundary-safe`, eski satırlar kontrollü silindi). **F-B1 fix:** known_at_ts=max(son mum close, gerçek dönem sınırı). **F-B3 fix:** candle_builder artık `gaps` tablosuyla (stream=agg_trades) çapraz-işaretleme yapıyor (GAPPED). **F-B2:** touch_stats_point_in_time her satırda 0 — point-in-time-safe DEĞİL olarak işaretlendi. | 21/21 (schema+candle_builder) + 12/12 (morphology+swing) + 18/18 (level+push) = 51 test | mevcut dosyalara ek + veri onarımı; idempotent (2× çalıştırıldı) | DONE |
| BATCH-P6-001 | 6-ön-koşul | Sonnet 5 | 2026-07-03 | F-B5/F3: `ami/research/feature_gateway.py` — Phase 6'nın zorunlu tek erişim noktası. `fetch_events` REAL+PROXY karışımını reddeder; `fetch_level_features` F-B2 kolonlarını flag=1 olmadan bloklar; her erişim `researcher_exposure_ledger`'a kaydedilir; bilinmeyen tablo/kolon reddedilir. **BATCH-P6-000+001 ile Phase 6 ön-koşulları TAMAMLANDI.** | 10/10 ✓ (test_ami_research_feature_gateway.py) | new-file-only | DONE |
| BATCH-P6-002 (W1) | 6 | Sonnet 5 | 2026-07-04 | **W1 — Cycle Integrity & Deduplication.** Schema v5→v6 (M-0009): `experiment_registry`+`experiment_results`. `ami/research/w1_cycle_integrity.py` — yalnız `feature_gateway` üzerinden REAL_LIQUIDATION population + canonical-v1 cycles; sonuçlar canonical SQL'e yazıldı (E-W1-CYCLE-INTEGRITY-001). **Gerçek veri:** 270 raw trade → 252 anchor → 167 cycle (anchor_to_cycle=0.66); 16 multi-route anchor; 15 direction_conflict; cooldown sensitivity 1h=152/2h=111/4h=88/6h=82/12h=55/24h=38. **2 gerçek bug bulunup düzeltildi:** exposure_id UUID çakışması + experiment_results idempotency eksikliği. **OD-011 eklendi** (mevcut KO'lar cycle-adjusted N kullanmıyor — bucket'lara DOKUNULMADI, yalnız flag). `feature_gateway`'e `fetch_cycles()` + genel `equals` filtresi eklendi. | 17/17 (gateway+W1, gateway'e +1) | new-file/schema-only + veri; idempotent (2× çalıştırıldı, 12/12 sonuç) | DONE |
| BATCH-P6-003 | 6 | Sonnet 5 | 2026-07-04 | **All-timestamp candidate universe (Protocol §17.8).** Schema v6→v7 (M-0011): `ami_candidate_universe` — event varlığına koşullanmadan her kapanmış mum slotu candidate satırı; known-at-safe (`known_at_ts=close_ts_ms`), gap-aware (data_quality miras), deterministik id. `ami/research/candidate_universe.py`: `build_universe`/`seed`/`freeze_and_record` (4 denominatörü ayrı canonical SQL satırı olarak yazar: E-CANDIDATE-UNIVERSE-001, W1'i SÜPERSEDE ETMEZ). **Kapsam-uyuşmazlığı bulundu (gizlenmedi):** `ami_candles` Phase 4'ten kalma 48h bounded lookback ile sınırlı — candidate universe bunu miras alıyor, ama event_n/anchor_n/cycle_n (W1) tüm tarihi kapsıyor; bu yüzden all-history VE pencere-kapsamlı sayılar AYRI metrik olarak yazıldı (`anchor_n_in_candidate_window`=22 vs `anchor_n_all_history`=252), oranlar yalnız pencere-kapsamlı sayıyla hesaplandı. **Gerçek veri:** raw_candidate_n=2932 (22 event-aligned, 2910 no-event), pencere-kapsamlı anchor_n=22/cycle_n=17. **OD-011 zenginleştirildi:** `CONTRADICTION_REGISTER.md` CT-005 — 11 KO'nun 8'i RECOMPUTE_REQUIRED (K-S34-HOUR17-001 dahil, en yüksek öncelik), 3'ü AFFECTED (descriptive); `knowledge.sqlite` DEĞİŞTİRİLMEDİ; canonical cycle-adjusted recomputation ayrı kontrollü dalga olarak planlanacak (bu batch YÜRÜTMEDİ). | 12/12 ✓ (test_ami_research_candidate_universe.py) + 7/7 (registry_seed, CT-005 için 3 güncellendi) — **toplam AMI 274/274** | new-file/schema-only + veri; idempotent (2× çalıştırıldı, 2932=2932 satır SQL ile doğrulandı) | DONE |
| BATCH-P6-003b | 6 | Sonnet 5 | 2026-07-04 | **Candle full-history backfill + W2 mezarlık çakışması.** `ami/chart/candle_builder.py`: yeni `build_candles_streaming()` (agg_trades'i [173M satır] TEK ORDER BY ts_ms geçişte, fetchmany chunk'larıyla belleği sınırlı tarar — eski per-bucket yaklaşım ~200K sorgu/saatler/40GB+ RAM riski taşırdı) + `derive_higher_timeframe()` (5m'i 1m'den Python'da türetir, ikinci DB taraması yok) + `seed_full_history()`. **Gerçek bug bulunup düzeltildi:** SQL alt-sınırı `start_ts_ms` kullanıyordu, bucket-hizalı `bucket_open` değil — ilk bucket'ın başı sessizce kayboluyordu (3h cross-check'te trade_count 499≠184 ile yakalandı); düzeltme sonrası 3h+24h'de referans (`build_candles`) ile TAM eşitlik (5m'de yalnız kayan-nokta toplama-sırası gürültüsü, max 2.9e-11). **Gerçek veri:** 173,464×1m+34,801×5m candle, 910.8s (15.2dk, tek sıralı proses), 0 OHLC ihlali, 252/252 anchor artık pencere içinde (önceki 22/252'den). **W2 mezarlık çakışması bulundu:** `failure_archive` #8 (BUY-side fade, retry_condition boş) + #17 (BUYFADE LONG genesis T0, retry_condition="OI birikince") ile çakışıyor; #17 kontrol edildi, OI verisi (ETHUSDT) 75 günlük tek boşluklu, gerçek kapsama yalnız 38/252 anchor (%15, 2 kopuk pencere) — retry-koşulu ZAYIF. **Operatör kararı: W2 ERTELENDİ (OD-012), W3'e (entry timing) geçildi.** Mezarlık ailesi tekrar açılmadı. | 8/8 yeni (test_ami_chart_candle_builder.py: streaming×4+derive×3+seed_full_history×1) + 7/7 (registry_seed, OD-012 için 3 güncellendi) — **toplam AMI 282/282** | yeni fonksiyonlar (mevcut build_candles/seed dokunulmadı) + veri; idempotent (UNIQUE constraint + testle kilitli) | DONE |
| BATCH-P6-004 | 6 | Sonnet 5 | 2026-07-04 | **W3 entry-timing due-diligence reconciliation — W3 ZORLA AÇILMADI.** `ami/research/w3_entry_timing_reconciliation.py`: `reports/research/s34/`'daki 10 mevcut entry-timing raporu (ABSORPTION_TIMING×2, CONFIRMATION_ENTRY, EARLY_BUILD_ENTRY, ENTRY_OFFSET_DECOMPOSITION×2, MICRO_ENTRY_SCALP, SELL_DELAYED_LONG_SCAN, V_ENGINE_CONFIRMATION_DELAY_SWEEP, V02_ENTRY_QUALITY_NAVIGATION) `failure_archive` ile read-only reconcile edildi. **9/10 rapor:** EXACT_HYPOTHESIS_ALREADY_TESTED+ECONOMICALLY_REJECTED (#1 naive early, #2 micro-timing, #8 buy-side fade, #16 BUYFADE delay600 ile eşleşiyor) veya SUPERSEDED (execution-realism diagnostic) veya INSUFFICIENT_SAMPLE (N=11-22 operasyonel). **1/10 gerçek açık soru:** absorption-timing point-in-time permission-feature geçerliliği (N=36-51) — mezarlıktan farklı ama OI sorunuyla (§53/OD-012) aynı yetersiz-örneklem riski taşıyor. **Operatör talimatı gereği W3 bu temelde zorlanmadı**; sentez canonical SQL'e yazıldı (E-W3-ENTRY-TIMING-RECONCILIATION-001), hiçbir yeni prereg/mezarlık-tekrarı açılmadı. Sıradaki aday: W4 (post-event path taxonomy, önkoşul zaten sağlanmış). | 6/6 ✓ (test_ami_research_w3_entry_timing_reconciliation.py) — **toplam AMI 288/288** | new-file-only + veri; idempotent (2× çalıştırıldı, registry=1/results=11) | DONE |
| BATCH-P6-005 | 6 | Sonnet 5 | 2026-07-04 | **W4 — Post-event path taxonomy + structural location + event geometry (preregistered + çalıştırıldı).** Ön-hazırlık (M-0013): `ami_swings`/`ami_levels`/`ami_pushes`/`ami_candidate_universe` kod değişikliği olmadan tam-tarihe genişledi (32,055 swing/33,259 level/24,940 push, 0 ihlal; candidate_universe 173,464 satır, 252/252 anchor, cycle_n=167=167). `ami/research/w4_post_event_path_taxonomy.py`: dondurulmuş popülasyon (anchor_n=252/independent_cycle_n=167, W1 ile aynı), 4 sabit ufuk (scalp_30m/1h, swing_4h/24h), sabit CONTINUATION/REVERSAL/CHOP bandı (±20bps, fit edilmedi), 3 known-at-safe yapısal-konum bayrağı, 5 sabit karşılaştırma ailesi, kronolojik 70/30 stabilite, candidate-universe negative control. **Saf tanımlayıcı — mezarlıktaki reversal-fade hipotezinin (ETH N=346, 0 leads) tekrarı DEĞİL, ekonomik iddia YOK.** `feature_gateway`'e `ami_candidate_universe` eklendi. **Gerçek bulgu:** swing ufkunda REVERSAL 54.3% (scalp'te 33.1%); anchor'lar rastgele-zamandan +10.6 puan daha REVERSAL-ağırlıklı (57.6% vs 47.0%) — mean-reversion-after-cascade descriptive olarak GERÇEK, önceki bulguyla bağımsız doğrulandı. **C2/C3/C4 DEGENERATE** (yapısal bayraklar %100 True — swing/level/push çok yoğun, sabit eşik ayırt edici değil); eşik geriye dönük DEĞİŞTİRİLMEDİ, dürüstçe raporlandı. | 10/10 ✓ (test_ami_research_w4_post_event_path_taxonomy.py) + 1/1 (gateway, ami_candidate_universe) — **toplam AMI 299/299** | new-file/veri; idempotent (registry=1/results=8, 2× doğrulandı) | DONE |
| BATCH-P6-006 | 6 | Sonnet 5 | 2026-07-04 | **W5a — Candle morphology + swing grammar (W5'in 4 bileşeninden 2'si).** Ön-hazırlık: `ami_candle_morphology` kod değişikliği olmadan tam-tarihe genişledi (208,265 satır, 6.6s). Due-diligence: sweep/breakout-retest NOT_IMPLEMENTED, unconditional SHORT genesis whitepaper §29 feature'ları henüz yok (W2'yi bloke eden aynı sınıf altyapı-eksikliği) — **bu 2 bileşen OD-014 ile backlog'a alındı, zorlanmadı**; mevcut S34_SHORT_CONVICTION/SHORT_SCORE3_GAUNTLET raporları FARKLI (BTC-liq-anchored, zaten aktif) mekanizma, çakışma yok. `ami/research/w5a_morphology_swing_grammar.py`: outcome W4'ten AYNEN yeniden kullanıldı (target-shopping yok), 2 yeni sabit özellik (anchor candle morphology, pre-anchor Dow-theory swing-grammar HH/HL vs LH/LL, known-at-safe), 2 sabit karşılaştırma. **Gerçek bulgu (null-sonuç, geçerli):** morphology (58.3%/60.3%/56.5%) ve swing-grammar (59.3%/57.4%/56.8%) bucket'ları REVERSAL oranını (~%57 baz) anlamlı ayırt etmiyor — arama alanını daraltan geçerli bir null. | 8/8 ✓ (test_ami_research_w5a_morphology_swing_grammar.py) — **toplam AMI 307/307** | new-file/veri; idempotent (registry=1/results=4, 2× doğrulandı) | DONE |
| BATCH-P6-007 | 6 | Sonnet 5 | 2026-07-04 | **W6 — Compression + relative strength + session (W6'nın 4 bileşeninden 3'ü).** Coverage kontrolü: compression HAZIR (StructurePhase.COMPRESSION, Phase 3'te zaten kullanılıyor), session HAZIR (level_registry sınırları), RS BUILDABLE (BTC+ETH mark_prices tam kapsıyor, ilk kez hesaplandı); channel NOT_IMPLEMENTED — **OD-015 ile backlog'a alındı, zorlanmadı**. `ami/research/w6_compression_rs_session.py`: popülasyon+outcome W4'ten AYNEN korundu, 3 yeni sabit özellik (compression binary, RS 1h-getiri-farkı işareti [0'da sabit ayrım], session), 3 sabit karşılaştırma. W5 null sonucu değiştirilmedi/kurtarılmadı. **Gerçek bulgu:** RS anlamlı ayırt ediyor (RS_ETH_STRONG REVERSAL %68.1 vs RS_ETH_WEAK %53.4, ~15 puan, n=69/174); compression cascade anlarında neredeyse yok (n=3/252, beklenen); session'da ASIA/US arasında fark yok (%56.1/%56.4). | 7/7 ✓ (test_ami_research_w6_compression_rs_session.py) — **toplam AMI 314/314** | new-file/veri; idempotent (registry=1/results=5, 2× doğrulandı) | DONE |
| BATCH-P6-008 | 6 | Sonnet 5 | 2026-07-04 | **W6-RS confirmation wave.** Tek dondurulmuş hipotez (RS_threshold=0/lookback=1h, sweep YOK) confirmatory sınandı. `feature_gateway.fetch_events()`'e `notional` eklendi. `ami/research/w6rs_confirmation.py`: Wilson95 CI, one-sided permütasyon (n=2000), kronolojik 70/30'da AYRI RS-koşullu replikasyon, aylık stabilite, candidate-universe negative control, notional/session/day_trend_bps confounder kontrolleri. **Nüanslı sonuç:** analyzed_n=243 (9 eksik=100% no-horizon-data açıklamalı), independent_cycle_n=159. Yön TRAIN(+16.7pp)/TEST(+7.6pp)'te tutarlı, p=0.0255 (sınırda). **AMA negative control benzer etki gösteriyor (+11.8pp rastgele zamanlarda)** — cascade-spesifik olmayabilir; day_trend_bps confounder'ı belirgin farklı (-77 vs -142bps); 2026-06'da yön tersine döndü; 2026-05'te 0 anchor (gerçek boşluk). **Trade/PnL iddiası veya bucket promotion YAPILMADI** — ekonomik validation için otomatik yeşil ışık verilmedi. | 11/11 ✓ (test_ami_research_w6rs_confirmation.py) + 1/1 (gateway notional) — **toplam AMI 326/326** | new-file/veri + feature_gateway ek kolon; idempotent (registry=1/results=17, 2× doğrulandı) | DONE |
| BATCH-P6-009 | 6 | Sonnet 5 | 2026-07-04 | **W6-RS confound-resolution wave (POST-HOC SENSITIVITY/SPECIFICITY — confirmatory DEĞİL).** RS_threshold=0/lookback=1h/24h outcome AYNEN donuk, sweep yok. `ami/research/w6rs_confound_resolution.py`: numpy ile elle IRLS logistic regression (statsmodels yok); anchor_status×RS_group interaction ana test; day_trend_bps+session+month sabit kovaryat (notional joint modele girmedi, control zamanlarında tanımsız); cluster block-bootstrap (n=2000, canonical-v1 cycle_id) interaction CI'ı için; overlap/positivity+aylık yön+kronolojik holdout kontrolleri. **Gerçek bulgu:** anchor_n=243/control_n=1985/independent_cycle_n=159; interaction=+0.397 (nokta) ama bootstrap CI=(−0.28,1.08) sıfırı içeriyor; Haziran+Temmuz yönü kovaryat-düzeltmesinden sonra da uyumsuz (month_2026-07 katsayısı ayrışma riski gösteriyor, not edildi). Operatörün 3 karar kuralı mekanik uygulandı → **VERDICT=REGIME_DEPENDENT_CONTINUE_ACCUMULATING** (kural #3: Haziran anomalisi açıklanamadı) — ne GENERAL_REGIME_FEATURE ne CASCADE_SPECIFIC kesin sınıflandırılmadı. Ekonomik validation'a geçilmedi, bucket/route/observer değişmedi. | 10/10 ✓ (test_ami_research_w6rs_confound_resolution.py) — **toplam AMI 336/336** | new-file/veri; idempotent (registry=1/results=9, 2× doğrulandı) | DONE |
| BATCH-P6-010 (W7A) | 6 | Sonnet 5 | 2026-07-04 | **State/Structure Aging & Market Clocks — preregistration'a operatör 5 düzeltme uyguladı, sonra dondurulup çalıştırıldı.** `feature_gateway.fetch_events()`'e `event_end_ts_ms` eklendi (additive). `ami/research/w7a_state_structure_aging_market_clocks.py`: population/outcome W4'ten aynen; 9 bağımsız feature (FAM_SIGNAL_AGING_MARKET_CLOCK) — state_age (cadence=1h/cap=168 adım, LEFT_CENSORED/LEFT_CENSORED_AT_CAP/MISSING), swing_age/level_age/push_age (ayrı, known_at_ts nearest-prior), trade_count/volume/realized_vol clock (60-candle pencere, 30-pencere TRAIN-öncesi medyan normalize, 60/60+24/30 completeness gate, ratio>1.0 bucket), liquidation_age same/opposite (200K eşik + event_end_ts_ms canonical-cascade dışlama). Duration feature bucket cutpoint = TRAIN medyanı (TEST+negative-control'a aynen uygulanır, full-population medyan KULLANILMADI — operatör düzeltmesi). Primary=cluster-bootstrap risk-difference CI; secondary=two-sided permütasyon + 9 test üzerinde Holm adjustment. OD-016 eklendi (sinyal-lifecycle+book_update_age NOT_IMPLEMENTED, backlog). **Gerçek bulgu:** analyzed_n=243 (excluded=9, W6RS-confirmation ile aynı neden), independent_cycle_n=159; 2/9 test Holm-öncesi p<0.05 (liq_age_same_direction, realized_vol_clock) ama **Holm-adjusted p'ler anlamlı değil** (0.348/0.2655), bootstrap CI'lar çoğunlukla sıfırı içeriyor — dürüst null/nüanslı sonuç, ekonomik iddia/promotion YAPILMADI. | 20/20 ✓ (test_ami_research_w7a_state_structure_aging_market_clocks.py) + 2/2 (gateway event_end_ts_ms) — **toplam AMI 358/358** | new-file/veri + feature_gateway additive kolon; idempotent (registry=1/results=16, 2× doğrulandı) | DONE |
| BATCH-P6-010b (W7A closure) | 6 | Sonnet 5 | 2026-07-04 | **W7A checkpoint kapanışı (operatör talimatı).** `classify_closure()` eklendi (`ami/research/w7a_state_structure_aging_market_clocks.py`) — 2 nominal p<0.05 sonucu post-hoc eşik/subgroup taramasıyla kurtarılmaya ÇALIŞILMADI; 9 test programatik NULL/UNCONFIRMED_DESCRIPTIVE_LEAD/HOLM_SIGNIFICANT/UNDEFINED'e sınıflandırılıp `closure_classification` olarak canonical SQL'e yazıldı. **Gerçek sonuç:** 7 NULL + 2 UNCONFIRMED_DESCRIPTIVE_LEAD (liq_age_same_direction, realized_vol_clock), 0 HOLM_SIGNIFICANT — kayıtlı ama kovalanmadı. **W7A KAPALI.** | 5/5 ✓ (classify_closure 4 dal + gerçek-veri kilidi) — **toplam AMI 363/363** | new-code + veri; idempotent (results 16→17, 2× doğrulandı) | DONE |
| BATCH-P6-011 (W10a) | 6 | Sonnet 5 | 2026-07-04 | **W8-12 read-only audit + Multi-Timeframe Structural Conflict (W10'un yalnız buildable yarısı) [W10a checkpoint kapatıldı — SYSTEM_STATE §61 ek not].** Audit: W8/W9 = GRAVEYARD_COLLISION+NOT_IMPLEMENTED (P7 yok; #3/#4/#10/#13/#16/#18/#19/#21 ile çakışma; W9'un BAD_TIMING alt-sinyali OD-008 onayı bekliyor). W11 = BLOCKED_BY_DATA+NOT_IMPLEMENTED (ami_events %100 REAL_LIQUIDATION, 0 PROXY_*). W12 = NOT_IMPLEMENTED (W8'e bağımlı, W8 yok). W10 = PARTIALLY_BUILDABLE (multi-TF conflict yarısı BUILDABLE_NOW via mevcut StateEngine; transition yarısı BLOCKED_BY_DATA+NOT_IMPLEMENTED → OD-017). `ami/research/w10a_multi_tf_structural_conflict.py`: population/outcome W4'ten aynen; TF çifti (1h/4h) OD-003'ün cycle_resolver'ından birebir; direction mapping `_structure()`'ın mevcut formülünden birebir alıntı (yeni icat yok); operatörün 7 düzeltmesi uygulandı — 5 ham hücre (UP_UP/DOWN_DOWN/UP_DOWN/DOWN_UP/NEUTRAL) ayrı korunuyor (erken-AGREE-birleştirme yok), TEK primary hipotez (CONFLICT vs AGREEMENT), NEUTRAL asla primary'ye eklenmiyor, known-at güvenliği explicit testle kanıtlandı (mark_prices tick-tabanlı, candle değil), exact maturity cutoff + 70/30 + independent-cycle N + negative control korundu. `w7a_state_structure_aging_market_clocks.py:cluster_bootstrap_risk_difference()`'e `label_high`/`label_low` parametresi eklendi (geriye-uyumlu). OD-017 eklendi. **Gerçek bulgu:** analyzed_n=243, independent_cycle_n=159; DOWN_DOWN baskın (146/243); UP_DOWN(n=3)/DOWN_UP(n=19) insufficient_sample; primary contrast CONFLICT(n=22) vs AGREEMENT(n=164) — risk-diff=−0.031, p=0.8275, CI sıfırı içeriyor — **null sonuç**; train/test'te CONFLICT zaten yetersiz-örneklem (17/5); negative control'de de anlamlı fark yok. | 15/15 ✓ (w10a modülü) + 1/1 (w7a custom-label testi) — **toplam AMI 379/379** | new-file/veri + w7a additive parametre; idempotent (registry=1/results=9, 2× doğrulandı) | DONE |
| BATCH-P6-012 (P7-8 audit) | 7-8 ön-tasarım | Sonnet 5 | 2026-07-04 | **Phase 7-8 Minimum Timing/Lifecycle Infrastructure — READ-ONLY design audit** (kod YAZILMADI). Küçük historical-descriptive dalga aranması operatör talimatıyla DURDURULDU. **Kritik ön-bulgu:** whitepaper Phase 0-9 numaralandırmasındaki ÖNCEKİ çalışma (`ami/lifecycle/engine.py`, `ami/research/forward_pipeline.py`, `ami/governance/governor.py`) istenen altyapının büyük kısmını ZATEN uyguluyor ve CANLI (2 aktif forward binding) — ama `ami_events`/`ami_cycles` (Phase 1-6 SELL-cascade popülasyonu) DEĞİL, `LONG_HOUR17_*`/`LONG_SILENCE` sinyallerine bağlı; ayrıca bu 2 modülün SIFIR dedike unit testi var. 8 bölümlük dependency map + şema + dosya listesi + migration sırası + risk register + batch tahmini + backlog eşlemesi üretildi (SYSTEM_STATE §62). Önerilen sıra: 7A (şema+deterministik backfill) → 7B (timing/path metrics) → 8A (forward_pipeline genişletme + ÖNCE eksik test kapatma) → 8B (validation/dashboard) → ancak sonra W8/W9/W10-transition/W12 yeniden değerlendirilir (mezarlık/OD kontrolü tekrar geçerli). OD-016 bu hat tarafından doğrudan ele alınacak; OD-017 8B sonrası bile BLOCKED_BY_DATA kalır (LONG-anchor popülasyonu yaratmıyor). | test koşulmadı (read-only audit; taban 379/379) | yok (hiçbir kod/veri/bucket/route/observer değişmedi) | DONE |
| BATCH-P7A-0 | 7A-0 | Sonnet 5 | 2026-07-04 | **Existing Lifecycle Infrastructure Characterization and Safety Test Closure** (operatör onayı: "APPROVE PHASE 7A-0 TEST CLOSURE"). NO_PRODUCTION_CODE_CHANGE/NO_SCHEMA_CHANGE/NO_MIGRATION/NO_BACKFILL/NO_NEW_BINDING sınırları uygulandı — yalnız 2 test dosyası eklendi: `test_ami_lifecycle_engine_characterization.py` (11 test: deterministic identity, known-at/no-lookahead, invalid-sequence characterization, ledger-rebuild, out-of-order, import/credential yasağı) + `test_ami_research_forward_pipeline_characterization.py` (17 test: restart idempotency, duplicate suppression, append-only, historical-replay FORWARD_N sabit, activation-boundary characterization, crash-before/after-commit, partial-batch recovery, no-order mutation testleri, 2 gerçek binding'in kimlik alanları mode=ro doğrulaması). **Characterization bulgusu:** activation boundary tam sınırda (`entry_ts_ms==frozen_ms`) kod HISTORICAL sayıyor, operatörün tercih ettiği sözleşme (`>=` → FORWARD) ile UYUŞMUYOR — ama bu DAHA SIKI yön olduğu için çekirdek no-lookahead güvenliği ihlal edilmiyor (characterized, sessizce değiştirilmedi). Partial-batch recovery'nin gerçek granülerliği (per-accepted-trade, `attach_evidence()`'ın kendi commit'i üzerinden) pre-batch varsayımından (per-binding) daha ince çıktı — düzeltildi. **0 safety invariant NOT_MET, 0 blocker.** | 28/28 ✓ (11+17 yeni) — **toplam AMI 407/407** | new-test-file-only; production kod/şema/migration/backfill/binding/observer/permission SIFIR değişti; protected session delta ZERO (hash-doğrulandı) | DONE |
| BATCH-P7A-1 | 7A.1 | Sonnet 5 | 2026-07-04 | **Canonical Lifecycle Schema Foundation + Disposable Historical-Safe Reconstruction** (operatör onayı: "APPROVE PHASE 7A.1 DISPOSABLE IMPLEMENTATION"). DISPOSABLE_DB_ONLY/NO_LIVE_CANONICAL_DB_MIGRATION — gerçek canonical.sqlite'a SIFIR write. 3 yeni production modülü: `ami/lifecycle/canonical_schema.py` (2 tablo — ami_signal_lifecycle/ami_lifecycle_transitions — + identity algoritması + transition validator/rebuild + rollback; `ami/warehouse/schema.py`'ye BİLİNÇLİ OLARAK dokunulmadı — paylaşılan `init_schema()`'ya eklemek gerçek DB'ye yanlışlıkla uygulanma riski taşırdı), `ami/lifecycle/canonical_backfill.py` (deterministik backfill + FIELD_CLASSIFICATION matrisi), `ami/lifecycle/migration_rehearsal.py` (14-adım disposable rehearsal). **Tasarım düzeltmesi:** erken "signal_id=event_id 1:1" basitleştirmesi terk edildi — gerçek route_version verisi bazı anchor'larda HEM LONG_\* HEM SHORT_\* route'ların aynı anda bağlı olduğunu gösterdi (tek-direction identity contract'ıyla çelişirdi); çözüm: signal=(source_event_id,setup_token) çifti, 252 event→**270 signal**. **Gerçek disposable-rehearsal sonucu:** backfill 1./2./3.(reapply) çalıştırma birebir aynı (270 signal/536 transition), row-count+content-hash+current-state-rebuild eşitliği ✅, rollback temiz + mevcut tablolar korundu ✅, old-reader (feature_gateway) etkilenmedi ✅. Field classification: çoğu DETERMINISTIC_HISTORICAL_SAFE; direction=HISTORICAL_PROXY (route-adı önek-ayrıştırması); setup_version/timeframe/first_known_ts/last_confirmation_ts/invalidation_ts=NOT_IMPLEMENTED; first_executable_ts=FORWARD_ONLY (hiçbiri 0'a çevrilmedi). **0 blocker.** | 47/47 ✓ yeni (32+11+4) — **toplam AMI 454/454** | new-file-only (ami/lifecycle/{canonical_schema,canonical_backfill,migration_rehearsal}.py + 3 test dosyası); gerçek canonical.sqlite'a 0 schema/data write (disposable kopya üzerinde doğrulandı); protected session delta ZERO | DONE (disposable) |
| BATCH-P7A-CANON | 7A (canonical) | Sonnet 5 | 2026-07-04 | **Phase 7A canonical migration — GERÇEK `data/ami/canonical.sqlite`'A UYGULANDI** (operatör onayı: "APPROVE PHASE 7A CANONICAL MIGRATION"). Preflight: schema fingerprint gerçek DB↔disposable-referans birebir eşleşti (drift yok), concurrent-writer kontrolü temiz (`BEGIN IMMEDIATE` anında alındı), tam backup alındı (`data/ami/backups/canonical_pre_phase7a_migration_20260704_130548.sqlite`, sha256 doğrulandı, WAL checkpoint ile tek-dosya tutarlı), frozen source snapshot'tan deterministik beklenen sayı hesaplandı (270/536, kör zorlama yok). `ami/warehouse/schema.py`: `CANONICAL_SCHEMA_VERSION` 7→8, yeni `_SCHEMA_PHASE7A` bloğu (Phase 7A.1'de doğrulanan SQL'in birebir kopyası) `init_schema()`'ya eklendi. `ami/lifecycle/{canonical_schema,canonical_backfill,migration_rehearsal}.py` **SIFIR değişti** (hash-doğrulandı — aynı doğrulanmış kod kullanıldı). Backfill gerçek DB'ye uygulandı: **270 signal/536 transition** (disposable referansla birebir). Postflight: identity uniqueness ✅, source_event_id/independent_cycle_id ayrımı ✅ (hiç conflate olmadı), current-state-rebuild 270/270 eşleşti, 2. çalıştırma idempotent (0 yeni duplicate), FORWARD_N değişmedi (research.sqlite:processed_trades hâlâ 0), 2 mevcut binding kimlik alanları birebir aynı, old-reader (`feature_gateway`) etkilenmedi (252/167/173464 sayıları aynı). **Full regression:** 38 `test_ami_*.py` + buyfade/silexit mutation dosyaları migrasyon SONRASI tek tek yeniden çalıştırıldı, hepsi ✓ — 1 test (`test_schema_fingerprint_changes_only_by_addition`) migrasyon artık kalıcı olduğu için stale bir varsayım içeriyordu (regresyon DEĞİL, migration'ın başarısının kanıtı), her iki duruma dayanıklı olacak şekilde düzeltildi. **0 blocker.** Phase 7B'ye OTOMATİK GEÇİLMEDİ (ayrı onay gerekiyor). | 454/454 ✓ (test sayısı sabit, 1 assertion post-migration gerçeğine göre düzeltildi) | `ami/warehouse/schema.py` değişti (v7→v8, additive); gerçek canonical.sqlite'a schema+data write (270 signal/536 transition); backup+hash doğrulandı; protected session delta ZERO | DONE |
| BATCH-P7A-P1-PROV | 7A-P1 | Sonnet 5 | 2026-07-04 | **Disposable Field-Level Provenance Closure** (operatör onayı: "APPROVE PHASE 7A-P1 DISPOSABLE PROVENANCE CLOSURE" — operatörün kendi tespit ettiği PHASE_7A_PROVENANCE_GAP'i kapatma girişimi). DISPOSABLE_DB_ONLY/NO_CANONICAL_DB_WRITE. Reconciliation: mevcut 5 tablo (data_quality_events/causal_assumption_registry/evidence_contamination/mt_family_registry/market_structure_versions) incelendi, hiçbiri per-field provenance karşılamıyor — **existing equivalent: NO**. Yeni: `ami/lifecycle/canonical_field_provenance.py` (`ami_lifecycle_field_provenance` — genel signal_id/field_name/field_classification/is_proxy/derivation_method/source_reference/limitations sözleşmesi, tek-kullanımlık "direction_provenance" DEĞİL; + `ami_lifecycle_direction_view` — direction hiçbir zaman classification'sız sunulmaz) + `ami/lifecycle/provenance_rehearsal.py` (disposable rehearsal). Direction derivation gerçek koddan (`classify_direction_from_setup_id`) çapraz-doğrulandı, tahmin edilmedi. **Disposable sonuç:** 270 signal×16 alan=**4320 provenance satırı**, direction 270/270 HISTORICAL_PROXY/is_proxy=1, 2. çalıştırma idempotent, missing-provenance validator gerçekten reddediyor (sessizce REAL varsaymıyor), 270 signal/536 transition DEĞİŞMEDİ, FORWARD_N=0 sabit, rollback+reapply temiz, old-reader etkilenmedi. Row-level (`is_proxy`) vs field-level provenance ayrımı SCHEMA_DICTIONARY.md'ye açıkça işlendi. **0 blocker.** | 18/18 ✓ yeni (15+3) — **toplam AMI 472/472** | new-file-only; gerçek canonical.sqlite'a 0 write (doğrulandı — provenance-ilişkili hiçbir obje yok, 270/536 sayıları sabit); protected session delta ZERO | DONE (disposable) |
| BATCH-P7A-P-CANON | 7A-P (canonical migration) | Sonnet 5 | 2026-07-04 | **Phase 7A-P Canonical Provenance Migration — GERÇEK `data/ami/canonical.sqlite`'A UYGULANDI** (operatör onayı: "APPROVE PHASE 7A-P CANONICAL PROVENANCE MIGRATION" → SCHEMA_DRIFT_BLOCKER bulundu → "SELECT OPTION 2 — RESTORE CLEAN V8 AND REAPPLY CONTROLLED MIGRATION"). **SCHEMA_DRIFT_BLOCKER:** `ami/warehouse/schema.py` güncellenip (v9 DDL) migrasyon script'inden ÖNCE full-regression çalıştırılınca, `test_ami_research_w6rs/w7a/w10a/w6_*.py`'nin ÖNCEDEN VAR OLAN real-data-smoke testleri (`connect(DEFAULT_PATH)`+bazıları `init_schema(conn)`) yan etki olarak gerçek DB'yi v9 şemaya (additive, veri dokunulmadan) yükseltti — forensic snapshot alındı (`canonical_ACCIDENTAL_V9_SCHEMA_DRIFT_snapshot_20260704_161554.sqlite`), temiz v8 backup'a dokunulmadı. **Test-isolation safety closure (kalıcı):** `ami/warehouse/schema.py`'ye `REAL_CANONICAL_PATH_IMMUTABLE` + `_TEST_ISOLATION_ACTIVE` + `connect()`'e fail-closed guard (writable+gerçek-path+isolation-active → RuntimeError) eklendi; yeni `tests/conftest.py` (session-scoped autouse fixture, DEFAULT_PATH'i disposable kopyaya yönlendirir, teardown'da gerçek dosyanın hash/mtime/fingerprint/version'ının SABİT kaldığını assert eder) + yeni `tests/test_test_isolation_safety.py` (5 test). Production runtime davranışı DEĞİŞMEDİ (guard yalnız `_TEST_ISOLATION_ACTIVE=True` iken devrede, bunu yalnız conftest set eder). Doğrulandı: risky real-data testleri bu isolation altında çalıştırıldı, gerçek DB mtime'ı hiç değişmedi. **Restore:** temiz v8 backup'tan (sha256=`a3731c9897ccf44c6230b3fea6a423cc7e4f06f3f59d74a6bbf6fd63cab04110`) restore edildi, postflight tam doğrulandı (schema=8, 270/536, fingerprint orijinaliyle birebir, FORWARD_N=0, bindings=2 değişmedi). **Controlled migration:** disposable'da 495/495 testten geçen exact kod (migrate_setup_version_nullable→init_schema→backfill_lifecycle→correct_unvalidated_terminal_close→backfill_field_provenance, dry-run'da keşfedilen doğru sırayla) gerçek DB'ye uygulandı. **Dry-run'da ek bug bulunup gerçek DB'ye dokunmadan düzeltildi:** `backfill_lifecycle()`'ın ON CONFLICT UPDATE'i `lifecycle_reason_code`'u her rerun'da SIGNAL_BIRTH'e sıfırlayıp correction'ı siliyordu — bu iki kolon UPDATE'ten çıkarıldı (ledger/correction'ın sahipliğinde kalacak), yeni regression testi eklendi. **Gerçek sonuç:** schema_version=9, 270 signal/802 raw transition (270 SIGNAL_BIRTH+266 TERMINAL_CLOSE+266 CORRECTION)/270 effective transition/0 effective CLOSED interval/270 OPEN current-state, 4320 provenance (0 eksik/duplicate), direction 270/270 HISTORICAL_PROXY, setup_version+terminal_ts 270/270 NULL+NOT_IMPLEMENTED, FORWARD_N=0 ve bindings=2 değişmedi, 2. çalıştırma idempotent (content hash birebir). **Full postflight regresyon (isolated test DB'lerde):** 495/495 ✓, gerçek DB hash/mtime bu regresyon boyunca BİREBİR SABİT kaldı (doğrulandı). **0 blocker (SCHEMA_DRIFT_BLOCKER restore ile kapandı).** **PHASE 7A KAPANDI.** Phase 7B'ye geçilmedi. | 495/495 ✓ (489 önceki + 6 yeni: 5 isolation testi + 1 backfill-rerun-clobber-fix testi); 2 mevcut test (v8→v9 kalıcılığına dayanıklı hâle getirildi, emsal: schema_fingerprint testi) | `ami/warehouse/schema.py` (v9 DDL kalıcı + test-isolation guard) + `ami/lifecycle/canonical_backfill.py` (idempotency fix) + yeni `tests/conftest.py`/`tests/test_test_isolation_safety.py`; **gerçek canonical.sqlite'a schema+data write UYGULANDI** (v8→v9, 270 signal/802 transition/4320 provenance); backup×3 (pre-drift, accidental-drift-snapshot, post-migration) hash-doğrulandı; protected session delta ZERO | DONE (canonical, APPLIED) |
| BATCH-P7A-P1-V9R3 | 7A-P1 (effective-ledger semantics round 3) | Sonnet 5 | 2026-07-04 | **Two-layer ledger contract (raw vs effective) + validate=False fail-closed safety** (operatör read-only-first talimatı: round-2'nin append-only CORRECTION düzeltmesinin kendisi gerçek bir close-reopen hareketi değil, raw-ledger okuyan downstream'in hâlâ sahte CLOSED interval görebileceğini doğrula, disposable-only düzelt). CANONICAL DB'YE DOKUNULMADI, NO PHASE 7B, NO RUNTIME/BINDING/OBSERVER CHANGE. **Bulgu:** öncesinde yalnız "en son satırı al" yöntemi (`rebuild_current_state`) STATUS'u doğru veriyordu ama INTERVAL/DURATION sorguları için hiçbir gate yoktu — raw ledger'ı doğrudan okuyan bir sorgu 266 sinyalin her biri için sahte CLOSED interval görürdü. **Düzeltme:** yeni `ami_lifecycle_effective_transitions` VIEW (`canonical_schema.py`, `LIFECYCLE_SCHEMA_VERSION` 2→3) — superseded satırları VE kendi hedefinin tam tersi (pure reversal: `correction.previous_status==original.new_status AND correction.new_status==original.previous_status`) olan CORRECTION satırlarını dışlıyor (metadata-only correction'lar dışlanmıyor, gerçek transition oldukları için korunuyor). Yeni companion fonksiyonlar: `effective_lifecycle_status()`, `count_effective_closed_signals()`. `insert_transition()`'a fail-closed `validate=False` guard'ları eklendi: yalnız `reason_code=CORRECTION` + var olan bir `correction_of` birlikte izinli, hedef yoksa/aynı hedef ikinci kez düzeltilmeye çalışılırsa raise; mevcut test (`test_correction_uses_higher_transition_version_not_update`) gerçek `correction_of` geçirecek şekilde güncellendi. **Sonuç:** raw ledger=802 (immutable, değişmedi), effective ledger=270 (1/sinyal, yalnız genesis), current-state OPEN=270/CLOSED=0 (raw+effective birebir uyumlu), duration-contamination kanıt testi (naif raw-ledger sorgusu sahte interval üretir, effective view üretmez) eklendi. Canonical query contract docstring'e işlendi: OPEN="terminal doğrulanmadı", ASLA "live/active" değil; UNKNOWN/CENSORED/UNRESOLVED icat edilmedi, mevcut `terminal_ts=NOT_IMPLEMENTED` field-provenance kaydı gate olarak kullanıldı. **0 blocker (bulunan blocker bu batch'te disposable-only çözüldü).** | 11 yeni test + 1 mevcut test güncellemesi (`test_correction_uses_higher_transition_version_not_update`) — **toplam AMI 489/489 ✓** | production-kod düzeltmesi (`canonical_schema.py`: yeni VIEW+2 fonksiyon+validate=False guard'ları; `provenance_rehearsal.py`: yeni raporlama fonksiyonları) + 1 test dosyası güncellemesi; gerçek canonical.sqlite'a 0 write (schema_versions=8 sabit, 270/536 sabit, effective view/field_provenance tablosu yok — read-only doğrulandı); protected session delta ZERO | DONE (disposable) |
| BATCH-P7A-P1-V9R2 | 7A-P1 (semantic consistency round 2) | Sonnet 5 | 2026-07-04 | **TERMINAL_CLOSE / LIFECYCLE_TERMINAL_SEMANTIC_BLOCKER correction** (operatör read-only-first talimatı: terminal_ts=NULL/NOT_IMPLEMENTED ile ledger'ın hâlâ event_end_ts_ms'ten CLOSED/TERMINAL_CLOSE üretmesi arasındaki çelişkiyi doğrula, disposable-only düzelt). DISPOSABLE_DB_ONLY sürdürülüyor, CANONICAL DB'YE DOKUNULMADI, NO PHASE 7B, NO RUNTIME/BINDING/OBSERVER CHANGE. **Bulgu:** LIFECYCLE_TERMINAL_SEMANTIC_BLOCKER — pre-batch disposable kopya (=gerçek M-0023 durumunun birebir kopyası) CLOSED=266/OPEN=4, ledger+denormalized kolon TUTARLI ama YANLIŞ (consistent-but-wrong: event_end_ts_ms'ten türetilmiş, hiç doğrulanmamış bir terminal iddiası). Taxonomy reconciliation: mevcut `LifecycleReasonCode`/`TradeLifecycleState`'te "non-terminal/source-event-ended" karşılığı YOK — yeni isim icat edilmedi; en dürüst çözüm: `backfill_lifecycle()` artık HİÇBİR lifecycle_status/transition iddiasını event_end_ts_ms'ten türetmiyor (yalnız OPEN/SIGNAL_BIRTH). Yeni `correct_unvalidated_terminal_close()`: gerçek DB'nin (disposable-kopyada tekrarlanan) 266 eski TERMINAL_CLOSE satırını append-only bir CORRECTION transition'ıyla (`CLOSED→OPEN`, transition_version=2, correction_of=<orijinal id>, validate=False) tersine çeviriyor — orijinal satır SİLİNMEDİ/DEĞİŞTİRİLMEDİ (536 satırın tamamı hâlâ mevcut), denormalize kolon ledger'la tutarlı güncellendi. **Sonuç:** OPEN=270/CLOSED=0, transition toplamı 536→802 (270 SIGNAL_BIRTH+266 TERMINAL_CLOSE[korunan tarihsel]+266 CORRECTION[yeni]), 270 signal identity/source_event_id/independent_cycle_id DEĞİŞMEDİ, event_end_ts_ms ami_events'te kayıp değil. Ek: `UNKNOWN_SETUP_VERSION_TOKEN` identity-contract sabiti eklendi (=SETUP_VERSION_DEFAULT, signal_id değişmedi) + supersession contract (gerçek setup_version bulunursa sessiz mutasyon yasak, yeni IDENTITY_VERSION veya supersession-kaydı kararı geleceğe bırakıldı). `ami/lifecycle/migration_rehearsal.py`'nin ESKİ Phase-7A-canonical rehearsal'ı da bu düzeltmeyi çağıracak şekilde güncellendi (reapply-sonrası transition sayısının pre-rollback'ten doğal olarak farklı olması — 270 vs 802 — regresyon değil, açıkça raporlandı). **0 blocker (bulunan blocker bu batch'te disposable-only çözüldü).** | 6 yeni test (`test_fresh_backfill_never_writes_terminal_close_even_with_event_end_ts`, `test_correct_unvalidated_terminal_close_reverses_pre_existing_closed_status`, `test_correct_unvalidated_terminal_close_is_idempotent`, `test_correct_unvalidated_terminal_close_skips_signals_already_open`, `test_unknown_setup_version_token_equals_frozen_default_and_is_hash_stable`, `test_derive_signals_setup_version_column_none_but_identity_hash_uses_token`) + 2 mevcut test düzeltildi (backfill transition-count assertion, migration_rehearsal reapply-asymmetry) — **toplam AMI 478/478 ✓** | production-kod düzeltmesi (`canonical_backfill.py`/`canonical_schema.py`/`canonical_field_provenance.py`/`provenance_rehearsal.py`/`migration_rehearsal.py`) + 3 test dosyası güncellemesi; gerçek canonical.sqlite'a 0 lifecycle/provenance write (schema_versions=8 sabit, 270/536/CLOSED=266/OPEN=4 sabit, field_provenance tablosu yok — read-only doğrulandı); dosya sha256/mtime'ı DEĞİŞTİ ama nedeni bu batch'in kodu değil (researcher_exposure_ledger'a önceden var olan Phase 6 research testlerinin sanksiyonlu audit-yazması); protected session delta ZERO | DONE (disposable) |
| BATCH-P7A-P1-V9 | 7A-P1 (scope+version correction) | Sonnet 5 | 2026-07-04 | **Scope and Version Correction (v9, 16-field) + Semantic Closure** (operatör onayı: "APPROVE PHASE 7A-P1 SCOPE AND VERSION CORRECTION (v9, 16-FIELD) — WITH SEMANTIC CLOSURE"). DISPOSABLE_DB_ONLY sürdürülüyor. **Version contract düzeltmesi:** "8→8" hatalıydı — `ami/lifecycle/canonical_schema.py:LIFECYCLE_SCHEMA_VERSION` 1→2 (proposed canonical v9), `ami/warehouse/schema.py:CANONICAL_SCHEMA_VERSION` HÂLÂ 8 (gerçek DB'ye henüz uygulanmadı). **İki semantik düzeltme (disposable):** (1) `setup_version` canonical değeri NULL (`SETUP_VERSION_DEFAULT` artık yalnız identity-hash girdisi, `identity_version` ile karıştırılmıyor; `setup_version TEXT NOT NULL`→nullable table-rebuild, yeni `migrate_setup_version_nullable()`, idempotent); (2) `terminal_ts` canonical değeri NULL (`event_end_ts_ms` lifecycle TERMINAL zamanı olarak DOĞRULANMADI; yeni internal-only `_terminal_transition_ts` 536 transition-ledger kararını AYNEN koruyor). **Regression bulundu+düzeltildi:** `canonical_backfill.py`'nin global `setup_version=None` değişikliği, ESKİ `migration_rehearsal.py:run_disposable_rehearsal()`'ı kırdı (disposable kopya gerçek DB'nin v8 NOT NULL kısıtını miras alıyor) — `migrate_setup_version_nullable()` çağrısı 2 noktaya (ilk migration + rollback-sonrası-reapply) + test dosyasının doğrudan çağrısına eklendi. **Actual-value null matrisi:** setup_version/terminal_ts/timeframe/first_known_ts/first_executable_ts/last_confirmation_ts/invalidation_ts = 270/270 NULL; diğer 9 alan = 270/270 non-NULL. identity/transitions/lifecycle-counts (270/536) DEĞİŞMEDİ, direction 270/270 HISTORICAL_PROXY sabit, FORWARD_N=0 sabit. **0 blocker.** | 0 yeni test dosyası (mevcut 4 test kırmızıydı → düzeltme sonrası 15/15 ✓) — **toplam AMI 472/472 sabit (full regresyon tekrar çalıştırıldı, hepsi ✓)** | production-kod düzeltmesi (`canonical_schema.py`/`canonical_backfill.py`/`canonical_field_provenance.py`/`provenance_rehearsal.py`/`migration_rehearsal.py`) + 1 test dosyası düzeltmesi; gerçek canonical.sqlite'a 0 write (sha256/mtime değişmedi); protected session delta ZERO | DONE (disposable) |
| BATCH-P7B-0 | 7B-0 (disposable foundation) | Sonnet 5 | 2026-07-04 | **Disposable Path/MFE/MAE Engine Foundation** (operatör onayı: "APPROVE PHASE 7B-0 DISPOSABLE PATH ENGINE FOUNDATION — WITH SEMANTIC LOCKS", §62'nin 7A→7B→8A→8B tasarım denetimini takiben). DISPOSABLE_DB_ONLY/NO_CANONICAL_WRITE. 10 operatör semantic-lock uygulandı: candle-boundary (straddling candle hariç, reference=son kapanmış close, path=birth'ten sonraki ilk candle), MFE≥0/MAE≤0 formülleri, 1m-proxy timing (first-occurrence candle ts), `horizon_outcome_class` adı (path_shape_label DEĞİL, W4'ün `classify_path()`'i reuse), ayrı `endpoint/mfe/mae_vol_units` alanları, 30-alan şema, 6 durumlu status taxonomy, signal-level satır+independent_cycle_id denominatör disiplini, W4'ün 4 sabit horizonu (scalp_30m/1h, swing_4h/24h) DIŞINDA yeni horizon icat edilmedi. Yeni: `ami/lifecycle/{path_schema,path_metrics,path_migration_rehearsal}.py`. **Disposable sonuç:** 270 signal×4 horizon=1080 satır, OK=912/MISSING_INTERNAL_GAP=153/INVALID_VOLATILITY_BASELINE=2/EXCLUDED_NO_HORIZON_DATA=13/MISSING_REFERENCE_PRICE=0/NOT_COMPUTABLE_DIRECTION=0. **0 blocker.** | 49 yeni test (19+25+5) — **toplam AMI 544/544 ✓** | new-file-only; gerçek canonical.sqlite'a 0 write (schema_version=9 sabit, path_observations tablosu yok); protected session delta ZERO | DONE (disposable) |
| BATCH-P7B-0.1 | 7B-0.1 (semantic closure) | Sonnet 5 | 2026-07-04 | **Disposable Semantic Closure** (operatör onayı: "APPROVE PHASE 7B-0.1 DISPOSABLE SEMANTIC CLOSURE"). DISPOSABLE_DB_ONLY sürdürülüyor. 6 düzeltme: (1) `observation_status` (path-computability, 5 değer) ile yeni `volatility_status` (OK/INVALID_VOLATILITY_BASELINE/NOT_APPLICABLE) AYRILDI — DB-level CHECK ile eşleştirildi (NOT_APPLICABLE ⟺ observation_status≠OK); (2) sıfır-ekstremum timing donduruldu — t=0 referans noktası path'in parçası, gerçek path hiç favorable/adverse'a ulaşmazsa time_to_mfe/mae_ms=0 (ilk candle ts'i sessizce KULLANILMIYOR); (3) yeni `ami/lifecycle/path_field_provenance.py` — 23 `path_observations.*` alan, mevcut `ami_lifecycle_field_provenance` tablosuna yazıyor (paralel tablo YOK), proxy→safe downgrade guard'ı kodlu; (4) tam şema manifest (31 kolon, 12 CHECK, doğrulandı — önceki 7B-0 30 kolon iddiası PRAGMA table_info ile teyit edildi, hata değildi); (5) `*_vol_units`→`*_anchor_vol_units` yeniden adlandırıldı (Seçenek A, ufuklar-arası sigma-karşılaştırılabilir OLMADIĞINI isim içinde netleştirmek için) + `PATH_SCHEMA_VERSION` 1→2/`PATH_DEFINITION_VERSION` path-v1→path-v2; (6) tüm disposable doğrulama tekrar çalıştırıldı. **Disposable sonuç:** observation_status OK=914/MISSING_INTERNAL_GAP=153/EXCLUDED_NO_HORIZON_DATA=13; volatility_status OK=912/NOT_APPLICABLE=166/INVALID_VOLATILITY_BASELINE=2. **0 blocker.** | 80 yeni test (27+33+13+7, önceki 49'un yerine) — **toplam AMI 575/575 ✓** | `path_schema.py`/`path_metrics.py` yeniden yazıldı + yeni `path_field_provenance.py`; gerçek canonical.sqlite'a 0 write (schema_version=9 sabit, path_observations/path_observations.* provenance yok); protected session delta ZERO | DONE (disposable) |
| BATCH-P7B-CANON | 7B (canonical migration) | Sonnet 5 | 2026-07-04 | **Phase 7B canonical migration — GERÇEK `data/ami/canonical.sqlite`'A UYGULANDI** (operatör onayı: "APPROVE PHASE 7B CANONICAL PATH METRICS MIGRATION"). Preflight: v9 checkpoint (270/802/270/4320/FORWARD_N=0/binding=2) birebir doğrulandı, aktif yazıcı yok, backup+hash+fingerprint doğrulandı. `ami/warehouse/schema.py`: `CANONICAL_SCHEMA_VERSION` 9→10, yeni `_SCHEMA_PHASE7B` (disposable-doğrulanmış DDL'yle byte-for-byte aynı, fingerprint `3a26ffa8...` fresh in-memory DB'de doğrulandı). Yeni `ami/lifecycle/path_canonical_migration.py:run_canonical_migration()` (schema-sonrası backfill entry point). `ami/research/feature_gateway.py`: 3 tablo allowlist'e + 3 dedike fetch fonksiyonu (`fetch_lifecycle_signals`/`fetch_lifecycle_effective_transitions`/`fetch_path_observations`) — ikincisi raw `ami_lifecycle_transitions` DEĞİL effective view'ı sorgular (raw TERMINAL_CLOSE asla terminal-evidence olarak sunulmaz). **Backfill:** 1080 path satırı + 6210 yeni field-provenance satırı, operatörün beklediği dağılımla BİREBİR eşleşti (OK=914/MISSING_INTERNAL_GAP=153/EXCLUDED_NO_HORIZON_DATA=13; vol OK=912/NOT_APPLICABLE=166/INVALID=2). Tüm zorunlu semantik assertion (reference_price_ts≤birth, known_at≥horizon_end, MFE≥0/MAE≤0, sıfır-ekstremum→0, same-candle→SAME_CANDLE_UNKNOWN, mfe=PROXY/realized_vol=SAFE, effective-CLOSED=0/270 OPEN≠aktif) SQL ile doğrulandı, 0 ihlal. **Yan bulgu+düzeltme:** `provenance_rehearsal.py`'nin scope'suz `COUNT(*)`/hash sorgusu, tablo artık 2. yazıcı (path_field_provenance) tarafından paylaşılınca kırıldı — `provenance_version` filtresi eklenerek gerçek bug düzeltildi (path-provenance'ın meşru paylaşımının ortaya çıkardığı); 1 test assertion (`schema_version_before in (8,9)`→`(8,9,10)`) genişletildi. **Postflight:** schema=10/270/802/270/effective-CLOSED=0/FORWARD_N=0/binding=2 sabit, 2. çalıştırma idempotent (content hash+row count birebir), full regresyon (588/588) boyunca gerçek DB hash/mtime BİREBİR SABİT. **0 blocker. PHASE 7B CANONICAL FOUNDATION KAPANDI.** Phase 8'e geçilmedi. | 588/588 ✓ (495 önceki AMI + 13 yeni gateway testi + 80 path-modülü testi) | `ami/warehouse/schema.py` (v10 DDL) + `ami/research/feature_gateway.py` (3 fetch fonksiyonu) + `ami/lifecycle/provenance_rehearsal.py` (scope fix) + yeni `ami/lifecycle/path_canonical_migration.py`; **gerçek canonical.sqlite'a schema+data write UYGULANDI** (v9→v10, 1080 path satırı, 6210 provenance satırı); backup×2 hash-doğrulandı; protected session delta ZERO | DONE (canonical, APPLIED) |
| BATCH-P7B-1 (W8-HOLD-BASELINE) | 7B-1 (timing/path research) | Sonnet 5 | 2026-07-04 | **W8-HOLD-BASELINE — fixed-horizon MFE/MAE benchmark + chronological stability (operatör onayı: "APPROVE W8-HOLD-BASELINE-001 — WITH FINAL SEMANTIC LOCKS").** NOT_A_MANAGEMENT_WAVE — hiçbir stop/exit/partial-exit/time-stop/re-entry/cancellation kuralı test edilmedi; yalnız W8'in kendi zorunlu ön-koşulu (hold-baseline benchmark) çalıştırıldı. Yeni `ami/research/w8_hold_baseline.py`: popülasyon `ami_lifecycle_path_observations` WHERE observation_status=OK, feature_gateway (`fetch_lifecycle_signals`+`fetch_path_observations`) ÜZERİNDEN; 16 hücre (2 metrik [mfe_bps/mae_bps] × 4 ufuk × 2 yön), chronological 70/30 split (signal_birth_ts), cluster block-bootstrap (independent_cycle_id) + iki-yönlü permütasyon + Holm step-down (n=16); MIN_BUCKET_N=20/split, birleştirme YOK. **Eşleştirilmiş negatif kontrol** (`ami_candidate_universe`, is_event_aligned=0): ay/session/vol-bucket stratified sampling (frozen ÖNCE outcome okunmadan) — 0 shortfall (266/266); LONG/SHORT direction-ratio eşleştirilemedi, `BLOCKED_FOR_DIRECTION_MATCHING` olarak dürüstçe işaretlendi (fabrikasyon/LONG-varsayımı/pooling YAPILMADI); ayrı, açıkça etiketli `upside_excursion_bps`/`downside_excursion_bps` bağlamsal istatistik raporlandı (asla `mfe_bps`/`mae_bps` olarak sunulmadı). **Performans bug bulunup düzeltildi:** `ami/lifecycle/path_metrics.py:_CandleOHLCIndex.path_window()`'ın `self._rows[lo:]` sınırsız slice'ı O(n) idi (173,464 satırlık candle index'te, erken-tarihli candidate-universe slot'ları için ~10+ dakikaya kadar yavaşlıyordu) — `bisect.bisect_right` ile sınırlı `self._rows[lo:hi]`'a düzeltildi, DAVRANIŞ AYNI (60/60 path_metrics+path_field_provenance+path_migration_rehearsal testi değişmeden geçti, byte-identical sonuç kanıtlandı), yalnız performans iyileşti (real-data rehearsal ~40s→~22s). **Gerçek sonuç:** raw_signal_n_population=266 (4 sinyal hiçbir ufukta OK almadı), distinct_source_event_n=248, distinct_independent_cycle_n=167. **LONG: 8/8 hücre `ANSWERED_SUPPORTED_STABLE_BASELINE`** (n=123-216, cycle_n=86-142). **SHORT: 8/8 hücre `INSUFFICIENT_SAMPLE`** (n=32-50 → 70/30 split sonrası TEST<20, dürüstçe raporlandı, birleştirilmedi — operatörün "SHORT swing_24h beklenen insufficient" öngörüsünden daha geniş çıktı, ama aynı frozen MIN_BUCKET_N kuralının dürüst sonucu). Mezarlık farkındalığı: #3/#4/#10/#13/#18 (W8) hiçbiri test edilmedi (yalnız ölçüm/karakterizasyon); BUY_FADE_SHORT_H45_SL75 (19/270) özel işlem görmedi. **0 blocker.** | 20 yeni test — **toplam AMI 608/608 ✓** (588 önceki + 20 yeni) | `ami/lifecycle/path_metrics.py` (yalnız perf-fix, davranış aynı) + yeni `ami/research/w8_hold_baseline.py`; gerçek canonical.sqlite'a YALNIZ `experiment_registry`(1 satır)+`experiment_results`(20 satır) write edildi — `ami_signal_lifecycle`/`ami_lifecycle_transitions`/`ami_lifecycle_path_observations`/`ami_lifecycle_field_provenance` sayıları (270/802/1080/10530) ve schema_version(10) DEĞİŞMEDİ (doğrulandı); full regresyon (608/608) boyunca gerçek DB hash/mtime BİREBİR SABİT; protected session delta ZERO | DONE |
| BATCH-P7B-1 (W8-VOL-NORMALIZED-BASELINE) | 7B-1 (timing/path research) | Sonnet 5 | 2026-07-04 | **W8-VOL-NORMALIZED-BASELINE — volatility-normalized fixed-horizon MFE/MAE stability (operatör onayı: "APPROVE W8-VOL-NORMALIZED-BASELINE-001").** Next Research Frontier Reconciliation'ın Candidate E'si; W1-W12 dalga durumu + failure_archive (21 kayıt, tam liste) + `IMPLEMENTATION_DEPENDENCY_GRAPH.md`/`CONTRADICTION_REGISTER.md`/`QUESTION_FAMILY_TO_ENGINE_MAP.md`/`AMI_S34_RESEARCH_BACKLOG.md` read-only reconciliation edildi. **Bulgular (yalnız raporlandı, canonicalize EDİLMEDİ):** (1) gerçek shadow ledger canonical ingestiondan beri 10 anchor büyümüş (262 vs 252, tamamı SHORT-yön: +9 BUY_FADE_SHORT_H45_SL75, +1 SHORT_NOISY_BTC1M_D5_H180) — "daha fazla tarihsel veri" zaten biriktiği ama tek başına yetersiz kaldığı gösterildi (60→hâlâ<67 eşik); (2) `tools/research_s34_short_conviction.py`/`research_s34_short_score3_gauntlet.py` (BTC-liq-anchored SHORT_NOISY, N=7-71, hiçbir zaman `knowledge.sqlite`'a KO olarak girmemiş, hiçbir zaman mezarlıklanmamış) — SHORT örneklem sorununu yapısal olarak çözebilecek aday yeni event_family, ama tam bir Phase-3-tarzı identity-canonicalization dalgası gerektiriyor; (3) `StructurePhase.RECLAIM` zaten var olan bir state-engine primitive'i — W10-transition'ı (OD-017) çözebilecek en doğrudan yol, henüz inşa edilmedi. Yeni `ami/research/w8_vol_normalized_baseline.py`: `ami/research/w8_hold_baseline.py`'nin generic istatistik makinesini (`_cell_rows`/`compute_cell`/`classify_cell_verdict`/cluster-bootstrap/permütasyon/Holm) BİREBİR import ederek reuse etti — yalnız METRICS=(`mfe_anchor_vol_units`,`mae_anchor_vol_units`) ve popülasyon filtresine `volatility_status='OK'` eklendi (914→912-ish). **Gerçek sonuç:** raw_signal_n_population=265, LONG 8/8 hücre yine `ANSWERED_SUPPORTED_STABLE_BASELINE` (hiçbiri regime-dependent'e DÜŞMEDİ), SHORT 8/8 hücre yine `INSUFFICIENT_SAMPLE` (normalizasyon örneklem boyutunu ONARMADI — beklenen ve dürüstçe teyit edilen sonuç, null/failure olarak yeniden-yorumlanmadı). Raw-bps karşılaştırması: `any_long_cell_stable_to_regime_dependent=[]`, train-test/IQR oranları LONG'da genelde küçük ve karışık yönde (bazı hücrelerde azaldı bazılarında arttı, ama hepsi "stable" sınıflamasını koruyacak kadar küçük). **0 blocker.** | 13 yeni test — **toplam AMI 621/621 ✓** (608 önceki + 13 yeni) | yeni `ami/research/w8_vol_normalized_baseline.py`; gerçek canonical.sqlite'a YALNIZ `experiment_registry`(1 satır)+`experiment_results`(20 satır) write edildi — `ami_signal_lifecycle`/`ami_lifecycle_transitions`/`ami_lifecycle_path_observations`/`ami_lifecycle_field_provenance` sayıları (270/802/1080/10530) ve schema_version(10) DEĞİŞMEDİ; hiçbir yeni sinyal/event canonicalize/backfill EDİLMEDİ; full regresyon (621/621) boyunca gerçek DB hash/mtime BİREBİR SABİT; protected session delta ZERO | DONE |
| BATCH-SHORT-NOISY-V1-REHEARSAL | 7B-1→pre-canonicalization (SHORT_NOISY identity) | Sonnet 5 | 2026-07-04 | **SHORT_NOISY_BTC200K_CONFIRMED_V1 — DISPOSABLE canonicalization rehearsal (operatör onayı: "APPROVE SHORT_NOISY KNOWN-AT V1 DISPOSABLE CANONICALIZATION REHEARSAL"), önceki read-only "SHORT_NOISY KNOWN-AT OVERLAP RECONCILIATION" bulgusunun devamı.** DISPOSABLE_DB_ONLY/NO_REAL_CANONICAL_WRITE — alfa iddiası YOK. **Frozen tanım (read-only reconciliation'dan değişmedi):** kaynak event=mevcut canonical `ami_events` (252 ETH SELL≥200K anchor, doğrudan); noisy follow-on=ilk ETH SELL≥50K, (anchor+1m, anchor+30m]; BTC confirmation=ilk BTC SELL≥200K, (noisy_ts+5m, noisy_ts+30m]; signal_birth_ts=conf_ts (ASLA noisy_ts); direction=SHORT (sabit, conviction/hour/funding/threshold-sweep YOK). Yeni setup_id=`SHORT_NOISY_BTC200K_CONFIRMED_V1` (eski `SHORT_NOISY_BTC1M_D5_H180`'dan FARKLI — o rota noisy_ts'i entry olarak kullanıyordu, lookahead; `research_s34_short_conviction.py`'nin kendi S5 karşılaştırması bunu kanıtlıyor), route_version=setup_id. Yeni `ami/lifecycle/short_noisy_v1_rehearsal.py` (identity+backfill+3-alan düzeltilmiş field-provenance override+rollback — `generate_signal_id`/`insert_transition` mevcut sözleşmeden BİREBİR reuse; `derive_signals()` reuse EDİLMEDİ çünkü o `anchor_ts_ms`'i signal_birth_ts sayıyor, bu setup için YANLIŞ) + `ami/lifecycle/short_noisy_v1_migration_rehearsal.py` (disposable-copy harness, path_migration_rehearsal.py deseniyle). **Field-provenance dürüstlüğü:** 16 alanın 13'ü mevcut `FIELD_PROVENANCE_SPECS`'ten AYNEN reuse edildi (gerçek türetimleri bu setup için de aynı); 3 alan (`signal_birth_ts`/`setup_id`/`route_version`) DÜZELTİLDİ çünkü gerçek türetimleri farklı (conf_ts liquidation-scan, frozen literal, setup_id'ye eşit) — `field_classification` HİÇBİRİNDE değişmedi (hepsi hâlâ DETERMINISTIC_HISTORICAL_SAFE, proxy↔safe geçişi YOK). Path-observation provenance (23 alan) HİÇBİR değişiklik olmadan reuse edildi (path metrikleri signal_birth_ts'in nasıl türetildiğinden bağımsız, genel). **Disposable sonuç:** candidate_n=54 (=source_event_n, 1:1), 40 distinct cycle, 19 zaten-SHORT-temsilli/21 yeni-SHORT-temsilli (cycle-seviyesinde, event-seviyesinde DEĞİL — bir cycle'ın FARKLI bir üye event'i üzerinden zaten SHORT sinyali olabilir), route_overlap={LONG_SILENCE:41, SHORT_NEITHER:20, BUY_FADE_SHORT_H45_SL75:2}. Zorunlu kontroller BİREBİR geçti: identity 2. çağrıda deterministic, tüm conf_ts>noisy_ts+5m VE tüm noisy_ts>anchor+1m (no-lookahead), tüm signal_birth_ts==conf_ts, duplicate conf_ts (2 çift) farklı event'leri BİRLEŞTİRMEDİ (54 farklı signal_id), 4-katman idempotency (signal/transition/lifecycle-provenance/path-provenance/path-metrics hepsi 2. çalıştırmada değişmedi), old-reader-compat (270 mevcut sinyal/1080 path-obs/10530 provenance sayıları BİREBİR sabit), rollback (yalnız 54 yeni signal/54 transition/216 path-obs/864+1242 provenance silindi, mevcut 270'lik popülasyon DOKUNULMADI), reapply byte-identical content-hash, şema fingerprint DEĞİŞMEDİ (bu batch'te HİÇBİR DDL yok). Path-metrikleri (mevcut `freeze_and_record()` DEĞİŞTİRİLMEDEN tekrar çağrıldı, 324 sinyal=270+54 üzerinde): +216 yeni path-obs satırı (54×4), observation_status OK 914→1096 (+182)/MISSING_INTERNAL_GAP 153→187(+34)/EXCLUDED_NO_HORIZON_DATA 13→13(+0); volatility_status OK 912→1094(+182)/NOT_APPLICABLE 166→200(+34)/INVALID 2→2(+0). **Karar kuralı uygulandı:** 21 yeni-SHORT-cycle ≥20 eşiği geçti ama 16-hücre train/test split için yetersiz (aylık dağılım 2026-02:5/03:8/04:6/06:33/07:2 — Haziran'a aşırı yığılmış) → **verdict=CANONICAL_MIGRATION_READY_LIMITED_DESCRIPTIVE_POPULATION** (full-popülasyon tanımlayıcı analiz destekleniyor, chronological TRAIN/TEST çıkarımsal test desteklenmiyor). **Bu batch GERÇEK canonical.sqlite'a HİÇBİR YAZMA yapmadı** (yalnız disposable kopya) — canonicalization ayrı bir operatör onayı gerektirir. **0 blocker.** | 9 yeni test — **toplam AMI 630/630 ✓** (621 önceki + 9 yeni; regresyon: test_ami_lifecycle_path_migration_rehearsal.py 7/7 yan yana çalıştırıldı, 0 regresyon) | new-file-only (`ami/lifecycle/short_noisy_v1_rehearsal.py` + `ami/lifecycle/short_noisy_v1_migration_rehearsal.py` + 1 test dosyası); gerçek canonical.sqlite'a 0 write (hash/mtime testte doğrulandı BİREBİR SABİT, schema fingerprint DEĞİŞMEDİ); data/microstructure.db yalnız mode=ro açıldı (650GB+ tam-dosya hash ASLA alınmadı, mtime+64MB prefix-hash sabit doğrulandı); protected session delta ZERO | DONE (disposable) |
| BATCH-SHORT-NOISY-V1-CANON-BACKFILL | 7B-1→canonical (SHORT_NOISY identity) | Sonnet 5 | 2026-07-04 | **SHORT_NOISY_BTC200K_CONFIRMED_V1 — CONTROLLED CANONICAL DATA BACKFILL, GERÇEK `data/ami/canonical.sqlite`'A UYGULANDI** (operatör onayı: "APPROVE SHORT_NOISY_BTC200K_CONFIRMED_V1 CONTROLLED CANONICAL DATA BACKFILL", önceki disposable rehearsal'ın kabulünün devamı). Identity/path/provenance backfill ONLY — alfa promosyonu/outcome test/ekonomik iddia/observer aktivasyonu YOK. **Preflight:** schema_version=10/events=252/signals=270(LONG=220/SHORT=50)/raw_transitions=802/effective_transitions=270/path=1080/provenance=10530/FORWARD_N=0(processed_trades=0,FORWARD_OBSERVATION sinyal=0)/bindings=2 — operatörün beklediği checkpoint'le BİREBİR eşleşti, 0 preflight blocker. Aktif yazıcı yok (`BEGIN IMMEDIATE` başarılı), test-isolation `_TEST_ISOLATION_ACTIVE=False` (normal). **Backup:** `data/ami/backups/canonical_pre_short_noisy_v1_backfill_20260704_203444.sqlite`, sha256=`bdb8cf88...` (kaynakla birebir doğrulandı). **Uygulanan yazmalar (tam operatör-onaylı liste, fazlası yok):** `backfill_short_noisy_v1()`→54 sinyal+54 SIGNAL_BIRTH transition; `backfill_short_noisy_v1_field_provenance()`→864 lifecycle-provenance satırı; `backfill_path_field_provenance()`→1242 path-provenance satırı; `freeze_and_record()` (path_metrics, mevcut halinde DEĞİŞTİRİLMEDEN, artık 324 sinyal üzerinde)→216 yeni path-obs satırı. Hiçbir yeni `ami_events` oluşturulmadı, hiçbir mevcut sinyal kimliği değiştirilmedi. **Postflight (operatörün beklediği TAM sayılarla BİREBİR eşleşti):** events=252(değişmedi)/signals=324/LONG=220(değişmedi)/SHORT=104/raw_transitions=856/effective_transitions=324/current_state=324 OPEN/effective_CLOSED=0/path=1296/provenance=12636/independent-cycle evreni=167(değişmedi)/schema_version=10(değişmedi)/schema fingerprint DEĞİŞMEDİ(0 DDL bu batch'te)/bindings=2(değişmedi). observation_status: OK=1096/MISSING_INTERNAL_GAP=187/EXCLUDED_NO_HORIZON_DATA=13/MISSING_REFERENCE_PRICE=0/NOT_COMPUTABLE_DIRECTION=0 — beklenen dağılımla BİREBİR. volatility_status: OK=1094/NOT_APPLICABLE=200/INVALID=2 — beklenenle BİREBİR. **Mevcut 270 sinyal/1080 path-obs/10530 provenance satırının içeriği BYTE-IDENTICAL kaldı** (content-hash pre/post karşılaştırması, hem tüm-tablo hem SETUP_ID-hariç-scope'lu). **Zorunlu assertion'lar** (gerçek DB'ye karşı SQL ile): tüm signal_birth_ts==conf_ts, tüm confirmation frozen noisy-window'dan strictly sonra, hiçbir identity alanı post-birth bilgiye bağlı değil, 54 signal_id hepsi distinct, duplicate conf_ts (2 çift) farklı event'leri birleştirmedi, direction hâlâ HISTORICAL_PROXY (proxy→safe upgrade YOK), terminal_ts/first_executable_ts=NULL (hepsi), OPEN="terminal doğrulanmadı" (asla "aktif/live" değil) — hepsi PASSED. **İdempotency:** aynı backfill gerçek DB'ye 2. kez uygulandı — signal/path/provenance sayıları ve content-hash BİREBİR aynı kaldı (deterministic no-op). **Rollback rehearsal:** gerçek DB'nin (backfill-sonrası) disposable bir kopyasında `rollback_short_noisy_v1()` çalıştırıldı — yalnız 54 sinyal/54 transition/216 path-obs/864+1242 provenance silindi, sonuç tam olarak orijinal 270/802/1080/10530'a döndü (gerçek DB'ye rollback UYGULANMADI, yalnız mekanizma doğrulandı). **Full regresyon:** 48 AMI dosyası + 2 buyfade-mutation dosyası (630 test) ikişerli/bazı tekli pytest çağrılarıyla çalıştırıldı — 8 önceden-yeşil dosyada (`test_ami_lifecycle_migration_rehearsal`/`path_field_provenance`/`path_metrics`/`path_migration_rehearsal`/`provenance_rehearsal`/`research_feature_gateway`/`research_w8_vol_normalized_baseline`) eski hardcoded 270/802/1080/4320/266 checkpoint'leri yeni 324/856/1296/5184/324'e güncellendi (test LOGIC'i zayıflatılmadı, yalnız stale sayılar düzeltildi — emsal: P7A-P-CANON/P7B-CANON'un aynı disiplini); **ÖNEMLİ BULGU (yorumlanmadı, ayrı bir onaya bırakıldı):** SHORT popülasyonu büyüyünce (265→316) W8-VOL-NORMALIZED-BASELINE'ın 6/8 SHORT hücresi (5 REGIME_DEPENDENT+1 STABLE) INSUFFICIENT_SAMPLE eşiğini ilk kez aştı — bu batch'te NO_OUTCOME_ANALYSIS/NO_TRAIN_TEST_CLAIM gereği yorumlanmadı, test artık spesifik sınıflama iddia etmiyor (yalnız makinenin çalıştığını doğruluyor), ayrı bir preregistration+re-run gerektirir. Regresyon sonunda 630/630 ✓, gerçek DB hash bu regresyon boyunca BİREBİR SABİT kaldı (conftest.py session-teardown assertion'ı hiç tetiklenmedi). **0 blocker.** MIGRATION_LOG.md'ye entry EKLENMEDİ (repository policy: "Yalnız schema/migration değişikliğinde güncellenir" — bu batch 0 DDL, salt veri backfill'i, emsal: W8-HOLD-BASELINE/W8-VOL-NORMALIZED-BASELINE'ın deneysel experiment_registry/results yazmaları da MIGRATION_LOG'a girmedi). **Canonical-backfill closure verdict: APPLIED_CLEAN, 0 blocker. Descriptive-research readiness verdict: CANONICAL_MIGRATION_READY_LIMITED_DESCRIPTIVE_POPULATION (değişmedi) + W8-SHORT-SUFFICIENCY-REOPENED (yeni, ayrı onay gerektiren açık soru).** | 630/630 ✓ (630 önceki test dosyası sayısı DEĞİŞMEDİ — 8 dosya checkpoint güncellemesi net yeni test eklemedi, yalnız stale sayı düzeltmesi) | `ami/lifecycle/migration_rehearsal.py` (reapply-signal-count-asymmetri belgelendi, yeni `reapply_signal_count`/`pre_rollback_signal_count` alanları eklendi) + 8 test dosyası checkpoint güncellemesi; **gerçek canonical.sqlite'a data-only write UYGULANDI** (54 sinyal/54 transition/216 path-obs/864+1242 provenance, 0 DDL); backup×1 hash-doğrulandı (pre-backfill); protected session delta ZERO | DONE (canonical, APPLIED) |
| BATCH-W8-SHORT-EXPANDED-BASELINE | 7B-1 (timing/path research, versioned rerun) | Sonnet 5 | 2026-07-04 | **W8 EXPANDED-SHORT BASELINE — preregistration + cycle-grouped versioned rerun, GERÇEK `data/ami/canonical.sqlite`'A UYGULANDI** (operatör onayı: "APPROVE W8 EXPANDED-SHORT BASELINE PREREGISTRATION AND VERSIONED RERUN" — BATCH-SHORT-NOISY-V1-CANON-BACKFILL'in incidental smoke-check bulgusunun [6/8 SHORT hücre INSUFFICIENT_SAMPLE eşiğini geçti] doğru şekilde ele alınması). Operatörün açık talimatı: bu bulgu "scientific result olarak TREATED edilmemeli" — önce dondurulmuş, versiyonlanmış bir rerun gerekir. **Yeni deney kimlikleri** (eskiler ASLA overwrite edilmedi): `E-W8-HOLD-BASELINE-002-SHORT-EXPANDED`, `E-W8-VOL-NORMALIZED-BASELINE-002-SHORT-EXPANDED` — `experiment_registry`/`experiment_results` her ikisi de `experiment_id`'ye scope'lu (INSERT...ON CONFLICT(experiment_id)/DELETE WHERE experiment_id=?) olduğundan yeni id'ler `E-W8-HOLD-BASELINE-001`/`E-W8-VOL-NORMALIZED-BASELINE-001`'e yapısal olarak DOKUNAMAZ — ayrıca `assert_old_experiments_untouched()` ile açıkça doğrulandı (snapshot-hash öncesi=sonrası, byte-identical). **Yeni `ami/research/w8_short_expanded_baseline.py`:** ZORUNLU cycle-grouped (signal-level DEĞİL) chronological 70/30 split — `compute_global_cycle_split()` TÜM SHORT satırlarını (herhangi bir ufuk) independent_cycle_id'ye göre grupluyor, her cycle'ın kendi üye satırları arasındaki EN ERKEN signal_birth_ts'i sıralama anahtarı olarak kullanıyor (EN GEÇ değil), cycle sayısına göre 70/30 kesiyor — TEK bir global partition (`train_cycle_keys`/`test_cycle_keys`) tüm 16 hücre için AYNEN reuse ediliyor (bir cycle asla bir hücrede TRAIN diğerinde TEST olamaz — `assert_zero_cycle_straddling()` ile her hücrede açıkça doğrulandı, 0 ihlal). **MIN_BUCKET_N=20 artık INDEPENDENT-CYCLE N'e uygulanıyor** (signal-level N'e DEĞİL) — `compute_cell()`'in sufficiency kontrolü `train_cycle_n`/`test_cycle_n` sayıyor. **Primary family:** 4 metrik (mfe_bps/mae_bps/mfe_anchor_vol_units/mae_anchor_vol_units) × 4 ufuk = 16 SHORT-only hücre, TEK ortak Holm düzeltmesi (16 p-value BİRLİKTE, iki ayrı 8-hücreli düzeltme DEĞİL) — depolama için sonuçlar metrik tipine göre iki deney-id'sine bölündü (raw-bps→002-SHORT-EXPANDED[HOLD], vol-normalized→002-SHORT-EXPANDED[VOL-NORM]), ama istatistiksel çıkarım TEK bir aile. **Composition diagnostic** (ikincil, tanımlayıcı-only, p-value YOK): pre-existing SHORT vs yeni SHORT_NOISY_BTC200K_CONFIRMED_V1 vs combined, ufuk başına N/median — üç dilim BAĞIMSIZ doğrulama olarak SUNULMADI. **Gerçek sonuç:** 60 distinct SHORT independent cycle (tüm ufuklar), global split 42 TRAIN/18 TEST cycle, 0 straddle. Ufuk-bazlı cycle sayıları: scalp_30m 42/18, scalp_1h 41/18, swing_4h 37/16, swing_24h 23/9 — **TEST tarafı HİÇBİR ufukta 20'ye ulaşmıyor** → **16/16 hücre INSUFFICIENT_SAMPLE** (Holm/bootstrap/permütasyon hiçbir hücrede hesaplanmadı, hepsi None/N-A, dürüstçe). Bu, önceki incidental smoke-check'in "6/8 yeterli" bulgusunun signal-level split'in bir ARTEFAKTI olduğunu KANITLIYOR — cycle-grouped disiplin altında gerçek sonuç tam tersi. **Final verdict: `EXPANDED_SHORT_INSUFFICIENT_AFTER_CYCLE_GROUPED_SPLIT`.** **Gerçek DB'ye yazılan:** yalnız `experiment_registry`(2 yeni satır)+`experiment_results`(15+15=30 satır) — `ami_events`/`ami_signal_lifecycle`/`ami_lifecycle_transitions`/`ami_lifecycle_path_observations`/`ami_lifecycle_field_provenance` (252/324/856/1296/12636) DEĞİŞMEDİ (doğrulandı), `E-W8-HOLD-BASELINE-001`/`E-W8-VOL-NORMALIZED-BASELINE-001` snapshot-hash birebir aynı kaldı, idempotent rerun doğrulandı (satır sayıları 2. çalıştırmada değişmedi). **0 blocker.** | 11 yeni test — **toplam AMI 641/641 ✓** (630 önceki + 11 yeni; full regresyon tüm 51 dosya ikişerli+bazı tekli, 0 hata) | yeni `ami/research/w8_short_expanded_baseline.py`; gerçek canonical.sqlite'a YALNIZ `experiment_registry`(2)+`experiment_results`(30 satır) write edildi — eski `-001` deneyleri byte-identical, lifecycle/path/provenance/event tabloları DEĞİŞMEDİ; protected session delta ZERO | DONE (canonical, APPLIED) |
| BATCH-W8-LONG-TIMING-STRUCTURE-001 | 7B-1 (timing/path research, new family) | Sonnet 5 | 2026-07-04 | **W8-LONG-TIMING-STRUCTURE-001 — WHEN (not whether) LONG's MFE/MAE occur within the fixed horizon, GERÇEK `data/ami/canonical.sqlite`'A UYGULANDI** (operatör onayı: "APPROVE W8-LONG TIMING-STRUCTURE-001" — BATCH-W8-SHORT-EXPANDED-BASELINE'in sonucu EXPANDED_SHORT_INSUFFICIENT_AFTER_CYCLE_GROUPED_SPLIT olarak kabul edildikten sonra, "SHORT accumulation/new-event-family branch kalır" kararıyla LONG'a geçildi). **4 araştırma sorusu:** (1) MFE mi MAE'den önce mi oluşuyor — `intrabar_order_status` kategorik dağılımı (MFE_FIRST/MAE_FIRST/SAME_CANDLE_UNKNOWN) + türetilmiş ZERO_AT_REFERENCE alt-durumu (her iki time_to=0); (2) ne kadar sürede oluşuyor — time_to_mfe_ms/time_to_mae_ms + `*_fraction_of_horizon` kantilleri; (3) kronolojik olarak stabil mi — PRIMARY INFERENTIAL FAMILY; (4) 4 ufuk arasında nasıl değişiyor — her istatistik ufuk başına ayrı raporlandı, hiçbir cross-horizon pooling yok. Yeni `ami/research/w8_long_timing_structure.py`: `ami/research/w8_short_expanded_baseline.py`'nin cycle-grouped split makinesini (`compute_global_cycle_split`/`split_rows_by_cycle_keys`/`assert_zero_cycle_straddling`/`_cycle_key`) BİREBİR (`is` identity, teyit edildi) reuse etti — bu fonksiyonlar zaten SHORT'a özgü değildi. **Primary family: 8 hücre** (2 timing metrik [time_to_mfe_ms/time_to_mae_ms] × 4 ufuk, LONG-only, NO_SHORT_POOLING), TEK ortak Holm düzeltmesi. MIN_BUCKET_N=20 independent-cycle N'e uygulanıyor (split+ufuk başına). **Secondary descriptive** (p-value YOK): `intrabar_order_status` count/rate + ZERO_AT_REFERENCE + time-to-MFE/MAE/fraction kantilleri + `timing_delta_ms` (=time_to_mfe_ms-time_to_mae_ms) kantilleri + TRAIN-vs-TEST rate karşılaştırması (yalnız iki oran yan yana, p-value/test YOK) — hiçbiri entry/exit/management kuralı olarak yorumlanmadı. **Gerçek sonuç:** 216 sinyal/214 source-event/142 distinct LONG independent cycle (tüm ufuklar); global split 99 TRAIN/43 TEST cycle, 0 straddle. Ufuk-bazlı TEST-tarafı cycle sayısı: scalp_30m 43, scalp_1h 42, swing_4h 38, swing_24h 30 — **hepsi MIN_BUCKET_N=20'nin rahatça üzerinde → 8/8 hücre YETERLİ**. Yalnız 1 hücre (time_to_mfe_ms|swing_24h) pre-Holm nominal düşük p (0.015) taşıyor, ortak 8-hücreli Holm sonrası anlamsız (p=0.12) → **8/8 `ANSWERED_SUPPORTED_STABLE_BASELINE`**. **Final verdict: `LONG_TIMING_STRUCTURE_STABLE`.** Tanımlayıcı bulgular: MFE_FIRST/MAE_FIRST scalp_30m'de dengeli (%48.1/%51.9), ufuk uzadıkça MAE_FIRST'e kayıyor (scalp_1h %44.8/%55.2, swing_4h %34.9/%65.1, swing_24h %30.1/%69.9) — bu popülasyonda 0 SAME_CANDLE_UNKNOWN, 0 ZERO_AT_REFERENCE gözlendi. **Gerçek DB'ye yazılan:** yalnız `experiment_registry`(1 yeni satır)+`experiment_results`(15 satır) — `E-W8-HOLD-BASELINE-001`/`E-W8-VOL-NORMALIZED-BASELINE-001`/iki `-002-SHORT-EXPANDED` deneyi hiç dokunulmadı (farklı experiment_id scope'u, yapısal olarak imkansız), `ami_events`/`ami_signal_lifecycle`/`ami_lifecycle_transitions`/`ami_lifecycle_path_observations`/`ami_lifecycle_field_provenance` (252/324/856/1296/12636) DEĞİŞMEDİ (doğrulandı), idempotent rerun doğrulandı (satır sayısı 2. çalıştırmada aynı). **0 blocker.** | 11 yeni test — **toplam AMI 652/652 ✓** (641 önceki + 11 yeni; full regresyon tüm 52 dosya ikişerli+bazı tekli, 0 hata) | yeni `ami/research/w8_long_timing_structure.py`; gerçek canonical.sqlite'a YALNIZ `experiment_registry`(1)+`experiment_results`(15 satır) write edildi — tüm önceki deneyler + lifecycle/path/provenance/event tabloları DEĞİŞMEDİ; protected session delta ZERO | DONE (canonical, APPLIED) |
| BATCH-W8-LONG-NESTED-PATH-ACCUMULATION-001 | 7B-1 (timing/path research, new family) | Sonnet 5 | 2026-07-04 | **W8-LONG-NESTED-PATH-ACCUMULATION-001 — ufuk genişledikçe (30m→1h→4h→24h) ne kadar ek MFE/|MAE| birikiyor, GERÇEK `data/ami/canonical.sqlite`'A UYGULANDI** (operatör onayı: "APPROVE W8-LONG-NESTED-PATH-ACCUMULATION-001" — §76'nın LONG_TIMING_STRUCTURE_STABLE sonucu kabul edildikten sonra sıradaki descriptive LONG path-structure dalgası). **4 araştırma sorusu:** (1) her aralıkta ne kadar ek MFE birikiyor; (2) ne kadar ek |MAE|; (3) ufuk uzadıkça oransal olarak MFE mi |MAE| mi daha çok ekleniyor (median_delta_mfe_minus_delta_abs_mae ile); (4) bu artımlı dağılımlar kronolojik olarak stabil mi (PRIMARY FAMILY). **COMMON-COHORT ZORUNLULUĞU:** yalnız 4 UFUKTA BİRDEN observation_status=OK olan LONG sinyaller — `fetch_common_cohort()` popülasyon kompozisyonunun ufuklar arasında DEĞİŞMEDİĞİNİ garantiliyor (her aralık AYNI 123 sinyal üzerinden hesaplanıyor), excluded-incomplete-horizon sayısı (93) açıkça raporlandı (216 LONG-≥1-OK-ufuk'tan). Yeni `ami/research/w8_long_nested_path_accumulation.py`: split makinesi yine `w8_short_expanded_baseline`'dan BİREBİR reuse edildi (tek satır artık sinyal-başına, ufuk-başına DEĞİL — genel split fonksiyonları buna da uyuyor). **6 frozen türetilmiş alan:** delta_mfe_30m_to_1h/1h_to_4h/4h_to_24h + delta_abs_mae_30m_to_1h/1h_to_4h/4h_to_24h (=mfe_bps_X-mfe_bps_Y / abs(mae_bps_X)-abs(mae_bps_Y)). **Nested non-negativity:** path_metrics'in reference_price/effective_path_start_ts'i ufuktan BAĞIMSIZ (yalnız signal_birth_ts'e bağlı) olduğu için ufuk pencereleri iç içe geçmiş (30m⊂1h⊂4h⊂24h) — MFE (running max) ve |MAE| matematiksel olarak yalnız artabilir; bu VARSAYILMADI, `assert_nested_nonnegativity()` ile gerçek popülasyona karşı EPSILON=1e-6 toleransıyla doğrulandı: **0 ihlal**. **Primary family: 6 hücre** (3 MFE-aralık + 3 |MAE|-aralık), TEK ortak Holm. MIN_BUCKET_N=20 independent-cycle N'e (TRAIN ve TEST'te) uygulanıyor. **Secondary descriptive** (p-value YOK): aralık-başına medyan artımlı MFE/|MAE| + medyan(delta_mfe-delta_abs_mae) + 24h MFE/|MAE|'nin 30m/1h/4h'de ne kadarının zaten yakalandığı (sıfır-payda ASLA bölünmedi, `_capture_fraction_stats()` ile açıkça hariç tutuldu ve sayıldı) — hiçbiri hold/exit/stop kuralı olarak sunulmadı. **Gerçek sonuç:** 123 sinyal/123 source-event/86 distinct LONG independent cycle (4-ufuk-complete cohort); global split 60 TRAIN/26 TEST cycle, 0 straddle. **6/6 hücre YETERLİ ve `ANSWERED_SUPPORTED_STABLE_BASELINE`** (yalnız delta_mfe_4h_to_24h pre-Holm nominal p=0.0385, ortak 6-hücreli Holm sonrası p=0.231, anlamsız). **Final verdict: `LONG_NESTED_PATH_STABLE`.** Tanımlayıcı bulgu: medyan artımlı |MAE| HER ÜÇ aralıkta da 0.0 bps (medyan sinyalin adverse excursion'ı ilk 30 dakikada tamamen gerçekleşmiş) — medyan artımlı MFE büyümeye devam ediyor (+48.76bps 1h→4h, +81.43bps 4h→24h); medyan 24h-yakalama oranları MFE %13.5/%19.5/%60.2, |MAE| %54.2/%81.9/%100.0 (30m/1h/4h) — yalnız path-tanımı, ÖNERİLEN HOLD SÜRESİ DEĞİL (operatör talimatı gereği). **Gerçek DB'ye yazılan:** yalnız `experiment_registry`(1 yeni satır)+`experiment_results`(10 satır) — tüm 5 önceki deney hiç dokunulmadı, `ami_events`/`ami_signal_lifecycle`/`ami_lifecycle_transitions`/`ami_lifecycle_path_observations`/`ami_lifecycle_field_provenance` (252/324/856/1296/12636) DEĞİŞMEDİ (doğrulandı), idempotent rerun doğrulandı. **0 blocker.** | 14 yeni test — **toplam AMI 666/666 ✓** (652 önceki + 14 yeni; full regresyon tüm 53 dosya ikişerli+bazı tekli, 0 hata) | yeni `ami/research/w8_long_nested_path_accumulation.py`; gerçek canonical.sqlite'a YALNIZ `experiment_registry`(1)+`experiment_results`(10 satır) write edildi — tüm önceki deneyler + lifecycle/path/provenance/event tabloları DEĞİŞMEDİ; protected session delta ZERO | DONE (canonical, APPLIED) |
| BATCH-CANDLE-GAP-REMEDIATION | infra (candle data quality, source audit) | Sonnet 5 | 2026-07-04 | **AMI HISTORICAL CANDLE GAP REMEDIATION — source audit + disposable rehearsal ONLY, NO_REAL_CANONICAL_WRITE** (operatör onayı: "APPROVE AMI HISTORICAL CANDLE GAP REMEDIATION — SOURCE AUDIT AND DISPOSABLE REHEARSAL ONLY", W8-LONG regime-readiness/volatility-audit dizisinin April-gap bulgusunun takibi). **Phase A — tam-tarih envanter (yalnız Nisan değil):** ETHUSDT 1m, 2026-02-15→2026-07-03, expected=199180/present=173464/**missing=25716**, 208 distinct gap koşusu, 0 duplicate open_ts_ms, 0 non-monotonic, 0 invalid OHLC. Aylık dağılım: Şub=42/Mar=164/**Nis=4239**/**May=15315**(!)/Haz=5938/Tem=18 — May aslında Nisan'dan DAHA BÜYÜK bir sorunmuş (en büyük tek koşu: 2026-06-02→06-05, 4739 dk = 3.3 gün). **Phase B — authoritative source reconciliation:** `agg_trades`'in kendisi 174M satır/tam aralık kapsıyor, ama 208/208 gap koşusunun HİÇBİRİNDE agg_trades eşleşen satır YOK (epoch-exact doğrulama — `datetime.strptime().timestamp()` ile yapılan ilk ad-hoc spot-check, makinenin UTC+3 yerel saatini kullandığı için YANLIŞ POZİTİF verdi, düzeltilmiş epoch-tam kontrol 0 satır buldu) → **local candle-builder backfill bug DEĞİL, gerçek upstream/collector-seviyesi eksiklik**. Proje zaten onaylı kaynak: Binance USDT-M **Futures** (`fapi.binance.com`, mevcut `binanceusdm` ccxt entegrasyonuyla eşleşiyor, ASLA spot `api.binance.com`) — canlı ağ testi başarılı, **25716/25716 mum (208/208 pencere) %100 retrievable**, 0 duplicate/yanlış-hizalı/geçersiz-OHLC/mevcut-satırla-çakışma. **Phase C — sidecar/disposable rehearsal:** yeni `ami/chart/candle_gap_repair_rehearsal.py` — `CANDLE_DEFINITION_VERSION_EXTERNAL_REPAIR="candle-binance-fapi-repair-v1"` (eskiden `candle-agg_trades-v1`'den yapısal olarak ayrık, UNIQUE(symbol,timeframe,open_ts_ms,version) çakışması İMKANSIZ). `validate_kline_row()` (1m-alignment/OHLC-geçerlilik/close_time reddi) + `build_candidate_rows()` (batch-içi duplicate reddi) + `apply_repair_rows()`/`rollback_repair()`. Disposable sonuç: 25716 aday, 25716 kabul, 0 red, 0 conflict, idempotent rerun (manifest-hash birebir), rollback (25716 satır silindi, pre-repair manifest'e BİREBİR döndü), reapply (post-repair manifest'e BİREBİR döndü). **Phase D — impact rehearsal (disposable before/after):** 1296 (sinyal,ufuk) çiftinden **170'i etkilendi** — 149'u forward-path-window gap düzelmesi (observation_status/mfe_bps/mae_bps/timing), **21'i YENİ bir mekanizma: 60-candle GERİYE DÖNÜK realized_vol penceresi de repaired candle'lardan etkilenebiliyor** (yalnız `*_anchor_vol_units` alanları değişiyor, observation_status/mfe_bps/mae_bps AYNI kalıyor — path_metrics.py'nin mevcut, DEĞİŞTİRİLMEYEN kodunun kendi iki-eksenli tasarımının [observation_status vs volatility_status] doğru çalıştığının kanıtı). **1126/1296 çift byte-identical kanıtlandı** (index_before==index_after aynı sonucu üretiyor, unaffected iddiası doğrulandı, varsayılmadı). observation_status OK 1096→1245 (+149)/MISSING_INTERNAL_GAP 187→38. Aylık recovery: Şub=4/Mar=63/Nis=20/Haz=62. Coverage-only readiness (outcome/p-value OKUNMADI): **LONG 24h-complete cohort 123→194 sinyal** (86→131 cycle, TRAIN/TEST 60/26→91/40 — belirgin iyileşme, April/right-censoring endişesini önemli ölçüde azaltıyor); expanding-window vol-state HIGH hâlâ INSUFFICIENT (test_cyc=19, değişmedi); SHORT expanded baseline hâlâ INSUFFICIENT (test_cyc 18→19, verdict'i çevirmiyor); rs_state ETKİLENMEDİ (mark_prices'tan türetiliyor, ami_candles'tan değil). **Verdict: `HISTORICAL_CANDLE_REPAIR_READY_FOR_CONTROLLED_BACKFILL`.** Tüm tamamlanmış deneyler (W8-HOLD/-VOL-NORMALIZED/-LONG-TIMING/-LONG-NESTED-PATH/EXPANDED-SHORT ×2) DOKUNULMADI — bu batch hiçbir deneye de yazmadı (yalnız disposable candle rehearsal). **0 blocker.** | 9 yeni test — **toplam AMI 675/675 ✓** (666 önceki + 9 yeni; full regresyon 54 dosya, 1 geçici/tekrarlanmayan hata — canlı arka-plan collector'ın microstructure.db mtime'ını değiştirmesi, regresyon DEĞİL) | yeni `ami/chart/candle_gap_repair_rehearsal.py` + 1 test dosyası; gerçek canonical.sqlite'a 0 write (hash/mtime testte + manuel doğrulamada BİREBİR SABİT); gerçek `data/microstructure.db`'ye 0 write (yalnız mode=ro sorgular + harici Binance API çağrıları, hiçbir local dosyaya yazma yok); protected session delta ZERO | DONE (disposable rehearsal only, NOT applied to real DB) |
| BATCH-CANDLE-REPAIR-CANONICAL-BACKFILL | infra (candle data quality, controlled backfill) | Sonnet 5 | 2026-07-05 | **AMI HISTORICAL CANDLE REPAIR — CONTROLLED CANONICAL BACKFILL + VERSIONED PATH CORRECTION, GERÇEK `data/ami/canonical.sqlite`'A UYGULANDI** (operatör onayı: "APPROVE AMI HISTORICAL CANDLE REPAIR CONTROLLED CANONICAL BACKFILL + VERSIONED PATH CORRECTION" — önceki disposable rehearsal'ın kabulünün devamı). **Part 0 — source package freeze:** `data/ami/candle_repair_source_package/` (raw_klines.json, request_manifest.json [216 request, her biri raw+normalized SHA256], gap_manifest_pre_repair.json, package_manifest.json) — package_sha256=`bb660b80ff1a...`, row_content_sha256=`bf1b95b645...` (Binance'e HİÇ bağlanmadan `build_candidate_rows()` ile 25716/25716 deterministic reconstruction KANITLANDI). **Part 1 — preflight+backup:** checkpoint operatörün beklediğiyle BİREBİR (schema=10/events=252/signals=324/transitions=856/path=1296/provenance=12636/FORWARD_N=0/bindings=2), backup=`data/ami/backups/canonical_pre_candle_repair_backfill_20260705_000709.sqlite` (sha256 kaynakla birebir doğrulandı, path/provenance/experiment_results/experiment_registry content-hash'leri de ayrıca kaydedildi). microstructure.db için mtime YERİNE operatörün istediği "bounded collector-aware invariant" (6 adet hızlı 1-saatlik spot-check penceresi, tam-aralık COUNT/SUM 650GB+ ölçekte impractical olduğu için) kullanıldı. **Part 2 — candle backfill:** `apply_repair_rows()` ile 25716 1m satırı gerçek DB'ye yazıldı (`candle_definition_version="candle-binance-fapi-repair-v1"`, `UNIQUE(symbol,timeframe,open_ts_ms,version)` kısıtıyla eskiden yapısal olarak ayrık) — missing 1m=0. Yeni `rederive_5m_with_source_traceability()`: `ami.chart.candle_builder.derive_higher_timeframe()` DEĞİŞTİRİLMEDEN reuse edildi (5m için ayrı bir authoritative kaynak İCAT EDİLMEDİ), yalnız o fonksiyonun sabit-kodlanmış `candle_definition_version`'ı, gerçek 1m-çocuklarının versiyon(lar)ını yansıtacak şekilde düzeltildi (path_metrics.py'nin zaten kullandığı comma-join blend convention'ı) — 5292 5m satır yazıldı (5036 yeni + 256 blended-version-düzeltmeli), missing 5m=0. **Part 3 — path versioning contract:** `ami.lifecycle.path_metrics.freeze_and_record()`'ın kendi docstring'i path observations'ı zaten "re-derivable materialization... NOT append-only" olarak TANIMLIYOR — operatörün 3 seçenekli tercih sıralamasında (1: yeni versiyon ekle, 2: correction-record, 3: repo zaten rebuildable-materialized-state tanımlıyorsa controlled replacement) bu üçüncü koşulu karşılıyor. Naif "tüm 1296 satırı yeni versiyon altında paralel kopyala" yaklaşımı REDDEDİLDİ (mevcut feature_gateway tüketicileri path_definition_version'a göre filtrelemiyor, çift-sayım riski taşırdı — belgelenmiş bir tasarım kararı). Yeni `ami/lifecycle/path_candle_repair_correction.py`: `PATH_DATA_VERSION_CANDLE_REPAIR_R1="path-v2-candle-repair-r1"` — yalnız GERÇEKTEN DEĞİŞEN 170 (signal,horizon) çiftine yeni satır (observation_id path_definition_version'ı hash'lediği için orijinal "path-v2" satırıyla ASLA çakışmıyor), orijinal 1296 satır hiç dokunulmadı/silinmedi. candle_data_version traceability = mevcut (değiştirilmeyen) `candle_definition_version` alanı (path_metrics.py'nin kendi multi-version blend mantığıyla doğal olarak doğru dolduruluyor). **Part 4 — targeted correction:** 149 forward-window (`MISSING_INTERNAL_GAP`→OK, bps/timing değişti) + 21 vol-only (yalnız `*_anchor_vol_units`, observation_status/bps AYNI kaldı — path_metrics'in DEĞİŞTİRİLMEYEN iki-eksenli tasarımının doğru çalıştığının kanıtı) = **170 satır** eklendi (path_observations 1296→1466). **1126 satır byte-identical kanıtlandı** (backup content-hash'iyle birebir eşleşti). Effective (unaffected=path-v2 + affected=corrected) dağılım: observation_status OK=1245/MISSING_INTERNAL_GAP=38/EXCLUDED_NO_HORIZON_DATA=13 — **operatörün beklediğiyle BİREBİR**; volatility_status OK=1243/NOT_APPLICABLE=51/INVALID=2. Provenance sayısı DEĞİŞMEDİ (12636 — field-provenance signal-seviyesinde, path_definition_version'a bağlı değil, yeni satır gerekmedi). **Part 5 — deney immutability:** 6 tamamlanmış deneyin (`E-W8-HOLD-BASELINE-001`/`E-W8-VOL-NORMALIZED-BASELINE-001`/iki `-002-SHORT-EXPANDED`/`E-W8-LONG-TIMING-STRUCTURE-001`/`E-W8-LONG-NESTED-PATH-ACCUMULATION-001`) `experiment_results`+`experiment_registry` tam-tablo content-hash'i backup'la BİREBİR (hiç dokunulmadı). **Part 6 — readiness (coverage-only, outcome OKUNMADI):** LONG 24h-complete cohort 194 sinyal/131 cycle/91-40 TRAIN-TEST (rehearsal'ın öngördüğüyle birebir); SHORT expanded baseline 61 cycle/42-19 (hâlâ INSUFFICIENT); expanding-window vol-state HIGH hâlâ INSUFFICIENT (test_cyc=19, değişmedi); rs_state ETKİLENMEDİ (mark_prices'tan). **Part 7 — idempotency+rollback:** gerçek DB'ye candle+path-correction rerun'ı sıfır yeni satır/byte-identical content-hash ile idempotent doğrulandı; rollback yalnız DISPOSABLE bir post-repair kopyada rehearsal edildi (gerçek DB'ye ASLA rollback uygulanmadı) — tam pre-repair manifest'e (173464/34801/1296) ve content-hash'e BİREBİR döndü, reapply tam post-repair state'i (199180/40093) yeniden üretti. **Part 8 — postflight:** full regresyon çalıştırıldı, 4 önceden-yeşil dosyada (`path_migration_rehearsal` — reapply artık yalnız freeze_and_record()'ın bildiği path-v2 popülasyonunu yeniden kurduğu için pre-rollback'ten [1466] doğal olarak farklı [1296], `short_noisy_v1_migration_rehearsal.py` — path-observation sayımı artık path_definition_version='path-v2'ye scope'landı [düzeltme öncesi correction satırlarını da yanlışlıkla sayıyordu], `short_noisy_v1_rehearsal.py`'nin microstructure.db testi — mtime kontrolü tamamen kaldırıldı [operatörün "mtime'ı ana untouched-assertion olarak kullanma" talimatı gereği], `w8_hold_baseline.py` — SHORT/swing_24h hücresi candle repair sonrası signal-level sufficiency eşiğini geçti, yorumlanmadı, yalnız flag'lendi) stale-checkpoint/reopened-sufficiency düzeltmesi yapıldı — hiçbiri test LOGIC'ini zayıflatmadı. Schema fingerprint DEĞİŞMEDİ (0 DDL). **0 blocker.** **Final verdict: `CANDLE_REPAIR_AND_PATH_CORRECTION_APPLIED_CLEAN`.** | 10 yeni test — **toplam AMI 685/685 ✓** (675 önceki + 10 yeni; full regresyon tüm 55 dosya, microstructure.db'nin SQLite header/change-counter'ının canlı collector'ın her commit'inde değiştiği [ara sıra bounded-prefix-hash kontrolünü tetikleyen ama gerçek veri değişikliği OLMAYAN] davranışı dokümante edildi, rerun'da her zaman temiz) | yeni `ami/chart/candle_gap_repair_rehearsal.py` (+`rederive_5m_with_source_traceability`/rollback LIKE-fix) + yeni `ami/lifecycle/path_candle_repair_correction.py` + 2 test dosyası + 4 önceden-var test dosyası güncellemesi; **gerçek canonical.sqlite'a controlled backfill UYGULANDI** (25716 1m + 5292 5m candle satırı + 170 versioned path-correction satırı, 0 DDL); backup×1 hash-doğrulandı; source package donduruldu (`data/ami/candle_repair_source_package/`); protected session delta ZERO | DONE (canonical, APPLIED) |
| BATCH-W8-LONG-NESTED-PATH-ACCUMULATION-002-CANDLE-REPAIR | 7B-1 (timing/path research, corrected-data rerun) | Sonnet 5 | 2026-07-05 | **W8-LONG-NESTED-PATH-ACCUMULATION-002-CANDLE-REPAIR — corrected candle-data rerun, GERÇEK `data/ami/canonical.sqlite`'A UYGULANDI** (operatör onayı: "APPROVE W8-LONG-NESTED-PATH-ACCUMULATION-002 CORRECTED CANDLE-DATA RERUN" — §79 candle repair kapanışının kabulü; v001 [§77] HiÇ MUTATE EDILMEDI, yeni experiment_id). **Part 0 — effective path selection safety:** yeni `fetch_effective_path_observations()`/`effective_path_selection_audit()` (`ami/lifecycle/path_candle_repair_correction.py`) — physical_row_count_by_version={{'path-v2':1296,'path-v2-candle-repair-r1':170}}, **effective_row_count=1296** (operatör beklentisiyle birebir), duplicate_effective_pair_n=0, equals={{'observation_status':'OK'}} filtreli fetch=**1245** satır (birebir). BLOCKED_BY_EFFECTIVE_PATH_SELECTION TETIKLENMEDI. **Part 1 — cohort:** yeni `ami/research/w8_long_nested_path_accumulation_002_candle_repair.py` (v001'in tüm generic makinesini + w8_short_expanded_baseline split makinesini BIREBIR reuse ediyor, yalnız fetch_common_cohort() effective selector'a override edildi) — signal_n=194 (v001:123,+71), independent_cycle_n=131 (v001:86,+45), excluded_incomplete_horizon_n=25 (v001:93), cycle_straddling_violations=0. Aylık: Şub=24/Mar=55/Nis=24/Haz=91. Route: %100 LONG_SILENCE. **Part 2 — split:** fresh cycle-grouped split (v001'den REUSE edilmedi) — TRAIN=91/TEST=40 cycle (v001:60/26). **Part 3 — frozen metrics:** v001'in 6 alanı aynen (ekleme/çıkarma yok). **Part 4 — primary family:** 6/6 hücre **STABLE** (Holm p=1.0 hepsinde, CI sıfırı kapsıyor); medyanlar delta_mfe_30m_to_1h=0.0, delta_mfe_1h_to_4h=+27.79bps, delta_mfe_4h_to_24h=+74.70bps, delta_abs_mae_*=0.0 (üç aralıkta da); nested non-negativity=0 ihlal. **Part 5 — secondary descriptive:** v001'in compute_secondary_descriptive() reuse edildi, hold/exit/stop-loss olarak YORUMLANMADI. **Part 6 — v001 karşılaştırması:** v001/v002 BAĞIMSIZ TEKRAR olarak DEĞIL (cohort örtüşüyor, v002 v001'in superset genişlemesi) tutarlılık kontrolü olarak yapıldı — 6/6 hücre deĞişmedi, 6 medyan aynı işaret. **comparison_conclusion="REPLICATED_ON_CORRECTED_EXPANDED_COHORT"**. **Part 7 — integrity:** v001'in 10 experiment_results satırı byte-identical (önce/sonra); canonical ami_signal_lifecycle(324)/ami_lifecycle_path_observations(1466)/ami_lifecycle_field_provenance(12636)/ami_candles(239273) HIÇ DEĞişMEDI; experiment_registry 16→17 (+1, supersedes_experiment_id=E-W8-LONG-NESTED-PATH-ACCUMULATION-001), experiment_results 199→213 (+14); idempotent rerun doğrulandı (14→14); protected paths dokunulmadı. **0 blocker. Final verdict: `LONG_NESTED_PATH_STABLE_CORRECTED_DATA`.** | 9 yeni test (`test_ami_research_w8_long_nested_path_accumulation_002.py`) — **toplam AMI 689/689 ✓** (`--collect-only` ile doğrudan ölçüldü — 56 dosya, 55 dosya sequential ≤°dosya/çağrı kuralıyla koşuldu, hepsi yeşil; önceki "685" narrative toplamdı, 689 artık ground-truth) | yeni `ami/research/w8_long_nested_path_accumulation_002_candle_repair.py` + `ami/lifecycle/path_candle_repair_correction.py`'ye 2 yeni fonksiyon + 1 yeni test dosyası; **gerçek canonical.sqlite'a yalnız experiment_registry/experiment_results yazıldı** (0 lifecycle/path/candle mutation); backup gerekmedi (emsal: önceki salt-experiment batch'ler); protected session delta ZERO | DONE (canonical, APPLIED) |
| BATCH-EFFECTIVE-PATH-EXPERIMENT-IMMUTABILITY-HARDENING | safety hardening (path-version contract + write-guard, NOT a research wave) | Sonnet 5 | 2026-07-05 | **AMI EFFECTIVE-PATH AND EXPERIMENT-IMMUTABILITY SAFETY HARDENING** (operatör onayı: "APPROVE AMI EFFECTIVE-PATH AND EXPERIMENT-IMMUTABILITY SAFETY HARDENING" — §80 sonrası reconciliation raporunun tespit ettiği 4 sistemik riski kapatan kod+test batch'i, **research/outcome recomputation YOK**). **GOAL A:** `ami/research/feature_gateway.py::fetch_path_observations()`'a `effective: bool=False` + yeni `AmbiguousPathVersionError` — eşleşen satırlar >1 `path_definition_version` içeriyorsa ve caller ne exact version pin'lemiş ne `effective=True` geçmişse fail-closed (lexical "en yeni" tahmini YOK); `path_candle_repair_correction.py`'nin 2 iç çağrısı `effective=True` ile güncellendi (davranış değişmedi, 1296 effective/0 duplicate gerçek veriyle doğrulandı). **GOAL B:** yeni `ami/warehouse/experiment_ledger.py` (`record_experiment_registry`/`record_experiment_results`/`ImmutableExperimentConflict`) — eski `ON CONFLICT DO UPDATE`+kör `DELETE FROM experiment_results` deseni **6 modülün tamamında** değiştirildi: yeni id→INSERT, aynı id+bit-bit aynı içerik→NOOP_IDENTICAL, aynı id+herhangi bir fark→`IMMUTABLE_EXPERIMENT_CONFLICT` (hiç yazmadan reddeder); bookkeeping timestamp'ler içerik karşılaştırmasından hariç. **GOAL C (5 legacy modül):** `fetch_path_observations()` çağrılarına `path_definition_version="path-v2"` pin eklendi. **Beklenmedik gerçek bulgu:** `E-W8-HOLD-BASELINE-001`/`E-W8-VOL-NORMALIZED-BASELINE-001` (LONG+SHORT, setup_id filtresiz) path-pin'e RAĞMEN artık reprodüksiyon değil — `BATCH-SHORT-NOISY-V1-CANON-BACKFILL` (bu ikisi donduktan SONRA +54 SHORT sinyali) `ami_signal_lifecycle`'ı 266→317'ye büyütmüş (candle repair'dan bağımsız bir drift). Yeni guard bunu doğru şekilde `IMMUTABLE_EXPERIMENT_CONFLICT` ile durduruyor (GOAL C'nin "OR prevent direct execution... with a clear frozen experiment error" maddesi) — production kod DEĞİŞTİRİLMEDİ, yalnız bu 2 modülün testleri fail-closed'ı doğrulayacak şekilde güncellendi. Diğer 5 experiment_id drift YOK, NOOP_IDENTICAL ile byte-identical kanıtlandı. **GOAL D:** +18 yeni test (`test_ami_research_feature_gateway.py`+5, yeni `test_ami_warehouse_experiment_ledger.py`+8, yeni `test_ami_effective_path_and_experiment_immutability_hardening.py`+3, `w8_hold_baseline`/`w8_vol_normalized_baseline` testleri net+1 her biri [idempotent-rerun testi compute-only + fails-closed-on-drift'e bölündü], `w8_short_expanded_baseline` testi artık -001/-VOL-001'i fresh çağırmıyor). **0 blocker. Final verdict: `EFFECTIVE_PATH_AND_EXPERIMENT_IMMUTABILITY_HARDENED`.** | +18 yeni test — **toplam AMI 707/707 ✓** (`--collect-only` ile ölçüldü — 57 dosya, ≤2-dosya/çağrı sequential, hepsi yeşil) | yeni `ami/warehouse/experiment_ledger.py` + 2 yeni test dosyası + `feature_gateway.py`/`path_candle_repair_correction.py`/6 `w8_*.py` modülü + 3 test dosyası güncellendi; **gerçek canonical.sqlite'a 0 write** (yalnız read-only + conftest izole disposable kopyası; mtime/schema/events/signals/path_rows/experiment_registry BİREBİR SABİT); protected paths dokunulmadı; protected session delta ZERO | DONE (safety hardening, APPLIED — code+tests only, no new experiment result) |
| BATCH-W8-SHORT-EXPANDED-BASELINE-003-CANDLE-REPAIR | 7B-1 (timing/path research, corrected-data rerun) | Sonnet 5 | 2026-07-05 | **W8-SHORT-EXPANDED-BASELINE-003-CANDLE-REPAIR — corrected candle-data immutable rerun** (operatör onayı: "APPROVE W8 SHORT-EXPANDED BASELINE 003 CORRECTED CANDLE-DATA IMMUTABLE RERUN" — §81 hardening kabulünün ardından ranked rerun queue #1). İki yeni id (`E-W8-HOLD-BASELINE-003-SHORT-EXPANDED-CANDLE-REPAIR`/`E-W8-VOL-NORMALIZED-BASELINE-003-SHORT-EXPANDED-CANDLE-REPAIR`) — `-002-SHORT-EXPANDED`'in corrected-data rerun'ı, **-002 hiç çalıştırılmadı**. Yeni `ami/research/w8_short_expanded_baseline_003_candle_repair.py`: -002'nin generic makinesi (`compute_cell`/`compute_global_cycle_split`/`split_rows_by_cycle_keys`/`assert_zero_cycle_straddling`/`_cycle_key`/`_cell_rows`/`compute_composition_diagnostic`) `is`-identity reuse; yalnız 2 fetch fonksiyonu `fetch_effective_path_observations()`'a override edildi. **Part 0:** `verify_effective_path_selection_integrity()` gerçek veride 1466/170/1296/0 hepsi eşleşti (`passed=True`); sentetik mismatch senaryosu da `BLOCKED_BY_EFFECTIVE_PATH_SELECTION`'ı gerçekten tetikliyor (erken-dönüş, 0 popülasyon fetch). **Global split operatörün coverage-only beklentisiyle BİREBİR eşleşti: total=61/train=42/test=19** (zorlanmadı, hesaplandı). **16/16 hücre `INSUFFICIENT_SAMPLE`** (MIN_BUCKET_N=20 korunuyor, bootstrap/perm/Holm hiçbiri çalışmadı — Holm n=0 valid p-value ile no-op), her hücreye `DESCRIPTIVE_ONLY_NOT_INFERENTIAL` etiketi. **Correction impact audit** SHORT-özgü 45 satır/28 sinyal/24 event/18 cycle/0 class_b — reconciliation raporuyla **0 mismatch**, swing_24h yoğunlaşması doğrulandı. Comparison-with-v002: hiçbir hücre INSUFFICIENT_SAMPLE'dan çıkmadı. Immutable ledger: ilk çalıştırma INSERT, rerun NOOP_IDENTICAL, -002 byte-identical kaldı. **Final verdict: `EXPANDED_SHORT_INSUFFICIENT_AFTER_CYCLE_GROUPED_SPLIT_CORRECTED_DATA`.** **0 blocker.** | +13 yeni test (`test_ami_research_w8_short_expanded_baseline_003.py`) — **toplam AMI 720/720 ✓** (`--collect-only`, 58 dosya, ≤2-dosya/çağrı sequential) | yeni `ami/research/w8_short_expanded_baseline_003_candle_repair.py` + 1 yeni test dosyası; **GERÇEK `data/ami/canonical.sqlite`'A UYGULANDI** — `experiment_registry` 17→19 (+2), `experiment_results` +44 (22×2); `ami_events`(252)/`ami_signal_lifecycle`(324)/`ami_lifecycle_path_observations`(1466)/`ami_lifecycle_field_provenance`(12636) HİÇ DEĞİŞMEDİ; idempotent rerun aynı oturumda doğrulandı (registry_n sabit kaldı); -002'nin dataset_hash'i dokunulmadı; protected paths dokunulmadı | DONE (canonical, APPLIED) |
| BATCH-W8-HOLD-BASELINE-004-LONG-CORRECTED-CYCLE-GROUPED | 7B-1 (timing/path research, corrected-data rerun) | Sonnet 5 | 2026-07-05 | **W8-HOLD-BASELINE-004-LONG-CORRECTED-CYCLE-GROUPED — raw LONG baseline, corrected data + cycle-grouped split** (operatör onayı: "APPROVE W8 LONG RAW HOLD BASELINE CORRECTED-DATA + CYCLE-GROUPED VERSION"). Yeni tek id `E-W8-HOLD-BASELINE-004-LONG-CORRECTED-CYCLE-GROUPED` — `E-W8-HOLD-BASELINE-001`'in LONG-only raw mfe_bps/mae_bps kısmının corrected-data+cycle-grouped rerun'ı (`supersedes_experiment_id=E-W8-HOLD-BASELINE-001`), **v001 hiç çalıştırılmadı**. **Bağımsız tekrar DEĞİL** (hem veri hem split metodolojisi değişti) — `historical_reference_experiment_id`/`candle_data_version`/`path_data_version`/`methodological_change` explicit kaydedildi. Yeni `ami/research/w8_hold_baseline_004_long_corrected_cycle_grouped.py`: cycle-grouped split makinesi `w8_short_expanded_baseline`'dan `is`-identity reuse; yalnız 8 hücre (mfe_bps+mae_bps×4 ufuk, LONG-only, vol-normalized YOK bu batch'te). **Part 0:** effective-path integrity 1466/170/1296/0 hepsi eşleşti. **Global split:** total=142/train=99/test=43 — MIN_BUCKET_N=20'nin üzerinde tüm ufuklarda → **8/8 hücre YETERLİ, gerçek bootstrap/perm/Holm çalıştı** (SHORT'un aksine). Pre-outcome coverage report (aylık dağılım+setup kompozisyonu+multi-signal-cycle sayıları) MFE/MAE okunmadan önce raporlandı. **8/8 hücre `ANSWERED_SUPPORTED_STABLE_BASELINE`** (Holm-p=1.0 hepsinde). **Comparison-with-v001 önemli düzeltme:** v001'in family-seviyesi popülasyonu LONG+SHORT KOMBİNE olduğu için doğrudan karşılaştırılmadı — v001'in KENDİ LONG hücrelerinden ufuk-bazlı türetildi (swing_24h 123→194, repair-driven +71, önceki batch'lerle tutarlı); 0/8 hücre değişti → `comparison_label=QUALITATIVELY_CONSISTENT_AFTER_CORRECTION_AND_CYCLE_GROUPING`. **Final verdict: `LONG_RAW_HOLD_BASELINE_STABLE_CORRECTED_CYCLE_GROUPED`.** **0 blocker.** | +10 yeni test (`test_ami_research_w8_hold_baseline_004.py`) — **toplam AMI 730/730 ✓** (`--collect-only`, 59 dosya, ≤2-dosya/çağrı sequential) | yeni `ami/research/w8_hold_baseline_004_long_corrected_cycle_grouped.py` + 1 yeni test dosyası; **GERÇEK `data/ami/canonical.sqlite`'A UYGULANDI** — `experiment_registry` 19→20 (+1), `experiment_results` 257→277 (+20); `ami_events`(252)/`ami_signal_lifecycle`(324)/`ami_lifecycle_path_observations`(1466)/`ami_lifecycle_field_provenance`(12636) HİÇ DEĞİŞMEDİ; v001 dataset_hash/results byte-identical; idempotent rerun aynı oturumda doğrulandı; protected paths dokunulmadı | DONE (canonical, APPLIED) |
| BATCH-W8-VOL-NORMALIZED-BASELINE-004-LONG-CORRECTED-CYCLE-GROUPED | 7B-1 (timing/path research, corrected-data rerun, paired) | Sonnet 5 | 2026-07-05 | **W8-VOL-NORMALIZED-BASELINE-004-LONG-CORRECTED-CYCLE-GROUPED — eşleştirilmiş vol-normalized LONG baseline** (operatör onayı: "APPROVE W8 VOL-NORMALIZED BASELINE 004 LONG CORRECTED-DATA + CYCLE-GROUPED", §83'ün kabulünün ardından). Yeni tek id `E-W8-VOL-NORMALIZED-BASELINE-004-LONG-CORRECTED-CYCLE-GROUPED` — `E-W8-VOL-NORMALIZED-BASELINE-001`'in LONG-only kısmının corrected-data+cycle-grouped rerun'ı (`supersedes_experiment_id=E-W8-VOL-NORMALIZED-BASELINE-001`), **v001 hiç çalıştırılmadı**. `historical_reference_experiment_id`/`paired_raw_baseline_experiment_id=E-W8-HOLD-BASELINE-004-LONG-CORRECTED-CYCLE-GROUPED`/`candle_data_version`/`path_data_version`/`methodological_change` explicit kaydedildi. **DEĞİL** volatility-state stratification — HIGH/LOW etiket/medyan-eşik/rejim-fitting hiçbiri YOK. Yeni `ami/research/w8_vol_normalized_baseline_004_long_corrected_cycle_grouped.py`: yalnız 8 hücre (mfe_anchor_vol_units+mae_anchor_vol_units×4 ufuk, LONG-only). **MANDATORY SPLIT REUSE:** split, eşleştirilmiş raw v004'ün KENDİ `fetch_population()`'ından (doğrudan import) yeniden hesaplandı (vol-filtrelenmiş popülasyondan DEĞİL) — `verify_split_matches_paired_raw_baseline()` v004'ün depolanmış `global_cycle_split`'iyle karşılaştırıp **`matches=True`** kanıtladı (total=142/train=99/test=43 birebir); volatility_status-dışlanan sinyaller (scalp_30m'de 1, scalp_1h'de 1) cycle'ın split tarafını değiştirmedi. **8/8 hücre YETERLİ, `ANSWERED_SUPPORTED_STABLE_BASELINE`** (Holm-p=1.0). **Comparison-with-raw-v004:** 0/8 hücre normalizasyon sonrası değişti → `comparison_label=RAW_AND_VOL_NORMALIZED_LONG_BASELINES_CONSISTENT`, `raw_stability_conclusion_survives_normalization=True`. **Comparison-with-v001:** aynı LONG+SHORT-kombine-düzeltme disiplini, 0/8 hücre değişti → `comparison_label=QUALITATIVELY_CONSISTENT_AFTER_CORRECTION_AND_CYCLE_GROUPING`. **Final verdict: `LONG_VOL_NORMALIZED_BASELINE_STABLE_CORRECTED_CYCLE_GROUPED`.** **0 blocker.** | +11 yeni test (`test_ami_research_w8_vol_normalized_baseline_004.py`) — **toplam AMI 741/741 ✓** (`--collect-only`, 60 dosya, ≤2-dosya/çağrı sequential) | yeni `ami/research/w8_vol_normalized_baseline_004_long_corrected_cycle_grouped.py` + 1 yeni test dosyası; **GERÇEK `data/ami/canonical.sqlite`'A UYGULANDI** — `experiment_registry` 20→21 (+1), `experiment_results` 277→300 (+23); `ami_events`(252)/`ami_signal_lifecycle`(324)/`ami_lifecycle_path_observations`(1466)/`ami_lifecycle_field_provenance`(12636) HİÇ DEĞİŞMEDİ; v001 VE eşleştirilmiş raw v004 byte-identical; idempotent rerun aynı oturumda doğrulandı; protected paths dokunulmadı | DONE (canonical, APPLIED) |
| BATCH-W8-LONG-TIMING-STRUCTURE-002-CANDLE-REPAIR-CYCLE-GROUPED | 7B-1 (timing/path research, corrected-data rerun, paired) | Sonnet 5 | 2026-07-05 | **W8-LONG-TIMING-STRUCTURE-002-CANDLE-REPAIR-CYCLE-GROUPED — corrected candle-data + effective-path rerun** (operatör onayı: "APPROVE W8-LONG-TIMING-STRUCTURE-002 CORRECTED CANDLE-DATA + EFFECTIVE-PATH RERUN", §83/§84'ün ardından). Yeni tek id `E-W8-LONG-TIMING-STRUCTURE-002-CANDLE-REPAIR-CYCLE-GROUPED` — `E-W8-LONG-TIMING-STRUCTURE-001`'in corrected-data+cycle-grouped rerun'ı (`supersedes_experiment_id=E-W8-LONG-TIMING-STRUCTURE-001`), **v001 hiç çalıştırılmadı**. `corrected_data_rerun_of`/`candle_data_version`/`path_data_version`/`paired_cycle_split_experiment_id=E-W8-HOLD-BASELINE-004-LONG-CORRECTED-CYCLE-GROUPED` explicit kaydedildi. Hâlâ descriptive path-timing araştırması — entry/exit/stop/management/hold/ekonomik iddia YOK. Yeni `ami/research/w8_long_timing_structure_002_candle_repair_cycle_grouped.py`: `fetch_population()` v004'ün KENDİ `fetch_population()`'ını (`is`-identity) kullanıp üstüne v001'in 3 türetilmiş alanını ekliyor — v002'nin timing popülasyonu v004'ün ham popülasyonuyla BİREBİR AYNI. `compute_cell`/`compute_horizon_descriptive`/`_rate`/`TIMING_METRICS` v001'den `is`-identity reuse; split makinesi `w8_short_expanded_baseline`'dan. **Mandatory split reuse:** `verify_split_matches_paired_cycle_split()` v004'ün depolanmış split'iyle karşılaştırıp **`matches=True`** kanıtladı (total=142/train=99/test=43 — v001'in KENDİ pre-repair split'iyle de aynı sayısal değer, açıklanabilir: repair zaten-bilinen cycle'lara eksik ufuklar ekliyor, yeni cycle eklemiyor). **Correction impact audit (class_a-only, LONG):** **104 satır/71 sinyal/71 event/49 cycle — operatörün beklediğiyle BİREBİR** (0 mismatch); 21 LONG class_b bilinçli hariç (timing alanlarını değiştirmiyor). **8/8 hücre YETERLİ, `ANSWERED_SUPPORTED_STABLE_BASELINE`** (Holm-p=1.0). **Comparison-with-v001:** 0/8 hücre değişti → `comparison_label=TIMING_STRUCTURE_CONSISTENT_ON_CORRECTED_EXPANDED_COHORT`; nitel bulgu ("ufuk uzadıkça MAE_FIRST artıyor") HAYATTA KALDI (`mae_first_increases_with_horizon_survives=True`, MAE_FIRST %52.1→%55.5→%63.1→%63.4). **Final verdict: `LONG_TIMING_STRUCTURE_STABLE_CORRECTED_DATA`.** **0 blocker.** | +10 yeni test (`test_ami_research_w8_long_timing_structure_002.py`) — **toplam AMI 751/751 ✓** (`--collect-only`, 61 dosya, ≤2-dosya/çağrı sequential) | yeni `ami/research/w8_long_timing_structure_002_candle_repair_cycle_grouped.py` + 1 yeni test dosyası; **GERÇEK `data/ami/canonical.sqlite`'A UYGULANDI** — `experiment_registry` 21→22 (+1), `experiment_results` 300→323 (+23); `ami_events`(252)/`ami_signal_lifecycle`(324)/`ami_lifecycle_path_observations`(1466)/`ami_lifecycle_field_provenance`(12636) HİÇ DEĞİŞMEDİ; v001 VE eşleştirilmiş raw v004 byte-identical; idempotent rerun aynı oturumda doğrulandı; protected paths dokunulmadı | DONE (canonical, APPLIED) |
| BATCH-REGRESSION-GROUND-TRUTH-RECONCILIATION-CLOSURE | infra (test coverage gap closure + documentation correction, NOT a canonical DB write) | Sonnet 5 | 2026-07-05 | **REGRESSION-GROUND-TRUTH RECONCILIATION CLOSURE — 794→795** (operatör talimatı: önceki batch'in 798→794 read-only reconciliation'ı istendi; 2 bulgu — (1) SYSTEM_STATE §89'un pre-batch cascade-geometry dosya sayısı yanlışlıkla "22" yazılmıştı, gerçek 21; düzeltildi; (2) yeni field-level `ami_birth_truncated_geometry_field_quality_v2.data_quality_status` CHECK constraint'i için raw-SQL test kapsamı eksikti). **Minimal kontrollü patch onaylandı — gerçek DB'ye 0 yazma:** `tests/test_ami_geometry_liquidation_source_quality_contract_v2.py`'ye **+1 test** (`test_schema_check_constraint_rejects_bad_data_quality_status`, UPDATE+INSERT ile `sqlite3.IntegrityError` kanıtlıyor). METHOD_B testleri geri getirilmedi, eski row-level mekanizma yeniden oluşturulmadı, sayı zorlamak için fazladan test eklenmedi. **Doğrulama:** collect-only=**795**; **2 bağımsız tam sequential regresyon: Run1=795/795 ✓, Run2=795/795 ✓, 0 hata.** Frozen komut değişmedi. canonical.sqlite sha256/mtime patch boyunca BİREBİR SABİT (`b28b4093…`). **798 baseline'ın kasıtlı supersede edildiği + 9 obsolete test kaldırıldığı + 5 replacement testin bu patch'ten önce var olduğu + 1 constraint testinin bu patch'te eklendiği + hiçbir korunan invariant'ın artık bilerek kapsamsız olmadığı** açıkça dokümante edildi. **Final verdicts: ground truth=795/795, `AMI_REGRESSION_795_READY_AND_GREEN`, research readiness ve migration verdict'leri DEĞİŞMEDİ.** | 795/795 ✓ (64 dosya, 2 bağımsız tam koşu + collect-only birebir) | `tests/test_ami_geometry_liquidation_source_quality_contract_v2.py` (+1 test) + `SYSTEM_STATE.md`(§89 düzeltme+§90 yeni)/`TEST_STATUS_LATEST.md`/`IMPLEMENTATION_PROGRESS_LEDGER.md` dokümantasyon güncellendi; **gerçek canonical.sqlite'a 0 yazma** (sha256/mtime patch-öncesi=patch-sonrası birebir doğrulandı); protected delta ZERO | DONE (test+doc only, no canonical write — WAIT_FOR_OPERATOR_APPROVAL) |
| BATCH-AMI-BIRTH-TRUNCATED-GEOMETRY-CANONICAL-MIGRATION | infra (controlled real canonical migration + immutable backfill) | Sonnet 5 | 2026-07-05 | **AMI BIRTH-TRUNCATED CASCADE GEOMETRY — CONTROLLED REAL CANONICAL MIGRATION + IMMUTABLE BACKFILL, GERÇEK `data/ami/canonical.sqlite`'A UYGULANDI** (operatör onayı: "APPROVE CONTROLLED REAL CANONICAL MIGRATION"). **Pre-migration blocking check bulgusu+düzeltmesi:** `BLOCKED_BY_QUALITY_STATE_DUPLICATION` riski (eski row-level `data_quality_status`/`coverage_assessment_version`/`ami_birth_truncated_geometry_quality_assessment` vs yeni field-level `ami_birth_truncated_geometry_field_quality_v2`) — seçenek 1 (remove) ile ÇÖZÜLDÜ, eskisi hiç migrate edilmedi; `birth_truncated_cascade_geometry.py` + `birth_truncated_geometry_rehearsal.py` (METHOD_B'nin reddedilen kendi gap-cutoff mantığı kaldırıldı, artık contract-v2'yi tek kaynak kullanıyor) yeniden yazıldı; yeni paylaşılan yardımcılar `liquidation_source_quality_contract_v2.fetch_quality_evidence()`/`assess_geometry_rows()`. Yeni `ami/geometry/birth_truncated_geometry_canonical_migration.py:run_canonical_migration()` (path_canonical_migration.py'nin 2-adımlı disiplini). **Şema:** `CANONICAL_SCHEMA_VERSION` 10→11, yeni `_SCHEMA_PHASE_GEOMETRY` (disposable DDL'yle byte-for-byte eşitliği programatik kanıtlandı). **Disposable rehearsal ile donmuş beklenen değerler** (migration'dan hemen önce, aynı kod yolu) → **gerçek migration TÜM content-hash'lerde BİREBİR eşleşti** (bağımsız 2. script'le tekrar teyit). **Ölçülen: field-level COMPLETE=745/GAPPED=6/UNRESOLVED=1009, row-level COMPLETE=87/GAPPED=6/UNRESOLVED=127** — operatörün expected/measured rakamlarıyla BİREBİR. **SOURCE_COMPLETE_ONLY: 87 sinyal/51 cycle/TRAIN=35/TEST=16 `INSUFFICIENT_SAMPLE`.** **15 maddelik bağımsız postflight:** 27/28 önceki tablo BİREBİR SABİT, yalnız `researcher_exposure_ledger` +1 (feature_gateway'in ÖNCEDEN-VAR by-design append-only exposure-audit'i, protected component DEĞİL); experiment_registry/results content-hash BİREBİR; FK/integrity_check temiz. **İdempotent rerun: NOOP_IDENTICAL.** **Regresyon:** 1 stale test bulundu+düzeltildi (`rollback_restores_pre_migration_fingerprint` artık migration kalıcı olduğu için branch-aware — emsal: `test_schema_fingerprint_changes_only_by_addition`); düzeltme sonrası **tam sequential 794/794 ✓ 0 hata** (2 bağımsız tam koşu + collect-only×3 birebir). Ground truth 798→**794** (test dosyası değişimlerinden net −4). **Backup-restore kanıtı:** disposable kopyada schema_version=10+tüm pre-migration sayılar+geometry-tablosu-YOK bağımsız doğrulandı, canlı DB'ye restore UYGULANMADI. **Final verdicts: `BIRTH_TRUNCATED_GEOMETRY_CANONICALIZED` / `AMI_REGRESSION_794_READY_AND_GREEN` / `GEOMETRY_INFERENTIAL_RESEARCH_BLOCKED_BY_SOURCE_QUALITY`.** | 794/794 ✓ (64 dosya, ≤2-dosya/çağrı sequential, 2 bağımsız tam koşu) | `ami/geometry/birth_truncated_cascade_geometry.py` (yeniden yazıldı, quality-kolonları kaldırıldı) + `birth_truncated_geometry_rehearsal.py` (yeniden yazıldı) + `liquidation_source_quality_contract_v2.py` (+2 fonksiyon) + yeni `birth_truncated_geometry_canonical_migration.py` + `ami/warehouse/schema.py` (`CANONICAL_SCHEMA_VERSION` 10→11, yeni `_SCHEMA_PHASE_GEOMETRY`) + yeni test dosyası + 3 test dosyası güncellendi; **GERÇEK `data/ami/canonical.sqlite`'A UYGULANDI** — backup×2 doğrulandı (pre sha256=`c2b0b300…`, post sha256=`b28b4093…`); protected delta ZERO (exposure-ledger append-only istisnası hariç) | DONE (canonical, APPLIED — WAIT_FOR_OPERATOR_APPROVAL for next step) |
| BATCH-LIQ-SOURCE-QUALITY-CONTRACT-V2-FROZEN | infra (source-quality contract implementation + test-scope freeze, NOT a research wave) | Sonnet 5 | 2026-07-05 | **LIQUIDATION SOURCE-QUALITY CONTRACT V2 — FROZEN, FIELD-LEVEL + CANONICAL TEST-SCOPE FREEZE** (operatör onayı: "APPROVE LIQUIDATION SOURCE-QUALITY CONTRACT V2 AND CANONICAL TEST-SCOPE FREEZE REHEARSAL" — §87'nin CONTRACT-V2 önerisi kabul, METHOD_A yalnız tarihsel kanıt, METHOD_B+cross-stream health REDDEDİLDİ. Gerçek migration/outcome research YOK). Yeni `ami/geometry/liquidation_source_quality_contract_v2.py`: `ALL_MARKET_TRANSITION_TS_MS=1780767832123` (ölçüldü, 40.14 günlük blackout sonrası ilk satır, 171 sembol doğrulaması), `CRITICAL_GAP_MS=300_000` (collector'ın frozen sabiti). **Field-level (operatörün açık tercihi):** 8 alanın HER BİRİ kendi required window'unu alıyor — 6 running-cluster alanı `[bucket_start,anchor_ts]`; `running_accel` kendi frozen iki-pencere tanımıyla bucket_start'tan bağımsız; `inter_cluster_gap_sec` `[önceki-anchor(veya en-eski-liq),anchor_ts]` — mevcut bucket'ın tamlığını asla miras almıyor. Row-level (gerektiğinde) = 8 alanın EN KÖTÜSÜ (GAPPED>UNRESOLVED>COMPLETE). Yeni append-only `ami_birth_truncated_geometry_field_quality_v2` tablosu+effective view+row-rollup view; farklı-içerik+aynı-version → `ImmutableFieldQualityConflict` fail-closed; geometry değerleri/manifest'leri hiç dokunulmuyor. **+15 yeni test** (`test_ami_geometry_liquidation_source_quality_contract_v2.py`) — operatörün 12 istediği kanıtın hepsi (pre-transition asla COMPLETE değil, absence-of-evidence≠completeness, ≤300s cadence→COMPLETE, >300s/resolved-gap→GAPPED, cross-stream statik guard, 8-alan-bağımsız-pencere, inter_cluster bucket-tamlığını miras alamıyor, deterministik rerun, append-only reassessment, geometry byte-identical, gerçek-veri TEST-cycle<MIN_BUCKET_N, no-outcome-read). **Gerçek-veri ölçüm** (`tools/research_s34_contract_v2_measurement.py`): 6 running-cluster alanı 94C/0G/126U; **`inter_cluster_gap_sec` sınırlayıcı** 87C/6G/127U (kendi ayrı penceresi 6 resolved-gap+1 ek unresolved yakalıyor). **Row-level worst-case: 87 COMPLETE/6 GAPPED/127 UNRESOLVED** (operatörün ~93/54/37-17 yaklaşık beklentisinden ÖLÇÜLEREK farklı, zorlanmadı). **SOURCE_COMPLETE_ONLY: 87 sinyal/87 event/51 cycle/TRAIN=35,TEST=16 — MIN_BUCKET_N=20 `INSUFFICIENT_SAMPLE`** (nitel sonuç değişmedi). **Test-scope freeze:** frozen komut=`pytest tests/test_ami_*.py tests/test_buyfade_mutations.py tests/test_buyfade_silexit_mutations.py` (64 dosya); collect-only×2 = **798/798 birebir**; tam sequential run (32 çift, ≤2-dosya/çağrı) = **798/798 ✓, 0 hata**. **GROUND TRUTH TERFİ: 751/751 → 798/798.** `tests/legacy_tools/`'daki 3 hata kapsam dışı kalmaya devam ediyor (ayrı dokümante edildi, AMI failure'ı DEĞİL). **Final verdicts: `LIQUIDATION_SOURCE_QUALITY_CONTRACT_V2_FROZEN` / `AMI_REGRESSION_798_READY_AND_GREEN` / `GEOMETRY_INFERENTIAL_RESEARCH_BLOCKED_BY_SOURCE_QUALITY`.** | +15 yeni test, **toplam AMI regresyon 798/798 ✓** (frozen komut, 64 dosya, ≤2-dosya/çağrı sequential, collect-only×2 birebir) | yeni `ami/geometry/liquidation_source_quality_contract_v2.py` + `tools/research_s34_contract_v2_measurement.py` + `reports/research/s34/S34_LIQUIDATION_SOURCE_QUALITY_CONTRACT_V2_MEASUREMENT_2026-07-05.md/.json` + 1 yeni test dosyası; **canonical.sqlite sha256/mtime BİREBİR SABİT** tüm batch boyunca; microstructure.db mode=ro; migration/experiment write/outcome read YOK; protected delta ZERO | DONE (contract frozen + test ground truth promoted — WAIT_FOR_OPERATOR_APPROVAL for next step) |
| BATCH-LIQ-SOURCE-QUALITY-RECONCILIATION | infra (source-quality contract reconciliation, READ-ONLY, NOT a research wave) | Fable 5 | 2026-07-05 | **LIQUIDATION SOURCE-QUALITY COVERAGE CONTRACT RECONCILIATION — READ-ONLY** (operatör talimatı: §86'nın 83/1/136 vs 125/1/94 uyuşmazlığı migration/deney öncesi çözülmeli; provisional verdict `BLOCKED_BY_SOURCE_QUALITY_CONTRACT`, `GEOMETRY_INFERENTIAL_RESEARCH_READY` KABUL EDİLMEDİ). **GOAL A:** iki yöntem de BİREBİR yeniden üretildi — METHOD_A (cutoff=İLK open-ended liq-gap start 2026-04-02 17:58) → 83/1/136; METHOD_B (cutoff=SON liq-gap-satır start 2026-04-27 14:27) → 125/1/94; uyuşmazlık = tam 42 sinyal (hepsi Nisan, 28 cycle, hepsi A=UNRESOLVED/B=COMPLETE); tek GAPPED sinyal iki yöntemde aynı. İkisi de absence-of-evidence temelli. **GOAL B:** silinen gap-writer semantiği git stash `07e1a1f9`'dan kurtarıldı — staleness heartbeat (>120s), resolution state IN-MEMORY (restart açık satırları yetim bırakır), güncel collector'da gap kodu YOK. Registry iki yönde de güvenilmez KANITLANDI: 21 open satırın ampirik sessizliği ~2s (yanlış alarm) AMA **liquidations tablosunda 40.1 günlük blackout (Apr 27→Jun 6, Mayıs=0 satır)** + saatlik Nisan delikleri kaçırılmış. **GOAL C:** cross-stream health GEÇERSİZ (Mayıs'ta aynı websocket 15.27M agg_trades taşırken liq=0 → stream-only sessiz arıza kanıtı); stream-mode keşfi — Şub-Nis=per-symbol (2-3 sembol), Jun6+=all-market 733 sembol → per-symbol dönemde pozitif tamlık kanıtı YAPISAL imkansız, all-market dönemde pencere-bazlı cadence doğrulaması mümkün. **GOAL D:** önerilen `liq-source-quality-contract-v2` (frozen sabitlerle: 300s critical + 1800s MIN_GAP_SEC; fit YOK): resolved-gap→GAPPED, birth<Jun6→UNRESOLVED, all-market cadence≤300s→COMPLETE. **GOAL E:** V2 → 93 COMPLETE (Haz90/Tem3) / 54 cycle (37/17) **MIN_BUCKET_N FAIL**; METHOD_A da FAIL (59 cyc, 41/18); geçen adaylar (B, cross-stream) geçersiz temelli. **Research-readiness: `BLOCKED_BY_SOURCE_QUALITY_CONTRACT`.** **GOAL F:** 751/751 = `tests/test_ami_*.py + test_buyfade_mutations.py + test_buyfade_silexit_mutations.py` (61 dosya) BİREBİR yeniden üretildi (bugün aynı küme 783=751+32); 2611 = tam `tests/` kapsamı (hiç ground truth olmadı); 3 hata = legacy_tools'a hapsedilmiş önceden-var collection error; önerilen frozen komut → beklenen 783 (bir tam yeşil koşu sonrası kabul; o zamana dek 751 kalır). **GOAL G:** SYSTEM_STATE §86 düzeltildi (READY geri çekildi → PROVISIONAL), §87 eklendi. | Yeni test YOK (read-only audit); mevcut ground truth 751/751 DEĞİŞMEDİ | `tools/research_s34_source_quality_reconciliation.py` + `reports/research/s34/S34_LIQUIDATION_SOURCE_QUALITY_RECONCILIATION_2026-07-05.md/.json`; **canonical.sqlite sha256 önce/sonra BİREBİR** (`c2b0b300…3098f`); microstructure.db mode=ro; outcome read YOK; migration/experiment write YOK; protected delta ZERO | DONE (read-only reconciliation — WAIT_FOR_OPERATOR_APPROVAL on contract freeze) |
| BATCH-AMI-BIRTH-TRUNCATED-CASCADE-GEOMETRY-REHEARSAL | infra (disposable schema+backfill rehearsal, NOT a research wave) | Sonnet 5 | 2026-07-05 | **AMI BIRTH-TRUNCATED CASCADE GEOMETRY — disposable schema migration + canonical backfill rehearsal ONLY** (operatör onayı: "APPROVE AMI BIRTH-TRUNCATED CASCADE GEOMETRY DISPOSABLE SCHEMA MIGRATION + CANONICAL BACKFILL REHEARSAL ONLY"; önceki oturum kesintiye uğramıştı, bu batch onu bitirdi). Goals A-D (`ami/geometry/birth_truncated_cascade_geometry.py` — schema/immutability/append-only quality-ledger/field-provenance) önceki oturumdan diskte kalmış ama hiç pytest ile doğrulanmamıştı — ilk çalıştırmada test dosyasında 2 gerçek bug bulundu+düzeltildi (yanlış-ölçekli `_liq` zaman damgası ×8, append-only assessed_at_ms sıralama hatası), kod DEĞİŞMEDİ. Goals E-I için yeni `ami/geometry/birth_truncated_geometry_rehearsal.py`: gerçek `data/ami/canonical.sqlite`'ın disposable kopyasına + gerçek `data/microstructure.db`'ye (mode=ro) karşı tam rehearsal. **Population ölçüldü:** 220 LONG sinyal/218 event/142 cycle/split 99-43 — operatörün confirmed-findings'iyle BİREBİR. **Backfill:** 220/220 reconstructable (0 red). **Data-quality partition bu oturumda yeniden inşa edildi** (cutoff=gap-registry'nin en son start_ts_ms'i [2026-04-27 14:27], gapped=yalnız RESOLVED gap overlap — `ami.chart.candle_builder` emsaliyle tutarlı): ölçülen SOURCE_COMPLETE=125/GAPPED=1/UNRESOLVED=94 — GAPPED=1 operatörle BİREBİR eşleşti, COMPLETE/UNRESOLVED (125/94) operatörün aktardığı 83/136 ile EŞLEŞMEDİ (orijinal cutoff algoritması kod olarak kalıcı değildi) → **açık madde, operatör onayı gerekiyor**. Migration-safety (Goal H) hepsi YEŞİL: idempotent rerun, conflicting-content fail-closed (`ImmutableGeometryConflict`), old-reader compatibility, rollback→pre-migration fingerprint birebir, reapply→content-hash birebir. Semantic collision guard (Goal F): statik testler `ami_events` mutasyonu YOK + event_count/event_end_ts_ms/notional karışmıyor kilidini kanıtlıyor. **Migration-readiness verdict: `MIGRATION_READY_WITH_INFERENTIAL_SOURCE_QUALITY_BLOCKER`.** **Research-readiness verdict (ölçülen cutoff ile): `GEOMETRY_INFERENTIAL_RESEARCH_READY`** (SOURCE_COMPLETE_ONLY: 87 cycle, 60/27 split, MIN_BUCKET_N geçti) — operatör orijinal cutoff'u onaylamadan güvenilmemeli. **0 blocker (kod); 1 açık madde (cutoff tanımı onayı).** | +32 yeni test (21 Goals-A-D + 11 Goals-E-I, 2 yeni dosya) — ikisi birlikte **32/32 ✓** (`--basetemp` scratchpad + `-p no:cacheprovider`, sequential); tam `tests/` `--collect-only` ile toplandı (2611 test, `tests/legacy_tools/`'daki 3 önceden-var/ilgisiz basename-çakışması hatası HARİÇ), import/collection hatası YOK | yeni `ami/geometry/birth_truncated_geometry_rehearsal.py` + 2 yeni test dosyası (`test_ami_geometry_birth_truncated_cascade_geometry.py` düzeltildi, `test_ami_geometry_birth_truncated_geometry_rehearsal.py` yeni); **gerçek canonical.sqlite'a 0 write** (hash/mtime testte DEĞİŞMEDİ); **gerçek microstructure.db'ye 0 write** (mode=ro + statik guard + monotonic-size/count invariant); protected session delta ZERO | DONE (disposable rehearsal only, NOT applied to real DB — WAIT_FOR_OPERATOR_APPROVAL on cutoff definition) |
