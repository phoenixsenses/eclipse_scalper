{
  "purpose": "suppression_and_persistence_aware_research_event_watchboard",
  "required": [
    "summary.raw_top_lane",
    "summary.effective_top_lane",
    "summary.hidden_lane_count",
    "summary.degraded_lane_count",
    "summary.collapsed_lane_count",
    "summary.noisy_lane_count",
    "summary.primary_noisy_lane",
    "top_event.lane",
    "top_event.level",
    "top_event.recommended_action",
    "top_event.persistence_recommendation",
    "top_event.recommended_min_persist_snapshots",
    "top_event.recommended_cooldown_snapshots",
    "lanes"
  ],
  "lane_required": [
    "lane",
    "level",
    "freshness_status",
    "recommended_action",
    "headline",
    "detail",
    "priority_score",
    "effective_display_mode",
    "effective_priority_score",
    "persistence_recommendation",
    "recommended_min_persist_snapshots",
    "recommended_cooldown_snapshots",
    "is_noisy"
  ],
  "runtime_rules": [
    "Use effective_top_lane rather than raw_top_lane for primary UI emphasis.",
    "Render degraded lanes in a lower-emphasis style instead of hiding them entirely.",
    "Use persistence fields to avoid fast banner/card flipping when the top lane is noisy.",
    "Do not auto-wire the effective watchboard into execution logic."
  ]
}
