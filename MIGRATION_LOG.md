# MIGRATION_LOG

Yalnız schema/migration değişikliğinde güncellenir. Irreversible/destructive migration = operatör onayı.

| ID | Tarih | DB | Değişiklik | Reversible? | Statü |
|---|---|---|---|---|---|
| M-0035 | 2026-07-07 | **data/ami/canonical.sqlite (GERÇEK)** | BATCH-CASCADE-ABSORPTION-IMPACT-CANONICAL-MIGRATION-V1 (operatör onayı, üç önceki batch'in checkpoint'ine dayanır: readiness/contract `fc1321f5`, disposable rehearsal `fc43e972`, row-accounting freeze `931cd3dd`). **Naming ruling (operatör):** kontratın illustrative `ami_impact_*` adlandırması ve rehearsal'ın disposable `absorption_impact_*` adlandırması, canonical üretim adı olarak `ami_absorption_impact_*` ile normalize edildi (formula/units/window/row-identity/quality-state/exclusion-identity/FLOOR_USD_M/feature-value DEĞİŞMEDİ, yalnız isimlendirme). **Şema:** `ami/warehouse/schema.py`: `CANONICAL_SCHEMA_VERSION` 12→13, yeni `_SCHEMA_PHASE_ABSORPTION_IMPACT` bloğu (`ami_absorption_impact_windowed_flow`+`window_quality_v1`+`exclusions` — rehearsal'ın (`fc43e972`) `_SCHEMA`'sıyla byte-for-byte aynı + CVD emsaliyle uyumlu FK satırları [signal_id/source_event_id/independent_cycle_id] + `window_id` enum CHECK, `init_schema()`'ya kayıtlı, fresh in-memory DB'de doğrulandı: 3/3 tablo, integrity_check=ok, fk_check=[]). Effective-view EKLENMEDİ (bu ailenin quality tablosunda CVD'nin `assessment_version` boyutu yok — tek quality satırı/signal/window/contract-version, "latest wins" senaryosu yok; kontratın illustrative CVD-paralel açıklamasıyla bu tek fark, A5 kararı olarak açıkça işaretlendi). Yeni `ami/absorption/cascade_absorption_impact_canonical_migration.py:run_canonical_migration()` — donmuş retained rehearsal paketinden (`.runtime_temp/absorption_impact_rehearsal_v1/rehearsal_run1.sqlite`) **3 tabloyu birebir kopyalıyor** (0 network call, 0 yeniden hesaplama, content-compare idempotent, aynı-kimlik+farklı-içerik→`FrozenSourceRowConflict`). **Disposable testler ÖNCE** (`tests/test_ami_absorption_impact_canonical_migration.py`, +7, gerçek DB'nin disposable kopyasına karşı, dahil donmuş content-hash parity testi): hepsi yeşil. **Preflight:** tüm 23 kaynak/kod/kontrat hash'i (fc1321f5/fc43e972/931cd3dd'nin ürettiği tüm dosyalar + retained rehearsal paketi + 7 tablo şeması) bağımsız yeniden hesaplandı, birebir eşleşti (0 drift); schema_version=12/experiment_registry=23/experiment_results=350/researcher_exposure_ledger=1176 doğrulandı; backup alındı (`canonical_pre_M0035_absorption_impact_canonical_migration_20260707_065549.sqlite`, sha256 kaynakla birebir), disposable restore-proof (schema_version=12, 3 yeni tablo YOK, tüm protected sayaçlar birebir, integrity_check=ok) — canlı DB'ye dokunmadan ÖNCE; ayrıca disposable kopyada tam production-shaped dry-run (SQLite authorizer'lı) çalıştırıldı. **Migration sonucu (gerçek DB'ye UYGULANDI, SQLite authorizer'la outcome/experiment/nullifier erişimi=0 kanıtlanmış):** schema_version=13; 1,619 usable feature + 1,620 quality + 1 exclusion satırı (0 conflict). **3 content hash [windowed_flow/quality/exclusions] donmuş rehearsal değerleriyle BİREBİR eşleşti** (`f7c834cc…`/`5d1a205c…`/`5e3ae2e5…`). Tam satır-seviyesi karşılaştırma (bookkeeping timestamp hariç): 0 eksik/0 ekstra/0 value-drift. **Postflight:** events=252/signals=324/cycles=167/geometry=220/experiment_registry=23/experiment_results=350 DEĞİŞMEDİ; `foreign_key_check`=[] temiz, `integrity_check`=ok; knowledge.sqlite bu batch'te HİÇ açılmadı (mtime doğrulandı, nullifier=1/gate_receipt=1 sabit). **İdempotent rerun:** ikinci kez uygulandı, 0 yeni satır, hepsi noop_identical, 3 content hash birebir (yalnız `schema_versions.applied_ms` bookkeeping timestamp'i değişti — beklenen, veri değil). **Full regresyon:** 987 test/76 dosya, ≤2-dosya/çağrı paired-subprocess; ilk deneme kendi hatamla kısa süre paralel process'e denk geldi (guardrail ihlali, tespit edilip temizlendi) → temiz tek-koşuda 973/987 yeşil, 14 hata **TAMAMI bu aileyle ilgisiz kanıtlandı**: 13'ü G2-CVD-PRIMARY-LONG-GOVERNED-EXECUTION-V1 batch'inin (bu session'dan önce, `60c3e26f`) nullifier tüketimi/experiment_registry 22→23/receipt EXECUTED değişikliklerinden (bu batch knowledge.sqlite'ı hiç açmadığı için yapısal olarak kanıtlı + pre-migration backup'ta zaten mevcut), 1'i (`test_verify_pre_execution_reports_zero_errors_against_real_db`) aynı köke bağlı `is_rerun_of_self` kontrolü. `test_ami_lifecycle_provenance_rehearsal.py`'nin `schema_version_before` tuple'ı `(8,9,10,11,12,13)`'e genişletildi (v13 yalnız ADDED tablo, aynı emsal zinciri) — düzeltme sonrası izole+tam-suite'te yeşil. 1 gözlem (`short_noisy_v1_rehearsal` microstructure.db prefix-hash) + 1 geometri dosyası (`FileNotFoundError`, WinError 3) kirli/paralel koşuda görüldü, temiz tek-process koşuda İKİSİ DE yeşil — canlı collector timing + filesystem flake, kod değişmedi. **Operatör kararı:** 13 ilgisiz hata açıkça belgelenerek COMPLETE kabul edildi (ayrı remediation batch'i gerektirir, bu batch'in kapsamı değil). **Backup-restore:** disposable kopyada schema_version=12+pre-migration sayılar+3 yeni tablo YOK bağımsız doğrulandı, canlı migrasyonlu DB'ye ASLA restore uygulanmadı. **Verdicts: `CASCADE_ABSORPTION_IMPACT_CANONICAL_MIGRATION_V1_COMPLETE` / `ABSORPTION_IMPACT_CANONICAL_DATA_READY_FOR_PREREGISTRATION`.** Protected delta = ZERO. | Evet (backup doğrulandı: pre-migration `canonical_pre_M0035_absorption_impact_canonical_migration_20260707_065549.sqlite` sha256=`25a56a98…` kaynakla birebir; additive-only migration [yeni tablolar], backup-restore disposable kopyada BAĞIMSIZ doğrulandı) | **APPLIED_TO_CANONICAL_DB** |
| M-0034 | 2026-07-06 | **data/ami/knowledge.sqlite (GERÇEK, canonical.sqlite DEĞİL)** | BATCH-EPISTEMIC-NULLIFIER-LEGACY-BYPASS-CLOSURE-V1 evidence-reconciliation kapanışı: 1 additive tablo (`experiment_gate_receipts` — `ami.warehouse.experiment_ledger.register_experiment_with_gates`/`register_legacy_snapshot_with_gates`'in yazdığı, `ami.research.registry.ResearchRegistry.register_experiment`'in okuduğu M-0034 receipt defteri). Gerçek dosyaya **kontrollü, byte-exact backup'lı deployment** ile uygulandı (`data/ami/backups/knowledge_pre_M0033_M0034_real_deployment_20260706_163058.sqlite`, sha256 `6c8905f7…` — bu batch öncesi son durumla birebir). Disposable rehearsal (aynı DDL, ayrı kopya) + gerçek dosyada 2× idempotent apply (0 diff) + gerçek üretim-yolu doğrulaması: `ami.knowledge.store.DEFAULT_PATH` çözümü (test-isolation KAPALI) gerçek dosyaya işaret ettiği, `issue_gate_receipt`/`has_gate_receipt`'in bu dosyaya yazıp okuduğu bir dry-run fixture (`DRY-RUN-VERIFICATION-EPISTEMIC-GATES-ONLY-2026-07-06`, bilimsel olmayan) ile kanıtlandı, ardından o satır silindi (net veri değişikliği YOK — şema kalıcı, veri sıfır). Mevcut `graveyard_slash_fingerprints` (31) + `epistemic_test_nullifiers` (0) + diğer tüm tablolar DEĞİŞMEDİ. canonical.sqlite bu batch'te hiç açılmadı (sha256 `458bc07c…` / schema_version 12 sabit, doğrulandı). | Evet (`DROP TABLE experiment_gate_receipts`; mevcut veriye dokunulmadı) | **APPLIED_TO_KNOWLEDGE_DB** |
| M-0033 | 2026-07-06 | **data/ami/knowledge.sqlite (GERÇEK, canonical.sqlite DEĞİL)** | BATCH-EPISTEMIC-NULLIFIER-ENFORCEMENT-WIRING-V1: 1 additive tablo (`epistemic_authorization_tokens` — retry/supersession yetkilendirme kayıtları) + 1 partial UNIQUE index (`uq_test_nullifiers_first_consumption` ON `epistemic_test_nullifiers(nullifier)` WHERE `supersession_token IS NULL` — eşzamanlı ilk-tüketim yarışını DB seviyesinde engeller). O batch sırasında yalnız disposable kopyalara uygulanmıştı (kayıt eksikliği — bu satır BATCH-EPISTEMIC-NULLIFIER-LEGACY-BYPASS-CLOSURE-V1'in evidence-reconciliation kapanışında geriye dönük eklendi); gerçek dosyaya fiili uygulama M-0034 ile AYNI kontrollü deployment adımında (2026-07-06) yapıldı — ayrıntılar için M-0034 satırına bakınız. Mevcut `epistemic_test_nullifiers` (0 satır) DEĞİŞMEDİ. | Evet (`DROP TABLE epistemic_authorization_tokens; DROP INDEX uq_test_nullifiers_first_consumption`; mevcut veriye dokunulmadı) | **APPLIED_TO_KNOWLEDGE_DB** |
| M-0032 | 2026-07-06 | **data/ami/knowledge.sqlite (GERÇEK, canonical.sqlite DEĞİL)** | BATCH-EPISTEMIC-NULLIFIER-GATES-V1: 2 additive tablo (`graveyard_slash_fingerprints` [31 curated satır seed'lendi, idempotent doğrulandı] + `epistemic_test_nullifiers` [append-only, 0 satır — henüz tüketim yok]) — `ami/governance/epistemic_gates.py:init_gates_schema()` (CREATE TABLE IF NOT EXISTS only). Mevcut failure_archive/audit_log/knowledge tabloları DEĞİŞMEDİ. Enforcement wiring yapılmadı (ayrı onay, SYSTEM_STATE §94). canonical.sqlite bu batch'te hiç yazılmadı (sha256 `458bc07c…` sabit). | Evet (2 tabloyu DROP etmek yeterli; mevcut veriye dokunulmadı) | **APPLIED_TO_KNOWLEDGE_DB** |
| M-0031 | 2026-07-06 | **data/ami/canonical.sqlite (GERÇEK)** | BATCH-CVD-REPAIR-REHEARSAL-AND-QUALITY-CONTRACT-V1, migration adımı (operatör onayı: "devam edelim" → "Gerçek schema 11→12 migrasyonunu çalıştır"; önceki rehearsal batch'in [M kaydı yok, disposable-only] `CVD_REPAIR_REHEARSAL_READY_FOR_CANONICAL_MIGRATION_PROPOSAL` verdict'ine + ayrı read-only feature-matrix accounting reconciliation'ın `CVD_MIGRATION_ROW_ACCOUNTING_FROZEN` verdict'ine dayanır). **Şema:** `ami/warehouse/schema.py`: `CANONICAL_SCHEMA_VERSION` 11→12, yeni `_SCHEMA_PHASE_CVD` bloğu (`ami_agg_trades_repaired` [rehearsal'ın `..._stage`'inden rename] + `ami_cvd_repair_batch_ledger` + `ami_cvd_windowed_flow`+`_proxy` + `ami_cvd_bucket_exclusions` + `ami_cvd_window_quality_v1` + 2 effective-view — rehearsal'ın 3 modülünün `_SCHEMA`'sıyla **byte-for-byte aynı + önerinin izin verdiği 3 FK satırı**, in-memory DB'de bağımsız uygulama kanıtı [6/6 tablo+2/2 view, FK zinciri çözülüyor, idempotent rerun temiz], `init_schema()`'ya kayıtlı). Yeni `ami/cvd/cvd_canonical_migration.py:run_canonical_migration()` — donmuş disposable rehearsal DB'sinden (`data/ami/cvd_rehearsal_disposable_20260705/cvd_rehearsal_disposable.sqlite`) **6 tabloyu birebir kopyalıyor** (0 network call, 0 yeniden hesaplama, content-compare idempotent, aynı-kimlik+farklı-içerik→`FrozenSourceRowConflict`). **Disposable testler ÖNCE** (`tests/test_ami_cvd_canonical_migration.py`, +5, gerçek DB'nin disposable kopyasına karşı): rehearsal sayılarını birebir üretiyor, idempotent, conflict-raise, protected-invariant-unchanged — hepsi yeşil. **Preflight:** schema_version=11 doğrulandı, backup alındı (`canonical_pre_cvd_repair_canonical_migration_20260706_065631.sqlite`, sha256 kaynakla birebir), disposable restore-proof (schema_version=11, 6 yeni tablo YOK, events=252/signals=324/cycles=167/geometry=220, integrity_check=ok) — canlı DB'ye dokunmadan ÖNCE. **Migration sonucu (gerçek DB'ye UYGULANDI):** schema_version=12; 40,934 repaired-trade + 8 batch-ledger + 1,840 exact + 1,840 proxy + 104 bucket-exclusion + 1,840 quality satırı, 0 conflict. **3 content hash [exact/proxy/quality] donmuş rehearsal değerleriyle BİREBİR eşleşti** (`ca11be78…`/`0a8ac304…`/`6e95d51a…`). **Postflight:** events=252/signals=324/cycles=167/geometry=220 DEĞİŞMEDİ; **tam tablo sayımı — 33 önceki tablonun HEPSİ birebir sabit** (protected delta ZERO, geometry migration'ın exposure-ledger istisnasının aksine burada HİÇ istisna yok), `foreign_key_check`=[] temiz, `integrity_check`=ok. **İdempotent rerun:** ikinci kez uygulandı, 0 yeni satır, hepsi noop_identical, hash'ler birebir. **Full regresyon:** ilk koşu yanlışlıkla tek-proses mega-invocation ile yapıldı (kendi guardrail ihlali) → 14 hata; doğru ≤2-dosya/çağrı paired-subprocess prosedürüyle yeniden koşulunca 13/14 hata SADECE bu ihlalin çapraz-dosya kontaminasyonu olduğu kanıtlandı (CVD'yle ilgisiz); kalan 2 gerçek bulgu düzeltildi — kendi yeni testim branch-aware hale getirildi (insert vs all-noop, geometry-migration emsali) + `test_ami_lifecycle_provenance_rehearsal.py`'nin `schema_version_before` tuple'ı `(8,9,10,11,12)`'ye genişletildi (v12 yalnız ADDED tablo, aynı emsal zinciri); 1 gözlem (`short_noisy_v1_rehearsal` microstructure.db prefix-hash) izole rerun'da temiz — canlı collector timing flake'i, kod değişmedi. **Düzeltmeler sonrası tam sequential regresyon 852/852 ✓ 0 hata** (collect-only birebir 852). **Backup-restore:** disposable kopyada schema_version=11+pre-migration sayılar+6 yeni tablo YOK bağımsız doğrulandı, canlı migrasyonlu DB'ye ASLA restore uygulanmadı. Post-migration backup alındı (`canonical_post_cvd_repair_canonical_migration_v12_20260706_070000.sqlite`). **Verdicts: `CVD_REPAIR_REHEARSAL_CANONICAL_MIGRATION_APPLIED` / `AMI_REGRESSION_852_READY_AND_GREEN`.** Protected delta = ZERO. | Evet (backup×2 doğrulandı: pre-migration `canonical_pre_cvd_repair_canonical_migration_20260706_065631.sqlite` sha256=`b28b4093...`, post-migration `canonical_post_cvd_repair_canonical_migration_v12_20260706_070000.sqlite` sha256=`458bc07c...`; additive-only migration [yeni tablolar], backup-restore disposable kopyada BAĞIMSIZ doğrulandı) | **APPLIED_TO_CANONICAL_DB** |
| M-0030 | 2026-07-05 | **data/ami/canonical.sqlite (GERÇEK)** | BATCH-AMI-BIRTH-TRUNCATED-GEOMETRY-CANONICAL-MIGRATION (operatör onayı: "APPROVE CONTROLLED REAL CANONICAL MIGRATION — AMI BIRTH-TRUNCATED CASCADE GEOMETRY CANONICAL MIGRATION + IMMUTABLE BACKFILL"; önceki 2 batch'in [rehearsal + source-quality-contract-v2] disposable doğrulaması + operatör tarafından kabul edilen checkpoint'e dayanır). **Pre-migration blocking checks:** (1) repo audit — tüm ilgili dosyalar tespit edildi; (2) **BLOCKED_BY_QUALITY_STATE_DUPLICATION riski bulundu ve ÇÖZÜLDÜ** — `ami_birth_truncated_cascade_geometry`'nin eski (Goals A-D döneminden kalma) `data_quality_status`/`coverage_assessment_version` kolonları + row-level `ami_birth_truncated_geometry_quality_assessment` tablosu/effective-view'ı, yeni field-level `ami_birth_truncated_geometry_field_quality_v2` ledger'ıyla çakışan ikinci bir quality-state mekanizması olduğu için TAMAMEN KALDIRILDI (seçenek 1: remove, gerçek DB'ye hiç migrate edilmedi) — `ami/geometry/birth_truncated_cascade_geometry.py` yeniden yazıldı (schema+backfill+content_hash+row_counts+rollback), `ami/geometry/birth_truncated_geometry_rehearsal.py` METHOD_B'nin (reddedilen) kendi gap-registry-cutoff mantığını tamamen kaldırıp `liquidation_source_quality_contract_v2`'yi tek kaynak olarak kullanacak şekilde yeniden yazıldı; (3) disposable rehearsal tam ve kontratlarla birebir doğrulandı. **Şema:** `ami/warehouse/schema.py`: `CANONICAL_SCHEMA_VERSION` 10→11, yeni `_SCHEMA_PHASE_GEOMETRY` bloğu (`ami_birth_truncated_cascade_geometry`+`ami_birth_truncated_geometry_field_provenance`+`ami_birth_truncated_geometry_field_quality_v2`+2 view — disposable-doğrulanmış DDL'yle **byte-for-byte aynı olduğu programatik olarak kanıtlandı** [ayrı in-memory DB karşılaştırması, 5/5 obje eşleşti], `init_schema()`'ya kayıtlı). Yeni `ami/geometry/birth_truncated_geometry_canonical_migration.py:run_canonical_migration()` (schema-sonrası backfill: `geo.backfill()` + `v2.assess_geometry_rows()`/`backfill_field_quality()`, path_canonical_migration.py'nin aynı 2-adımlı disiplini). **Disposable rehearsal ile donmuş beklenen değerler** (gerçek migration'dan HEMEN önce, aynı kod yoluyla): geometry=220/1760 field_quality/1760 provenance, content_hash'ler kaydedildi. **Preflight:** schema_version=10/events=252/signals=324/LONG=220/SHORT=104/experiment_registry=22 doğrulandı (operatörün beklenen checkpoint'iyle birebir), backup alındı (`canonical_pre_birth_truncated_geometry_canonical_migration_20260705_141345.sqlite`, sha256=`c2b0b300...`, bağımsız olarak mode=ro açılıp integrity_check=ok doğrulandı). **Migration + backfill sonucu (gerçek DB'ye UYGULANDI):** schema_version=11; 220/220 sinyal kabul edildi (0 red); 1760 field-quality satırı (220×8, 0 conflict); **TÜM content hash'ler [geometry/manifest-set/field-quality/provenance] donmuş rehearsal değerleriyle BİREBİR eşleşti** (bağımsız 2. doğrulama script'iyle de tekrar teyit edildi). **Ölçülen quality dağılımı (field-level, 1760 satır): SOURCE_COMPLETE=745/GAPPED=6/UNRESOLVED=1009** — operatörün "expected aggregate" rakamlarıyla BİREBİR eşleşti. **Row-level worst-case: COMPLETE=87/GAPPED=6/UNRESOLVED=127** — operatörün "current measured quality" rakamlarıyla BİREBİR eşleşti. **SOURCE_COMPLETE_ONLY: 87 sinyal/51 cycle/TRAIN=35/TEST=16 — MIN_BUCKET_N=20 `INSUFFICIENT_SAMPLE`** (operatörün beklediğiyle uyumlu). **Postflight (15 maddelik bağımsız doğrulama, ayrı script):** schema_version=11 ✓, 0 duplicate-pair ✓, 0 future-timestamp-violation ✓, 0 feature_available_ts-mismatch ✓, tüm hash'ler eşleşti ✓, field-provenance 1760/1760 tam ✓, **28 önceki canonical tablo sayısı tek tek karşılaştırıldı — 27/28 BİREBİR SABİT, yalnız `researcher_exposure_ledger` +1 arttı** (feature_gateway'in KENDİ, önceden-var, by-design append-only exposure-audit mekanizması — `fetch_lifecycle_signals()`'in her çağrısında beklenen bir satır, protected component DEĞİL, `PROTECTED_COMPONENTS_MANIFEST.md`'de yok, davranış zaten mevcut kodda belgeli), experiment_registry/experiment_results content-hash'i (yalnız count değil) BİREBİR aynı kanıtlandı ✓, foreign_key_check=0 ✓, integrity_check=ok ✓. **İdempotent rerun:** gerçek DB'ye ikinci kez uygulandı — 0 yeni satır, content hash'ler BİREBİR (yalnız exposure-ledger +1 daha, aynı sebep, expected). **Full regresyon:** ilk koşuda `test_ami_geometry_birth_truncated_geometry_rehearsal.py`'nin `rollback_restores_pre_migration_fingerprint` assertion'ı gerçek migration artık kalıcı olduğu için stale çıktı (emsal: `test_schema_fingerprint_changes_only_by_addition`) — aynı disiplinle branch-aware hale getirildi (yeni `source_already_has_geometry_tables` flag'i), düzeltme sonrası **tam sequential regresyon 794/794 ✓ 0 hata** (64 dosya, ≤2-dosya/çağrı, iki bağımsız tam koşu + collect-only×2 birebir 794). **Backup-restore kanıtı:** pre-migration backup hash'i kayıtlı orijinalle birebir; disposable bir kopyaya restore edilip schema_version=10 + tüm pre-migration sayıları (220 LONG/104 SHORT/22 registry/1466 path-obs) + geometry tablosunun YOK olduğu (pre-migration state) bağımsız doğrulandı; canlı migrasyonlu DB'ye ASLA restore uygulanmadı. Post-migration backup da alındı (`canonical_post_birth_truncated_geometry_canonical_migration_v11_20260705_144500.sqlite`). **Verdicts: `BIRTH_TRUNCATED_GEOMETRY_CANONICALIZED` / `AMI_REGRESSION_794_READY_AND_GREEN` / `GEOMETRY_INFERENTIAL_RESEARCH_BLOCKED_BY_SOURCE_QUALITY`** (research hâlâ engelli, MIN_BUCKET_N geçilmedi — outcome deneyi YAPILMADI). Protected delta = ZERO (exposure-ledger append-only audit istisnası hariç, protected component değil). | Evet (backup×2 doğrulandı: pre-migration `canonical_pre_birth_truncated_geometry_canonical_migration_20260705_141345.sqlite` sha256=`c2b0b300...`, post-migration `canonical_post_birth_truncated_geometry_canonical_migration_v11_20260705_144500.sqlite` sha256=`b28b4093...`; additive-only migration [yeni tablolar], backup-restore disposable kopyada BAĞIMSIZ doğrulandı) | **APPLIED_TO_CANONICAL_DB** |
| M-0029 | 2026-07-04 | **data/ami/canonical.sqlite (GERÇEK)** | BATCH-P7B-CANON (operatör onayı: "APPROVE PHASE 7B CANONICAL PATH METRICS MIGRATION", disposable doğrulama BATCH-P7B-0/P7B-0.1'de tamamlanmıştı): path-v2 paketi gerçek DB'ye uygulandı. `ami/warehouse/schema.py`: `CANONICAL_SCHEMA_VERSION` 9→10, yeni `_SCHEMA_PHASE7B` bloğu (`ami_lifecycle_path_observations`, disposable-doğrulanmış DDL'yle byte-for-byte aynı — fingerprint `3a26ffa86ecec9d8b63eff9455e3cfbbd594cc59eb36896feeba4d3bf232f1e7`, `init_schema()`'ya kayıtlı, fresh in-memory DB'de doğrulandı). Yeni `ami/lifecycle/path_canonical_migration.py:run_canonical_migration()` (schema-sonrası backfill: `path_metrics.freeze_and_record()` + `path_field_provenance.backfill_path_field_provenance()`). `ami/research/feature_gateway.py`: `ami_signal_lifecycle`/`ami_lifecycle_effective_transitions`/`ami_lifecycle_path_observations` allowlist'e eklendi, 3 yeni dedike fetch fonksiyonu (`fetch_lifecycle_signals`/`fetch_lifecycle_effective_transitions`/`fetch_path_observations`), exposure-logging korundu. **Preflight:** schema_version=9/signal=270/raw=802/effective=270/provenance=4320/FORWARD_N=0/binding=2 doğrulandı (operatörün beklenen v9 checkpoint'iyle birebir), aktif yazıcı yok (`BEGIN IMMEDIATE` başarılı), backup alındı (`canonical_pre_phase7b_path_metrics_migration_20260704_180134.sqlite`, sha256=`3d34f44e...`, fingerprint=`ef8eeebb...`, kaynakla birebir doğrulandı). **Backfill sonucu:** 1080 path satırı (OK=914/MISSING_INTERNAL_GAP=153/EXCLUDED_NO_HORIZON_DATA=13/MISSING_REFERENCE_PRICE=0/NOT_COMPUTABLE_DIRECTION=0), volatility_status (OK=912/NOT_APPLICABLE=166/INVALID_VOLATILITY_BASELINE=2) — operatörün beklenen dağılımıyla BİREBİR eşleşti; 6210 yeni `path_observations.*` field-provenance satırı (0 eksik/duplicate, 0 proxy→safe downgrade), mevcut 4320 satır DEĞİŞMEDİ (toplam 10530). **Postflight:** schema_version=10/signal=270/raw=802/effective=270/effective-CLOSED=0/FORWARD_N=0/binding=2 (aynı signal/knowledge_id çiftleri) doğrulandı; 2. çalıştırma idempotent (content hash + row count birebir); tüm semantik assertion'lar (reference_price_ts≤signal_birth_ts, effective_path_start_ts≥signal_birth_ts, known_at_ts≥horizon_end_ts, MFE≥0/MAE≤0, sıfır-ekstremum→time_to=0, same-candle→SAME_CANDLE_UNKNOWN, mfe_bps/mae_bps=HISTORICAL_PROXY, realized_vol_at_anchor=DETERMINISTIC_HISTORICAL_SAFE, effective-CLOSED=0/270 OPEN≠aktif pozisyon) real veriye karşı SQL ile doğrulandı, 0 ihlal. **Yan bulgu+düzeltme (gerçek DB'ye dokunmadan):** `ami/lifecycle/provenance_rehearsal.py`'nin `provenance_row_counts()`/`provenance_content_hash()`'i `ami_lifecycle_field_provenance` üzerinde scope'suz `COUNT(*)`/`SELECT *` yapıyordu — tablo artık 2. bir yazıcı (path_field_provenance) tarafından paylaşılınca 4320 varsayımı 10530 ile çakıştı; `provenance_version=?` filtresi eklenerek düzeltildi (gerçek bug, path-provenance'ın tabloyu meşru şekilde paylaşmasının ortaya çıkardığı). `tests/test_ami_lifecycle_provenance_rehearsal.py`'nin `schema_version_before in (8,9)` kontrolü `(8,9,10)`'a genişletildi (v9→v10 lifecycle semantiğini değiştirmedi, yalnız ekledi — emsal: `test_schema_fingerprint_changes_only_by_addition`). Post-migration backup: `canonical_post_phase7b_path_metrics_migration_v10_20260704_180906.sqlite`, sha256=`fa57cc79...`. Full regresyon (588/588 — 495 önceki + 13 yeni gateway testi + 80 yeni path-modülü testi) isolated test DB'lerinde çalıştırıldı, gerçek DB hash/mtime bu regresyon boyunca BİREBİR SABİT kaldı (doğrulandı, test-öncesi=test-sonrası). Protected session delta = ZERO | Evet (backup×2 doğrulandı: pre-migration `canonical_pre_phase7b_path_metrics_migration_20260704_180134.sqlite` sha256=3d34f44e..., post-migration `canonical_post_phase7b_path_metrics_migration_v10_20260704_180906.sqlite` sha256=fa57cc79...; additive-only migration, rollback disposable'da doğrulandı) | **APPLIED_TO_CANONICAL_DB** |
| M-0028 | 2026-07-04 | **data/ami/canonical.sqlite (GERÇEK)** | BATCH-P7A-P-CANON (operatör onayı: "APPROVE PHASE 7A-P CANONICAL PROVENANCE MIGRATION" + SCHEMA_DRIFT_BLOCKER sonrası "SELECT OPTION 2 — RESTORE CLEAN V8 AND REAPPLY CONTROLLED MIGRATION"): M-0025/M-0026/M-0027'de disposable kopyada doğrulanan v9 migrasyon paketi (setup_version nullable + NULL semantiği, terminal_ts NULL semantiği, 16-field provenance table/backfill, 266 append-only CORRECTION, `ami_lifecycle_effective_transitions` view, `validate=False` fail-closed contract) gerçek DB'ye uygulandı. Ara olay: schema.py düzenlemesi + full-regression çalıştırması, migrasyon script'inden ÖNCE gerçek DB'yi yanlışlıkla v9 şemaya (veri dokunulmadan) yükseltti (SCHEMA_DRIFT_BLOCKER) — forensic snapshot alındı, test-isolation safety closure eklendi (`tests/conftest.py` + `ami/warehouse/schema.py`'nin `_TEST_ISOLATION_ACTIVE` guard'ı, kalıcı altyapı), temiz v8 backup'tan restore edildi, sonra controlled migration TEKRAR uygulandı. `ami/warehouse/schema.py`: `CANONICAL_SCHEMA_VERSION` 8→9 (kalıcı), `_SCHEMA_PHASE7A`'nın setup_version'ı nullable'a düzeltildi, yeni `_EFFECTIVE_LEDGER_VIEW`+`_SCHEMA_PHASE7AP1_PROVENANCE` blokları eklendi (disposable-doğrulanmış DDL'yle byte-for-byte aynı, ayrıca doğrulandı). Dry-run sırasında `backfill_lifecycle()`'da bulunan bir idempotency bug'ı (rerun'ın lifecycle_reason_code'u SIGNAL_BIRTH'e sıfırlayıp CORRECTION'ı silmesi) gerçek DB'ye dokunulmadan düzeltildi. **Sonuç:** schema_version=9, 270 signal/802 raw transition (270+266+266)/270 effective transition/0 effective CLOSED interval/270 OPEN current-state, 4320 provenance (0 eksik/duplicate), FORWARD_N=0 ve 2 binding değişmedi, 2. çalıştırma idempotent (content hash birebir), old-reader (feature_gateway) etkilenmedi (252). Full postflight regresyon (495/495, isolated test DB'lerde) sırasında gerçek DB hash/mtime BİREBİR SABİT kaldı (doğrulandı) | Evet (backup×3 doğrulandı: pre-drift `canonical_pre_phase7a_p_provenance_migration_20260704_153645.sqlite` sha256=a3731c98..., accidental-drift-snapshot `canonical_ACCIDENTAL_V9_SCHEMA_DRIFT_snapshot_20260704_161554.sqlite`, post-migration `canonical_post_phase7a_p_provenance_migration_v9_20260704_162055.sqlite` sha256=3d34f44e...; append-only correction, rollback disposable'da doğrulandı) | **APPLIED_TO_CANONICAL_DB** |
| M-0027 | 2026-07-04 | **disposable copy only — data/ami/canonical.sqlite DEĞİL** | BATCH-P7A-P1-V9R3 (two-layer ledger contract): yeni `ami_lifecycle_effective_transitions` VIEW (`LIFECYCLE_SCHEMA_VERSION` 2→3) — superseded transition'ları ve pure-reversal correction'ları (bir transition'ı tam tersine çeviren correction: `previous_status`/`new_status` swap) dışlayan, immutable raw ledger'ın (`ami_lifecycle_transitions`) üzerine kurulu bir sorgu-katmanı. `insert_transition()`'a `validate=False` için fail-closed guard'lar eklendi (correction_of zorunlu + var olması zorunlu + reason_code=CORRECTION zorunlu + aynı hedef ikinci kez düzeltilemez). Sonuç: raw=802 (değişmedi), effective=270 (1/sinyal), current-state OPEN=270/CLOSED=0 hem raw hem effective'de tutarlı, duration-contamination kanıtı (naif raw sorgu sahte interval üretir, effective view üretmez) | Evet (additive VIEW; disposable kopyada rollback+reapply+idempotency doğrulandı) | **PREPARED_AND_VALIDATED_ON_DISPOSABLE_COPY — NOT_APPLIED_TO_CANONICAL_DB** |
| M-0026 | 2026-07-04 | **disposable copy only — data/ami/canonical.sqlite DEĞİL** | BATCH-P7A-P1-V9R2 (LIFECYCLE_TERMINAL_SEMANTIC_BLOCKER correction): `backfill_lifecycle()` artık hiçbir lifecycle_status/transition iddiasını `ami_events.event_end_ts_ms`'ten türetmiyor (yalnız OPEN/SIGNAL_BIRTH). Yeni `correct_unvalidated_terminal_close()`: gerçek DB'nin (M-0023'te uygulanmış, disposable-kopyada tekrarlanan) 266 eski TERMINAL_CLOSE→CLOSED satırını append-only bir CORRECTION transition'ıyla (CLOSED→OPEN, transition_version=2, correction_of=<orijinal id>, validate=False) tersine çeviriyor — orijinal satır silinmedi/değiştirilmedi. Sonuç: lifecycle_status OPEN=270/CLOSED=0 (önceden OPEN=4/CLOSED=266), transition toplamı 536→802 (270 genesis+266 eski TERMINAL_CLOSE[korunan]+266 yeni CORRECTION). 270 signal identity/source_event_id/independent_cycle_id DEĞİŞMEDİ. Ek: `UNKNOWN_SETUP_VERSION_TOKEN` identity-contract sabiti (=SETUP_VERSION_DEFAULT) + supersession contract belgelendi | Evet (append-only correction; disposable kopyada rollback+reapply+idempotency tekrar doğrulandı) | **PREPARED_AND_VALIDATED_ON_DISPOSABLE_COPY — NOT_APPLIED_TO_CANONICAL_DB** |
| M-0025 | 2026-07-04 | **disposable copy only — data/ami/canonical.sqlite DEĞİL** | BATCH-P7A-P1-V9 (operatör onayı: "APPROVE PHASE 7A-P1 SCOPE AND VERSION CORRECTION (v9, 16-FIELD) — WITH SEMANTIC CLOSURE"): version contract düzeltmesi (M-0024'teki zımni "8→8" hatalıydı — `ami/lifecycle/canonical_schema.py:LIFECYCLE_SCHEMA_VERSION` 1→2, proposed canonical v9; `ami/warehouse/schema.py:CANONICAL_SCHEMA_VERSION` gerçek DB'de HÂLÂ 8). İki semantik düzeltme disposable kopyada uygulandı: `setup_version` kolonu artık NULL (`NOT NULL`→nullable table-rebuild, `migrate_setup_version_nullable()`, identity-hash girdisi `SETUP_VERSION_DEFAULT` değişmedi), `terminal_ts` kolonu artık NULL (`event_end_ts_ms` ayrı `_terminal_transition_ts` internal alanına taşındı, 536 transition-ledger AYNEN korundu). Actual-value null matrisi: setup_version/terminal_ts/timeframe/first_known_ts/first_executable_ts/last_confirmation_ts/invalidation_ts=270/270 NULL, kalan 9 alan=270/270 non-NULL. 270 signal/536 transition/direction=HISTORICAL_PROXY/FORWARD_N=0 DEĞİŞMEDİ. **Yan-bulgu regresyon:** `canonical_backfill.py`'nin global setup_version=None değişikliği eski `migration_rehearsal.py` rehearsal'ını kırdı (4/15 test), `migrate_setup_version_nullable()` çağrısı eklenerek düzeltildi, full 472/472 regresyon yeniden yeşil | Evet (additive/nullable-relaxation table-rebuild; disposable kopyada rollback+reapply+idempotency tekrar doğrulandı) | **PREPARED_AND_VALIDATED_ON_DISPOSABLE_COPY — NOT_APPLIED_TO_CANONICAL_DB** |
| M-0022 | 2026-07-04 | **disposable copy only — data/ami/canonical.sqlite DEĞİL** | BATCH-P7A-1: `ami_signal_lifecycle` + `ami_lifecycle_transitions` (proposed CANONICAL_SCHEMA_VERSION 7→8, henüz uygulanmadı). Şema+migration+backfill kodu `ami/lifecycle/{canonical_schema,canonical_backfill,migration_rehearsal}.py`'de yazıldı ve gerçek canonical.sqlite'ın disposable KOPYASINDA tam doğrulandı (270 signal/536 transition, row-count+content-hash+rebuild eşitliği, rollback+reapply temiz, old-reader etkilenmedi). `ami/warehouse/schema.py` bilinçli olarak DOKUNULMADI (paylaşılan init_schema()'ya eklemek yanlışlıkla gerçek DB'ye uygulanma riski taşırdı) | Evet (additive-only; disposable kopyada rollback test edildi) | **PREPARED_AND_VALIDATED_ON_DISPOSABLE_COPY — NOT_APPLIED_TO_CANONICAL_DB** (M-0023 ile UYGULANDI — bu kayıt geçmiş olarak korunuyor, üzerine yazılmadı) |
| M-0023 | 2026-07-04 | **data/ami/canonical.sqlite (GERÇEK)** | BATCH-P7A-CANON (operatör onayı: "APPROVE PHASE 7A CANONICAL MIGRATION"): M-0022'de disposable kopyada doğrulanan AYNI kod (hash-doğrulandı, değişmedi) gerçek DB'ye uygulandı. `ami/warehouse/schema.py`: `CANONICAL_SCHEMA_VERSION` 7→8 (yeni `_SCHEMA_PHASE7A` bloğu — doğrulanan SQL'in birebir kopyası). Backfill: **270 signal / 536 transition** (deterministik beklenen sayıyla birebir, frozen source snapshot'tan hesaplandı). Preflight: schema fingerprint drift yok, concurrent-writer yok, tam backup alındı (`data/ami/backups/canonical_pre_phase7a_migration_20260704_130548.sqlite`, sha256=`d5aa43313ae8...`). Postflight: identity uniqueness✅, current-state-rebuild 270/270✅, 2. çalıştırma idempotent (0 duplicate)✅, FORWARD_N değişmedi (0)✅, 2 mevcut binding aynı✅, old-reader (feature_gateway) etkilenmedi✅, 38 test_ami_*.py+buyfade/silexit dosyaları migrasyon sonrası tek tek yeniden çalıştırıldı, hepsi ✓ | Evet (additive-only; backup+rollback-rehearsal-kanıtlı, ancak gerçek DB'de rollback bu batch'te ÇALIŞTIRILMADI — yalnız hazır) | **APPLIED_TO_CANONICAL_DB** |
| M-0024 | 2026-07-04 | **disposable copy only — data/ami/canonical.sqlite DEĞİL** | BATCH-P7A-P1 (operatör onayı: "APPROVE PHASE 7A-P1 DISPOSABLE PROVENANCE CLOSURE"): Operatörün tespit ettiği PHASE_7A_PROVENANCE_GAP'i (direction'ın field-level HISTORICAL_PROXY sınıflandırması yalnız Python kodunda vardı, veride yoktu) kapatmak için `ami_lifecycle_field_provenance` (yeni tablo, genel/tek-kullanımlık-olmayan sözleşme — signal_id/field_name/field_classification/is_proxy/derivation_method/source_reference/limitations/provenance_version) + `ami_lifecycle_direction_view` (canonical query contract — direction hiçbir zaman classification'sız sunulmaz) tasarlandı. Reconciliation: mevcut 5 tablo (data_quality_events/causal_assumption_registry/evidence_contamination/mt_family_registry/market_structure_versions) incelendi, hiçbiri per-field provenance karşılamıyor — yeni, minimal, tek nesne eklendi (paralel truth-layer YOK, yalnız `signal_id`+`field_name` referans eder). Gerçek `classify_direction_from_setup_id` koduna karşı çapraz-doğrulandı (tahmin edilmedi). Disposable kopyada: **270 signal × 16 alan = 4320 provenance satırı**, direction 270/270 HISTORICAL_PROXY/is_proxy=1, 2. çalıştırma idempotent (0 duplicate), rollback+reapply temiz, 270 signal/536 transition DEĞİŞMEDİ, missing-provenance validator gerçekten reddediyor (sessizce REAL varsaymıyor) | Evet (additive-only; disposable kopyada rollback test edildi) | **PREPARED_AND_VALIDATED_ON_DISPOSABLE_COPY — NOT_APPLIED_TO_CANONICAL_DB** |
| — | 2026-07-03 | — | Phase 0: migration yok (read-only audit) | — | NO_UPDATE_REQUIRED tabanı |
| M-0001 | 2026-07-03 | data/ami/canonical.sqlite | Warehouse iskeleti (BATCH-P1-001): schema_versions, artifact_registry, artifact_lineage, question_families, question_registry, contradiction_registry, operator_decision_queue, namespace_registry | Evet (dosya sil) | DONE — idempotent doğrulandı (2× init_db, tek schema_versions satırı) |
| M-0002 | 2026-07-03 | data/ami/canonical.sqlite | Schema v1→v2 (BATCH-P2-001): evidence_contamination, researcher_exposure_ledger, mt_family_registry, causal_assumption_registry, data_quality_events, market_structure_versions (whitepaper §70/§77 Phase A) | Evet (CREATE TABLE IF NOT EXISTS, v1 tabloları etkilenmedi) | DONE — idempotent doğrulandı (2× init_db) |
| M-0003 | 2026-07-03 | data/ami/canonical.sqlite | Schema v2→v3 (BATCH-P3-001): ami_events, ami_cycles, event_cycle_membership (Protocol §7.4/§8, Obs §5). ami_cycles bilinçli boş bırakıldı (OD-003 bekliyor) | Evet (CREATE TABLE IF NOT EXISTS, v1/v2 tabloları etkilenmedi) | DONE — idempotent doğrulandı (2× init_db + 2× ingest) |
| M-0004 | 2026-07-03 | data/ami/canonical.sqlite | BATCH-P3-004 remediation: `PRAGMA foreign_keys=ON` (F4) + schema_versions.note dinamik (F5). **Yan etki:** FK enforcement, `question_seed.py`'de gizli bir bug'ı ortaya çıkardı — `question_registry.family_id` UPSERT'i `question_families.family_id` ile eşleşmiyordu (prefix farkı); 1058 satır etkilenmiş, düzeltildi + UPDATE SET'e eklendi + gerçek DB onarıldı (0/1058 mismatch → repair sonrası 0) | Evet (yalnız pragma + kod düzeltmesi; veri onarımı idempotent upsert ile) | DONE — 12/12 test + gerçek DB doğrulaması |
| M-0005 | 2026-07-03 | data/ami/canonical.sqlite | BATCH-P3-005: ami_cycles canonical-v1 seed (OD-003 onaylı A2+B2+C2) — 167 cycle, event_cycle_membership'e is_canonical=1 satırları eklendi (append-only, mevcut 6 sensitivity pencere dokunulmadı) | Evet (veri; şema değişmedi) | DONE — idempotent doğrulandı (2× çalıştırıldı, 167/167) |
| M-0006 | 2026-07-03 | data/ami/canonical.sqlite | Schema v3→v4 (BATCH-P4-001..004): ami_candles, ami_candle_morphology, ami_swings, ami_levels, ami_pushes (Chart-Native Extension §4/§6/§7). ETHUSDT 1m(+5m candle) gerçek veri: 3456 candle, 556 swing, 576 level, 454 push. **Push geometrisinde 2 gerçek bug bulunup düzeltildi:** (1) path_length hareketli-taban bps ile toplanıyordu (displacement ile karşılaştırılamaz) → sabit referans fiyata (start swing) geçirildi; (2) path, swing'in gerçek pivot fiyatına (high/low) değil yalnızca pencere içi mum close'larına bağlanıyordu → uç segmentler eklendi. İkisi de efficiency_ratio>1 (imkansız, üçgen eşitsizliği ihlali) ile yakalandı, düzeltildi, gerçek DB onarıldı (0/369 → 0 ihlal), sınır-testi eklendi | Evet (CREATE TABLE IF NOT EXISTS, v1-v3 tabloları etkilenmedi; veri onarımı idempotent upsert ile) | DONE — idempotent doğrulandı (2× her script) |
| M-0007 | 2026-07-03 | data/ami/canonical.sqlite | BATCH-P6-000 (FABLE REVIEW B remediation): Schema v4→v5 — `ami_levels.touch_stats_point_in_time` (F-B2, idempotent `ALTER TABLE ADD COLUMN`, mevcut DB'de 0/572 satırda default 0). **F-B4 fix:** level_registry pencere-kenarı truncation kontrolü eklendi (dönemin ilk mumu gerçek sınırda değilse dönem atlanır) — gerçek veride 1 hatalı truncated SESSION_HIGH/LOW çifti (US 13:00 origin, yalnız 1 saatlik veriyle) tespit edilip kaldırıldı. **F-B1 fix:** known_at_ts artık max(son mum close, gerçek dönem-sonu sınırı). **F-B3 fix:** candle_builder artık `microstructure.db:gaps` (stream=agg_trades) ile çapraz-işaretleme yapıyor, çakışan pencereler GAPPED (mevcut 48h pencerede 0 çakışma, testlerle doğrulandı). Eski `level-v1` satırları (LEVEL_DEFINITION_VERSION="level-v2-boundary-safe" artık) kontrollü silindi — yazılım kusuru, araştırma verdict'i değil | Evet (CREATE TABLE IF NOT EXISTS + idempotent ALTER; level-v1 silme tek-yönlü ama yeniden-üretilebilir/deterministik) | DONE — 61/61 test + gerçek DB çapraz-doğrulama (0 truncation/boundary/GAPPED-mismatch ihlali) |
| M-0008 | 2026-07-03 | data/ami/canonical.sqlite | BATCH-P6-001 (F-B5/F3): `ami/research/feature_gateway.py` — Phase 6 için zorunlu tek erişim noktası. Şema değişikliği yok; `researcher_exposure_ledger`'a (Phase 2 tablosu) yazıyor | Evet (yeni kod dosyası, veri şeması sabit) | DONE — 10/10 mutation testi |
| M-0009 | 2026-07-04 | data/ami/canonical.sqlite | BATCH-P6-002 (W1): Schema v5→v6 — `experiment_registry` + `experiment_results` (Protocol §7.2/§7.7, önceden planlanmış ama kurulmamıştı). **Gerçek bug bulunup düzeltildi:** `_record_exposure`'ın exposure_id'si content-hash+ms-timestamp'ten üretiliyordu — aynı milisaniyede birden çok gateway çağrısı (W1'in tek koşumda 8 fetch çağrısı yapması) UNIQUE constraint ihlaline yol açtı; `uuid.uuid4()` ile düzeltildi. **İkinci bug:** `experiment_results` INSERT'i idempotent değildi (re-run'da satırlar birikiyordu, 12→24); `DELETE FROM experiment_results WHERE experiment_id=?` eklendi | Evet (CREATE TABLE IF NOT EXISTS; kod düzeltmeleri) | DONE — 17/17 test (gateway+W1) |
| M-0010 | 2026-07-04 | data/ami/canonical.sqlite | BATCH-P6-002 (W1) veri: E-W1-CYCLE-INTEGRITY-001 → experiment_registry(1) + experiment_results(12 metrik). OD-011 eklendi (registry_seed'e yansıdı, operator_decision_queue 10→11) | Evet (veri; şema v6 zaten kuruluydu) | DONE — idempotent doğrulandı (2× çalıştırıldı, 12/12 sonuç satırı) |
| M-0011 | 2026-07-04 | data/ami/canonical.sqlite | BATCH-P6-003: Schema v6→v7 — `ami_candidate_universe` (Protocol §17.8, event varlığına koşullanmamış all-timestamp candidate universe). `ami/research/candidate_universe.py` seed+freeze_and_record: ETHUSDT/1m gerçek veri **2932 candidate** (22 event-aligned/2910 no-event), E-CANDIDATE-UNIVERSE-001 (experiment_registry(1)+experiment_results(12 metrik), W1'i süpersede etmez). **Kapsam-uyuşmazlığı bulunup düzeltildi (bug değil, tasarım-farkındalığı eksikliği):** `ami_candles` 48h bounded lookback (Phase 4), W1 sayıları tüm-tarih — all-history ve pencere-kapsamlı sayılar ayrı metrik olarak yazıldı, oranlar yalnız pencere-kapsamlıyla hesaplandı. `CONTRADICTION_REGISTER.md` CT-005 eklendi (OD-011 per-KO zenginleştirme: 8 RECOMPUTE_REQUIRED + 3 AFFECTED) → contradiction_registry 14→15; `knowledge.sqlite` DEĞİŞTİRİLMEDİ | Evet (CREATE TABLE IF NOT EXISTS; veri idempotent upsert) | DONE — 12/12 (candidate_universe) + 7/7 (registry_seed, CT-005) — toplam AMI 274/274, 2× çalıştırıldı (2932=2932 SQL doğrulandı) |
| M-0012 | 2026-07-04 | data/ami/canonical.sqlite | BATCH-P6-003b: `ami_candles` full-history backfill (şema değişmedi, yalnız veri) — yeni `build_candles_streaming()`/`derive_higher_timeframe()`/`seed_full_history()` (`ami/chart/candle_builder.py`) ile ETHUSDT **173,464×1m + 34,801×5m candle** (910.8s, tek sıralı proses, agg_trades 173M satır TEK geçiş). **Gerçek bug bulunup düzeltildi:** streaming SQL alt-sınırı bucket-hizalı değildi (ilk bucket'ın başı kayboluyordu) — cross-check testiyle yakalandı, düzeltme sonrası referansla tam eşitlik doğrulandı. Sonuç: 252/252 anchor artık candle penceresi içinde (önceki 22/252'den). **W2 mezarlık çakışması bulundu** (`failure_archive` #8/#17) — #17 retry-koşulu (OI verisi) kontrol edildi, zayıf çıktı (38/252 anchor, 2 kopuk pencere); **OD-012 eklendi, W2 ERTELENDİ, W3'e geçildi** | Evet (yalnız veri; mevcut build_candles/seed dokunulmadı) | DONE — 8/8 (candle_builder yeni testler) + 7/7 (registry_seed, OD-012) — toplam AMI 282/282, gerçek veri 0 OHLC ihlali, idempotency UNIQUE constraint+test ile garanti |
| M-0013 | 2026-07-04 | data/ami/canonical.sqlite | BATCH-P6-005 hazırlık: `ami_swings`/`ami_levels`/`ami_pushes`/`ami_candidate_universe` **kod değişikliği olmadan** tam-tarih candle setine karşı yeniden seed edildi (bu 3 chart motoru zaten lookback sınırı taşımıyordu — yalnız `ami_candles` büyüdüğü için otomatik genişlediler). **Gerçek veri:** 32,055 swing (0.76s), 33,259 level (388.4s — touch-stat hesaplaması O(level×candle), F-B2 ile zaten araştırmaya bloklu ama hesaplanıyor), 24,940 push (179.9s); 0 OHLC/efficiency-ratio/known-at ihlali. `ami_candidate_universe` yeniden çalıştırıldı: **173,464 candidate, 252/252 anchor kapsandı, cycle_n_in_window=167=167 (all-history ile birebir)** — §52'deki 48h-pencere kapsam sorunu tamamen çözüldü | Evet (yalnız veri; kod dokunulmadı) | DONE — mevcut testler değişmeden geçti (kod değişmedi), gerçek veri 0 ihlal, idempotent (candidate_universe 2× doğrulandı) |
| M-0014 | 2026-07-04 | data/ami/canonical.sqlite | BATCH-P6-005 (W4): Post-event path taxonomy + structural location + event geometry — **saf tanımlayıcı** (descriptive-only, ekonomik kapı/giriş/çıkış iddiası YOK — mezarlıktaki "reversal fade" hipotezinin (`S34_REVERSAL_BACKTEST`/`S34_REVERSAL_STOP_BACKTEST`, ETH N=346 stop-sweep 0 leads) yeniden testi DEĞİL). `ami/research/w4_post_event_path_taxonomy.py`: dondurulmuş popülasyon (ETHUSDT REAL_LIQUIDATION, anchor_n=252, independent_cycle_n=167 — W1 ile aynı), 4 sabit ufuk (scalp_30m/1h, swing_4h/24h), sabit CONTINUATION/REVERSAL/CHOP bandı (±20bps, fit edilmedi), 3 yapısal-konum bayrağı (near_swing_low/near_level/recent_down_push, known-at-safe), 5 sabit karşılaştırmalık aile (FAM_POST_EVENT_PATH_TAXONOMY), kronolojik 70/30 stabilite kontrolü, candidate-universe üzerinden negative control. `feature_gateway`'e `ami_candidate_universe` eklendi (KNOWN_FEATURE_TABLES+filtre allowlist). **Gerçek bulgu:** swing ufkunda (4h/24h) REVERSAL oranı (54.3%) scalp ufkundan (33.1%) belirgin yüksek; anchor popülasyonu rastgele-zaman kontrolünden daha fazla REVERSAL gösteriyor (57.6% vs 47.0%) — descriptive mean-reversion-after-cascade etkisi gerçek (önceki "reversal base rate REAL ama harvestable değil" bulgusuyla tutarlı). **C2/C3/C4 DEGENERATE çıktı** (yapısal bayrakların hepsi %100 True) — swing_low/level/push'lar bu piyasada çok yoğun (sırasıyla ~12.5dk/~6dk/~16dk'da bir), sabit 50bps/4h eşiği ayırt edici değil; eşik GERİYE DÖNÜK DEĞİŞTİRİLMEDİ (post-hoc fishing olurdu), dürüstçe INSUFFICIENT_SAMPLE/degenerate olarak raporlandı | Evet (yeni tablo yok, yalnız deney verisi) | DONE — 10/10 (w4 modülü) + 1/1 (gateway ami_candidate_universe testi) — toplam AMI 299/299, idempotent (registry=1/results=8, 2× doğrulandı) |
| M-0015 | 2026-07-04 | data/ami/canonical.sqlite | BATCH-P6-006 (W5a): `ami_candle_morphology` **kod değişikliği olmadan** tam-tarih candle setine karşı yeniden seed edildi (208,265 satır [173,464×1m+34,801×5m], 6.6s; zaten lookback sınırı taşımıyordu). **W5a — Candle morphology + swing grammar (W5'in 4 bileşeninden 2'si; sweep/breakout-retest + SHORT-genesis OD-014 ile backlog'a alındı, altyapı eksik).** `ami/research/w5a_morphology_swing_grammar.py`: outcome W4'ten AYNEN yeniden kullanıldı (swing_24h path class, yeniden tanımlanmadı); 2 yeni sabit özellik (anchor candle morphology label, pre-anchor Dow-theory swing-grammar HH/HL vs LH/LL yapısı, known-at-safe); 2 sabit karşılaştırma (FAM_CANDLE_MORPHOLOGY_SWING_GRAMMAR). **Gerçek bulgu (null-sonuç, geçerli):** her iki özellik de REVERSAL oranını (~%56-60, tüm bucket'larda benzer) anlamlı ayırt etmiyor — morphology/grammar koşullandırması swing_24h çıktısına duyarsız | Evet (yeni tablo yok, yalnız deney verisi) | DONE — 8/8 (w5a modülü) — toplam AMI 307/307, idempotent (registry=1/results=4, 2× doğrulandı) |
| M-0016 | 2026-07-04 | data/ami/canonical.sqlite | BATCH-P6-007 (W6): **Compression + relative strength + session** (W6'nın 4 bileşeninden 3'ü; channel OD-015 ile backlog'a alındı, CHANNEL_BOUNDARY NOT_IMPLEMENTED). Coverage kontrolü: compression HAZIR (`ami/states/engine.py:StructurePhase.COMPRESSION`, Phase 3'te zaten kullanılıyor), session HAZIR (level_registry session sınırları), RS BUILDABLE (BTC+ETH mark_prices tam kapsıyor, ilk kez hesaplandı). `ami/research/w6_compression_rs_session.py`: outcome yine W4'ten aynen yeniden kullanıldı; 3 yeni sabit özellik (compression binary, RS 1h-return-farkı işareti, session ASIA/EUROPE/US/OFF); 3 sabit karşılaştırma (FAM_COMPRESSION_RS_SESSION). **Gerçek bulgu:** RS anlamlı ayırt ediyor — RS_ETH_STRONG'da REVERSAL %68.1, RS_ETH_WEAK'te %53.4 (~15 puan fark, n=69/174); compression cascade anlarında neredeyse hiç görülmüyor (n=3/252, beklenen/mantıklı bir degenerasyon — cascade ve compression yapısal olarak dışlayıcı); session'da ASIA (%56.1) ile US (%56.4) arasında fark yok (EUROPE/OFF n<20, insufficient) | Evet (yeni tablo yok, yalnız deney verisi) | DONE — 7/7 (w6 modülü) — toplam AMI 314/314, idempotent (registry=1/results=5, 2× doğrulandı) |
| M-0017 | 2026-07-04 | data/ami/canonical.sqlite | BATCH-P6-008 (W6-RS confirmation wave): W6'nın RS bulgusunu TEK, dondurulmuş hipotezle (RS_threshold=0, RS_lookback=1h — sweep yok) confirmatory olarak sınadı. `feature_gateway.fetch_events()`'e `notional` eklendi (confounder check için). `ami/research/w6rs_confirmation.py`: Wilson 95% CI, one-sided label-permutation testi (n=2000, sabit seed), kronolojik 70/30'da AYRI AYRI RS-koşullu replikasyon, aylık stabilite, candidate-universe negative control, 3 confounder kontrolü (notional/session/day_trend_bps — day_trend_bps `tools/research_s34_btc_sell_anomaly_audit.py` ile aynı formül). **Gerçek bulgu (nüanslı, dürüstçe raporlandı):** yön TRAIN (n=170, +16.7pp) ve TEST'te (n=73, +7.6pp) tutarlı, permütasyon p=0.0255; AMA candidate-universe negative control BENZER büyüklükte bir fark gösteriyor (rastgele zamanlarda da +11.8pp) — cascade'e özgü değil, genel piyasa etkisi olabilir; day_trend_bps confounder'ı iki grup arasında belirgin farklı (-77 vs -142 bps); 2026-06'da yön TERSİNE döndü (aylık stabilite kusurlu); 2026-05'te 0 anchor (gerçek boşluk, veri hatası değil). **Trade/PnL iddiası veya bucket promotion YAPILMADI** — sonraki adım operatöre bırakıldı | Evet (yeni tablo yok, yalnız deney verisi + notional kolonu feature_gateway'e eklendi) | DONE — 11/11 (w6rs modülü) + 1/1 (gateway notional) — toplam AMI 326/326, idempotent (registry=1/results=17, 2× doğrulandı) |
| M-0018 | 2026-07-04 | data/ami/canonical.sqlite | BATCH-P6-009 (W6-RS confound-resolution wave, **POST-HOC SENSITIVITY/SPECIFICITY — confirmatory DEĞİL**): RS>0 etkisinin cascade-anchor'a özgü mü genel piyasa-rejimi etkisi mi olduğunu test etti. RS_threshold=0/lookback=1h/24h outcome AYNEN donuk. `ami/research/w6rs_confound_resolution.py`: numpy ile elle yazılmış IRLS logistic regression (statsmodels yok), anchor_status×RS_group interaction terimi ana test; day_trend_bps+session+month sabit kovaryat (notional yalnız anchor-only confound olarak önceki dalgadan aktarıldı, joint modele girmedi — control zamanlarında tanımsız). Cluster block-bootstrap (n=2000, canonical-v1 cycle_id ile) interaction katsayısının CI'ı için. Overlap/positivity kontrolleri, aylık yön kontrolü, kronolojik holdout. **Gerçek bulgu:** anchor_n=243/control_n=1985, independent_cycle_n=159; interaction katsayısı=0.397 (pozitif nokta tahmini) ama bootstrap CI=(−0.28, 1.08) **sıfırı içeriyor** (belirsiz); Haziran (n=26/76) VE Temmuz (n=10/3, berabere) yönü kovaryat-düzeltmesinden SONRA da ters/uyumsuz; month_2026-07 katsayısı ayrışma riski gösteriyor (dürüstçe not edildi). **VERDICT=REGIME_DEPENDENT_CONTINUE_ACCUMULATING** (karar kuralı #3: Haziran anomalisi açıklanamadı) — ne GENERAL_REGIME_FEATURE ne CASCADE_SPECIFIC olarak kesin sınıflandırılmadı, daha fazla veri gerekiyor. Ekonomik validation dalgasına geçilmedi, bucket/route/observer değişmedi | Evet (yeni tablo yok, yalnız deney verisi) | DONE — 10/10 (w6rs_confound_resolution modülü) — toplam AMI 336/336, idempotent (registry=1/results=9, 2× doğrulandı) |
| M-0019 | 2026-07-04 | data/ami/canonical.sqlite | BATCH-P6-010 (W7A): **Historical State/Structure Aging & Market Clocks.** Operatör 5 düzeltme sonrası preregistration onayladı (TRAIN-medyan bucket cutpoint; market-clock 60/60+24/30 completeness gate; canonical event_end_ts_ms ile liquidation-age dışlama; exact maturity cutoff + excluded_no_horizon_data; primary=cluster-bootstrap risk-difference, secondary=Holm-adjusted permutation). `feature_gateway.fetch_events()`'e `event_end_ts_ms` eklendi (additive, mevcut index-tabanlı source_quality erişimini bozmadı). `ami/research/w7a_state_structure_aging_market_clocks.py`: population/outcome W4'ten aynen (anchor_n=252, swing_24h path class); 9 bağımsız feature/test (FAM_SIGNAL_AGING_MARKET_CLOCK): state_age (StructurePhase dwell, cadence=1h, cap=168 adım/7gün, LEFT_CENSORED/LEFT_CENSORED_AT_CAP/MISSING taksonomisi), swing_age/level_age/push_age (ayrı, known_at_ts nearest-prior), trade_count/volume/realized_vol clock (60-candle pencere, 30-pencere TRAIN-öncesi medyan normalizasyon, ratio>1.0 bucket), liquidation_age same/opposite-direction (200K eşik = `_cascade()`'den yeniden kullanım, event_end_ts_ms ile canonical cascade-dışlama, 248/252 anchor'da mevcut). OD-016 eklendi (sinyal-lifecycle tam şeması + book_update_age NOT_IMPLEMENTED, backlog). **Gerçek sonuç:** total_anchor_n=252, analyzed_n=243 (excluded=9, W6RS-confirmation ile birebir aynı neden), independent_cycle_n=159. 9 testin 2'si Holm-düzeltme ÖNCESİ p<0.05 (liq_age_same_direction p=0.0435, realized_vol_clock p=0.0295) ama **Holm-adjusted p'ler 0.348/0.2655 — hiçbiri family-wise anlamlı değil**; bootstrap CI'ların çoğu sıfırı içeriyor. Dürüstçe null/nüanslı sonuç, hiçbir ekonomik iddia veya bucket/route/observer promotion YAPILMADI | Evet (yeni tablo yok; feature_gateway'e additive kolon + yalnız deney verisi) | DONE — 20/20 (w7a modülü) + 2/2 (gateway event_end_ts_ms) — toplam AMI 358/358, idempotent (registry=1/results=16, 2× doğrulandı) |
| M-0020 | 2026-07-04 | data/ami/canonical.sqlite | BATCH-P6-010b (W7A checkpoint kapanışı, operatör talimatı): `classify_closure()` eklendi — 2 nominal (Holm-öncesi) p<0.05 sonucu (liq_age_same_direction, realized_vol_clock) post-hoc eşik/subgroup taramasıyla kurtarılmaya ÇALIŞILMADI; `closure_classification` sonucu canonical SQL'e yazıldı (7 NULL + 2 UNCONFIRMED_DESCRIPTIVE_LEAD + 0 HOLM_SIGNIFICANT). **W7A KAPALI.** | Evet (yeni tablo yok; yalnız yeni fonksiyon + deney verisi) | DONE — 5/5 yeni test — toplam AMI 363/363, idempotent (results 16→17, 2× doğrulandı) |
| M-0021 | 2026-07-04 | data/ami/canonical.sqlite | BATCH-P6-011 (W10a): **Multi-Timeframe Structural Conflict** (W10'un yalnız buildable yarısı; LONG↔SHORT transitions yarısı OD-017 ile backlog'a alındı — BLOCKED_BY_DATA [%100 SELL-cascade popülasyon] + NOT_IMPLEMENTED [whitepaper §54.5'in Phase-8 forward-dashboard sayfası]). Operatörün 7 düzeltmesi preregistration'a işlendi: (1) UP_UP/DOWN_DOWN/UP_DOWN/DOWN_UP/NEUTRAL 5 ham hücre AYRI korunuyor, AGREE olarak veri-seviyesinde erken birleştirilmiyor; (2) primary hipotez TEK: CONFLICT(UP_DOWN∪DOWN_UP) vs AGREEMENT(UP_UP∪DOWN_DOWN), 5-hücreli tablo yalnız descriptive, 5 ayrı p-value üretilmiyor; (3) NEUTRAL primary contrast'a hiç eklenmiyor (N'i ne olursa olsun), kendi tanımlayıcı kategorisinde kalıyor; (4) known-at güvenliği: `StateEngine._structure()`'ın candle değil mark_prices tick-tabanlı `ts_ms<=ts` sorgusu kullandığı açık testle (`test_direction_classification_is_known_at_safe` — gelecekteki bir mark_price satırı anchor'daki yönü değiştirmiyor) doğrulandı; (5) direction mapping (UP/DOWN/FLAT) `_structure()`'ın MEVCUT/DEĞİŞMEMİŞ formülünden birebir alıntı, yeni icat değil; (6) exact maturity cutoff (W6RS/W7A paterni) + kronolojik 70/30 + independent-cycle N + candidate-universe negative control korundu; (7) OD-017 eklendi. `ami/research/w7a_state_structure_aging_market_clocks.py:cluster_bootstrap_risk_difference()`'e `label_high`/`label_low` parametreleri eklendi (geriye-uyumlu, W7A'yı etkilemedi) — W10a'nın CONFLICT/AGREEMENT etiketleriyle yeniden kullanılabilmesi için. TF çifti (1h/4h) OD-003'ün cycle_resolver'ından birebir alındı, yeni seçim değil. **Gerçek bulgu:** analyzed_n=243, independent_cycle_n=159; DOWN_DOWN baskın (146/243, cascade popülasyonunun ayı-eğilimli doğasıyla tutarlı), UP_DOWN (n=3) ve DOWN_UP (n=19) hücreleri INSUFFICIENT_SAMPLE; primary contrast CONFLICT(n=22) vs AGREEMENT(n=164) — risk-difference=−0.031, p=0.8275, bootstrap CI (−0.2717,0.2094) sıfırı içeriyor — **null sonuç**, train/test'te CONFLICT n'si (17/5) zaten yetersiz-örneklem, negative control'de de anlamlı fark yok (p=0.1245). Ekonomik iddia/bucket/route/observer promotion YAPILMADI | Evet (yeni tablo yok; w7a'ya additive parametre + yalnız deney verisi) | DONE — 15/15 (w10a modülü) + 1/1 (w7a custom-label testi) — toplam AMI 379/379, idempotent (registry=1/results=9, 2× doğrulandı) |
