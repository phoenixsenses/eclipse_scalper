# ECLIPSE CROSSWALK V1 — what the three lanes said to each other without knowing

**Hand-written from mechanically extracted verdict tokens** (`_ATLAS_INDEX.json`). Every row below
names the tokens it rests on, so any claim here can be traced back to a section.

The three sessions ran in parallel and could not see each other. They agreed, answered and
contradicted one another anyway. **None of that was visible until now** — it surfaced only because a
memory file passed through one session's hands, which is luck, not design.

---

## 0 · The first finding is about the index itself

```
LANE A   395 distinct verdict tokens across 50 sections
LANE C   174 distinct verdict tokens across 31 sections
LANE B     0 distinct verdict tokens across 18 sections
```

**Lane B closed none of its eighteen sections with a fenced verdict block.** Its results are in prose
only. That is not a judgement about their quality — B's sections include some of the day's sharpest
method work — but it means **lane B is invisible to every mechanical index this estate has**,
including CKR-01's `_LANE_VERDICTS.json` and this one. Anything B established has to be re-read by a
human to be found.

It is the first item in B's charter.

---

## 1 · Independent agreement — the same conclusion, reached twice, in ignorance

| object | lane A | lane C | note |
|---|---|---|---|
| reaction vs prediction | `REACTION_VS_PREDICTION_IMPACT_PERMANENTLY_UNIDENTIFIED` | `REACTION_VS_PREDICTION_IMPACT_PERMANENTLY_UNIDENTIFIED` | **byte-identical token, two lanes, no contact.** Both routed through Bouchaud §11.2 |
| liquidity conditioning | `RESPONSE_IS_LIQUIDITY_CONDITIONED` | `RESPONSE_IS_LIQUIDITY_CONDITIONED` | identical again |
| `κ−χ` | `KAPPA_MINUS_CHI_0_255_0_361_0_193` (A-S30) | `KAPPA_MINUS_CHI_CONFIRMED_A_THIRD_TIME` (C-T21) | same law, different data, **and both flagged SOL**: A `SOLUSDT 0.193` outside the book's 0.25–0.30; C `SOL_KAPPA_MINUS_CHI_OUTSIDE_THE_BOOK_BAND` |
| propagator | `PROPAGATOR_PREDICTED_MINUS_ONE_HALF` (A-S40) | `PROPAGATOR_IDENTITY_NOT_REJECTED` | A measured `p → −0.5` from a predictor's capture; C tested the identity directly |
| Cartea's fill form | `CARTEA_EXPONENTIAL_FORM_CONFIRMED`, `CARTEA_KAPPA_IS_NEARLY_FLAT_OVER_AN_HOUR` (A-S45) | `FILL_CURVE_IS_A_POWER_LAW_ON_SMALL_TICK` | **these disagree in form** — see §3 |

**Two lanes arriving at a byte-identical verdict token, independently, is the strongest evidence
today produced for anything.** It is also the clearest waste: the same measurement was paid for twice.

---

## 2 · One lane answered another lane's open question

**C answered A's §455 puzzle.** A measured `R∞/(s/2) = 67` on BTC and wrote: *"this is not a claim
that market makers lose money … it defines a horizon"* — and left open how they survive at all.
C's token says it outright:

```
C:  MARKET_MAKERS_HERE_SURVIVE_BY_CANCELLING_NOT_BY_THE_SPREAD
```

**A spent §455 and §473 circling that and never reached it.**

**C also holds the term A's maker arithmetic was missing.** A closed §472 with a maker requirement of
0.36–0.82 bps/side. C:

```
C:  MAKER_BENEFIT_IS_3_BPS_MINUS_AN_UNMEASURED_ORDER_1_BPS_TERM
A:  MAKER_SAVING_COMPLETED_NOT_REFUTED
A:  MAKER_SAVING_IS_FEE_DIFFERENTIAL_TIMES_PHI_EXEC_MINUS_OPPORTUNITY_COST
```

C's *"unmeasured order-1 bps term"* and A's *"minus opportunity cost"* are the same missing quantity.
**Neither lane closed it, and neither knew the other was holding half of it.**

**C explains A's `ζ`.** A measured `ZETA_0_416_0_439_0_495` and could only say `ZETA_IS_NOT_DELTA`.
C measured why:

```
C:  ZETA_AT_LAG1_IS_MECHANICAL
C:  BOOK_ZETA_RANGE_HOLDS_ONCE_THE_MECHANICAL_PART_IS_REMOVED
```

---

## 3 · Contradiction

| # | A says | C says | status |
|---|---|---|---|
| CT-016 | `CARTEA_EXPONENTIAL_FORM_CONFIRMED` — the fill curve is `exp(−κδ)`, κ ≈ 0.0097/bp, nearly flat over an hour (A-S45, 293 attempts, 15 symbols, one day) | `FILL_CURVE_IS_A_POWER_LAW_ON_SMALL_TICK` — a power law, not an exponential | **OPEN.** Different horizons and different depth units; may be the same curve read at two scales, or a real disagreement about functional form. Neither lane saw the other. |
| CT-017 | `SPREAD_IS_EXACTLY_ONE_TICK` on 12 of 15 symbols; the spread carries no information on majors | `TICK_RENT_EXISTS_ON_SOL`, `OPPORTUNITY_COST_IS_ONE_TICK_ONLY_ON_THE_LARGE_TICK_SYMBOL` | **COMPATIBLE, and jointly stronger.** Both isolate SOL as the large-tick case from opposite directions. Recorded because the pair is more informative than either alone. |

Contradiction CT-016 is entered in `CONTRADICTION_REGISTER.md`. CT-017 is recorded here only —
it is an agreement, not a conflict, and the register is for conflicts.

---

## 4 · What C asked for, out loud, four times

```
C:  INDEPENDENT_REVIEW_STILL_OWED          x4
C:  INDEPENDENT_RE_REVIEW_STILL_OWED       x2
C:  SELF_REVIEW_NOT_INDEPENDENT            x2
C:  FINDINGS_ISSUED_ACCEPTANCE_WITHHELD    x2
C:  SESSION_WIDE_MULTIPLICITY_MUST_BE_AUDITED_BEFORE_ANY_LABEL_IS_PUBLISHED
```

**Lane C ran its own review rounds and recorded, eight times, that self-review does not count.**
CLAUDE.md agrees: *"review'ın epistemik değeri BAĞIMSIZLIĞINDADIR."*

C has been asking for an independent reviewer all day. B is the lane that spent the day finding other
lanes' method errors. **That pairing is not a design choice made here; it is what both lanes' own
records already ask for.**

---

## 5 · Duplicated work, so it is not paid for twice again

- `κ−χ` measured by A (A-S30) and C (C-T19, C-T21) on different data. **Both stand; C's is the
  third confirmation.** Neither needs redoing.
- `REACTION_VS_PREDICTION` closed identically in both lanes. **Closed. Do not reopen in either.**
- `RESPONSE_IS_LIQUIDITY_CONDITIONED` established twice. **Once is enough.**
- The impact-exponent family: A holds `ZETA_IS_NOT_DELTA` and `DELTA_IS_ASSUMED_NOT_MEASURED`;
  C holds `THREE_IMPACT_EXPONENTS_MEASURE_THREE_DIFFERENT_OBJECTS` and
  `GAMMA_AND_DELTA_ARE_CONSISTENT_WITH_ONE_SCALING_FUNCTION`. **C is further along. A should not
  re-derive it.**

---

## 6 · The one thing no lane holds

Every lane hit the fee from a different side and none can resolve it:

```
A:  FEE_TIER_IS_WORTH_8_7X_ON_THE_REQUIRED_HORIZON
A:  THE_FEE_TIER_LOOKUP_IS_THE_CHEAPEST_ACTION_AVAILABLE
A:  FEE_IS_A_LINEAR_LEVER_ON_SHARPE
C:  MAKER_BENEFIT_IS_3_BPS_MINUS_AN_UNMEASURED_ORDER_1_BPS_TERM
```

The number is not in the repo (`.env` has no fee key, confirmed by A-S45) and not in any lane's
reach. It is `OD-033` in `OPERATOR_DECISION_QUEUE.md`.
